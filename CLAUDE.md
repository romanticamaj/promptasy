# Promptasy

> 一款在瀏覽器裡「邊玩邊學 prompt engineering」的探索型小遊戲。
> 玩家在一個有氛圍的世界裡探索、遇到問題、在地圖上找線索，然後**寫 prompt** 去解決它；
> 解開後獲得經驗、解鎖新區域、把技巧收進圖鑑。核心是兩件事：**學習** ＋ **搜集**。

- **Repo / 品牌**：`promptasy`（Phase 29 由 `promptarcade` 改名；prompt ＋ fantasy）
- **一句話定位**：Learn Prompt Engineering by Playing.
- **狀態**：規劃中（尚未開工）。本檔是專案北極星，所有開發決策以此為準。

---

## 這份文件是什麼

這是 Promptasy 的**長期目標文件（north star）**與 AI 開發代理（Claude Code）的工作指南。
它刻意**不把美術風格、世界觀、關卡形式寫死**——只鎖定不可動搖的核心價值，其餘方向開放給每次迭代自行判斷、逐步演化。
任何一次開發，先讀這份文件，再決定「這次要把遊戲往哪個方向推進一格」。

---

## /goal — 長期、開放、不設限方向的目標

> **持續打造並精進 Promptasy：一款用「探索世界 ＋ 寫 prompt 解謎」來學會 prompt engineering 的瀏覽器遊戲。**

這是一個**沒有終點的 long-running 目標**。每一次執行，挑一個「當下投報率最高」的改進，端到端做完、確保遊戲仍可執行（`npm run dev` 能跑、無 console error），然後把進展記到本檔最底的變更紀錄。

方向完全開放——世界長怎樣、角色是誰、關卡是哪種形式、美術走什麼調性，**都由你（開發代理）自由發揮**，只要每次都讓遊戲更接近以下五個方向：

1. **學習更扎實** — 更完整、正確地涵蓋 68 條 prompt 技巧，且每條保留官方出處。
2. **探索＋升級＋搜集的體感更強** — 玩家想繼續走下去、想收集、想變強。
3. **感知美術價值更高、成本更低** — 用免費資產＋強美術指導，做出「看起來很貴」的畫面。
4. **離線評分更穩** — 不需 API key，核心迴圈就能判定 prompt 好壞並給有用回饋。
5. **手感與打磨更好** — 移動、鏡頭、音樂、UI、回饋節奏都更順。

**唯一不可妥協的護欄**（見下方「開發護欄」）：學習優先、內容正確且附出處、核心迴圈可離線、進度存在 localStorage 且可重置、每次交付都可執行。除此之外，放手去做。

**每次迭代的節奏**：讀本檔 → 選一個最高槓桿的改進 → 實作到可玩 → 自我驗證（能跑、能玩、無錯）→ 更新變更紀錄與路線圖。不求一次做完，求每次都往前一格且不倒退。

---

## 遊戲願景與核心體驗

玩家是一個在陌生世界裡的旅人。世界由數個**區域（regions）**組成，每個區域對應一組 prompt 技巧主題。
你到處走、看風景、聽音樂；走到某處會**遇到一個問題**（卡住的角色、壞掉的裝置、鎖住的門、需要被說服的守衛……）。
問題旁邊或地圖上藏著**線索**（提示該用什麼技巧）。你打開 prompt 主控台，**寫一段 prompt** 去解決它。
系統即時判定你的 prompt 是否用對技巧、給回饋；解對了 → 拿經驗、解鎖、把該技巧收進**圖鑑**、世界對你打開更多。

目標情緒：**平靜地探索、時不時的靈光一閃、收集的滿足感、變強的成就感。**

---

## 設計支柱（Design Pillars）

1. **Learn by doing** — 每個機制都在教一個真實、有官方出處的 prompt 技巧；不是包著遊戲皮的填空題。
2. **Exploration & Progression** — 半開放世界，靠技巧解鎖漸進推進，明確的「升級感」。
3. **Collection（搜集）** — 一本會被慢慢填滿的技巧圖鑑＋廠家徽章；收集本身就是動力。
4. **Lowest cost, highest perceived art** — 用免費 CC 資產，靠**美術指導**（打光、色調、後製、氛圍）而非資產數量做出質感。
5. **Offline-first** — 核心遊玩迴圈不需要任何 API key 或後端；可選的「真 LLM 模式」是加分項，不是前提。
6. **Accurate & sourced** — 技巧內容與判定標準來自官方文件；圖鑑每一條都能點到出處。

---

## 參考（Reference）

視覺與體感參考 **thibault-introvigne.com** 那類作品：可探索的 3D 世界、有角色、有氛圍、有音樂、靠「發現」推進。
**我們模仿的是那個「探索 / 氛圍 / 發現 / 配樂」的體感，不是任何特定畫風。**

> **美術方向完全開放，不綁定任何既有風格（先前的「水晶」版只是內容來源，不是美術基準）。**
> 每次迭代可以嘗試不同調性（低多邊形、手繪、剪影、霧氣、夜景、島嶼、遺跡……），以「最低成本做出最高感知質感」為準。

---

## 核心玩法迴圈（Core Loop）

```
探索世界  →  遇到問題（challenge）  →  蒐集線索 / 看提示  →
開啟 Prompt 主控台  →  寫一段 prompt  →  評分引擎判定（離線）  →
回饋（過 / 部分過 / 未過 ＋ 提示）  →  過關：經驗＋解鎖＋收進圖鑑  →  繼續探索
```

- **一關 = 一個技巧的實戰**：問題情境會逼你用上某個（或某幾個）技巧才解得開。
- **回饋要教學**：沒過時，給「你缺了什麼」的具體提示，並連到該技巧的官方出處。
- **可重玩**：關卡可重寫更好的 prompt 拿更高評價（S/A/B/C），鼓勵精進。

---

## 內容來源與課程（Curriculum）

課程內容**沿用 `prompting-crystal.html` 裡的資料，但只取內容、丟掉水晶視覺**。
該檔的 `<script>` 內含以下可直接抽出的資料結構：

- `DATA` — 15 個主題、68 條技巧（每條含 `t` 標題、`tip` 說明、`ex` 範例、`note` 模型差異、`v` 適用廠家、`src` 官方出處）。
- `COMPARE` — 各廠差異對照矩陣。
- `BUILDER` — Prompt 組裝積木（角色／任務／脈絡／few-shot／CoT／格式／護欄／自我檢查）。
- `BEFOREAFTER` — 弱→強 prompt 對照範例。
- `SOURCES` — 四廠官方文件出處總表（共 24 個連結）。

**第一步工作**：把這些抽成乾淨的 `src/data/curriculum.json`（含 techniques、groups、vendors、sources、challenges），之後遊戲只讀 JSON。
既有的 5 大主題分群（撰寫基本功 / 示範與推理 / 脈絡與長文 / 流程與代理 / 角色與參數）＋工具站，可直接對應到**世界的區域**。

> **鐵則：技巧內容與出處一字不改地保留正確性；圖鑑每條都要能點到原始官方連結。**

---

## 關卡與評分引擎（Challenge & Rubric — 離線核心）

這是「低成本、離線、可玩」的關鍵。**不需要呼叫 LLM 也能判定 prompt 好壞。**

每個 challenge 用資料定義：

```jsonc
{
  "id": "gate-of-clarity-01",
  "region": "foundations",
  "teaches": ["clarity", "format"],        // 對應 technique id
  "scenario": "守門人聽不懂你的請求，除非你把要求講得夠具體。",
  "clue": "試著明確指定『輸出格式、長度、風格』。",
  "rubric": [                               // 離線檢查：技巧是否『出現』，不是比對固定字串
    { "check": "specifiesFormat", "weight": 2, "hint": "指定輸出格式（表格 / 條列 / 一句話…）" },
    { "check": "hasConstraint",   "weight": 1, "hint": "加一個可量化的限制（例如『3 句話』）" },
    { "check": "positiveFraming", "weight": 1, "hint": "說『要做什麼』而非只說『不要』" }
  ],
  "pass": 3,                                // 達到分數即過關；更高分 → 更高評價
  "source": "https://ai.google.dev/gemini-api/docs/prompting-strategies"
}
```

- **檢查器（checks）是一組可重用函式**：`hasRole`、`hasFewShot`（偵測 example/範例結構）、`specifiesFormat`、`hasConstraint`、`positiveFraming`、`hasDelimiters`、`asksToVerify`、`groundsInContext`… 用關鍵字 / 正則 / 結構偵測，給**部分分數＋提示**。
- **反作弊**：判定「技巧是否被運用」而非死背字串；空泛亂寫拿不到分。
- **選配進階模式**：玩家可自填 API key，把 prompt 真的送去 LLM、用另一段 rubric prompt 評「輸出」品質。**這是加分模組，核心迴圈永遠不依賴它。**

---

## 進度、升級與搜集（Progression & Collection）

- **經驗 / 等級**：解關得 XP、升等；等級或前置技巧達標才解鎖新區域（soft gating）。
- **技巧＝能力**：解鎖一個技巧後，Prompt 主控台就多一個對應的輔助（例如解鎖 few-shot 後可插入 example 積木）——**學到的東西立刻變成變強的工具**。
- **圖鑑（Codex / 搜集）**：68 條技巧的收集冊，收齊一個區域 → 該區「精通」；每條可展開看說明、範例、官方出處。
- **廠家徽章**：完成標記某廠的關卡累積該廠徽章（OpenAI / Anthropic / Google / xAI），四廠全收集 = 隱藏成就。
- **評價制**：每關可重寫拿 S/A/B/C，鼓勵回頭精進，增加重玩與學習深度。

---

## 存檔與重置（localStorage）

- 進度存在 **localStorage**，key 命名空間 `promptasy.v1.*`（改名前是 `promptarcade.v1.*`，`load()` 會自動搬過來），資料含版本欄位以利未來遷移。
- 存：等級 / XP、已解鎖區域、已收集技巧、各關最佳評價、廠家徽章、設定（音量、選的音樂、畫質）。
- **一鍵重置**：設定頁提供「重置進度、重新學習」，清空存檔並回到起點（要二次確認）。
- 存檔 schema 範例：

```jsonc
// localStorage["promptasy.v1.save"]
{
  "version": 1,
  "xp": 320, "level": 4,
  "unlockedRegions": ["foundations", "reasoning"],
  "collected": ["clarity", "positive", "format", "..."],
  "bestGrades": { "gate-of-clarity-01": "A" },
  "badges": { "openai": 3, "anthropic": 2, "google": 1, "xai": 0 },
  "settings": { "music": "ambient-01", "volume": 0.6, "quality": "high" }
}
```

---

## 技術棧（低成本 / 高美術 / 可離線）

- **建置**：Vite。語言 vanilla JS/TS，不上重框架（遊戲不需要 React 的心智模型）。
- **3D**：three.js。第三人稱角色控制器（WASD／點擊移動）＋跟隨鏡頭；若某風格用 2.5D / 俯視更省更好看也可以。
- **感知美術靠「指導」不靠「量」**：three.js 燈光 ＋ 後製（`EffectComposer`：bloom、vignette、景深、色彩分級）＋ 霧 ＋ 一致的配色 ＋ HDRI 環境光。這是「便宜卻高級」的來源。
- **免費 CC 資產**（標好授權）：角色與動畫用 **Mixamo**；模型用 **Kenney / Quaternius / Poly Pizza**；材質/HDRI 用 **Poly Haven**。優先低多邊形、風格化，降低成本又好統一。
- **音訊**：Web Audio API 或 **Howler.js**；分區背景樂、進出區域**淡入淡出**切換；玩家可切歌／加自己的音樂。音檔放 `public/`，不 base64 內嵌。
- **持久化**：localStorage（見上）。無後端、無帳號。
- **部署**：純靜態（GitHub Pages / Netlify / Vercel 皆可）。

---

## 專案結構（建議）

```
promptasy/
├─ CLAUDE.md                 # 本檔：北極星 + /goal + 變更紀錄
├─ README.md                 # 對外說明（含 SEO 關鍵字，見下）
├─ index.html
├─ package.json
├─ vite.config.js
├─ public/
│  ├─ audio/                 # 分區背景樂
│  └─ models/ textures/      # CC 資產（附 LICENSE 註記）
└─ src/
   ├─ main.js                # 進入點
   ├─ engine/                # renderer、camera、loop、postprocessing
   ├─ world/                 # 區域、地圖、互動觸發區
   ├─ player/                # 角色控制器、跟隨鏡頭
   ├─ challenges/            # 關卡載入 + rubric 評分引擎（離線）
   ├─ prompt/                # Prompt 主控台 UI + 技巧積木
   ├─ progression/           # XP、解鎖、圖鑑、徽章
   ├─ audio/                 # 音樂管理、分區淡入淡出
   ├─ ui/                    # HUD、圖鑑、設定、重置
   ├─ save/                  # localStorage 讀寫 + 版本遷移 + reset
   └─ data/
      └─ curriculum.json     # 從 prompting-crystal 抽出的技巧/群組/出處/關卡
```

---

## 開發護欄（Agent Working Agreement）

> **世界的規則寫在 [`WORLD.md`](./WORLD.md)**（世界觀、視覺調性、互動文法、場景敘事原則、命名規範、效能與碰撞的硬規則）。
> 動到世界內容（場景、道具、互動、文案）之前先讀它，做完照它最後一節的**維護檢查表**逐條過一遍。本檔的護欄仍優先於其中的任何美學主張。

**只有這幾條不可妥協，其餘方向自由：**

1. **學習優先**：任何炫技都不能犧牲「玩家真的學到 prompt 技巧」。
2. **內容正確且附出處**：技巧說明與 rubric 判定忠於官方文件；圖鑑每條可點到原始連結；不得杜撰技巧或來源。
3. **核心迴圈可離線**：不靠 API key 也能玩、能被評分。真 LLM 模式只能是選配。
4. **進度可存可重置**：localStorage 存檔、設定頁有「重置重學」。
5. **每次交付可執行**：`npm run dev` 能跑、無 console error、核心迴圈可玩；不半途留下壞掉的建置。
6. **資產授權乾淨**：只用可商用/CC 資產並於 `public/**/LICENSE` 註記來源。
7. **不倒退**：新迭代不得破壞既有可玩內容；大改動前先確認舊功能仍在。

護欄之外——世界觀、美術、關卡形式、敘事、進程曲線——**放手嘗試、允許換方向**。

---

## 每次迭代的完成定義（Definition of Done）

- [ ] 選定的改進端到端完成，且**實際可玩 / 可見**。
- [ ] `npm run dev` 正常、無 console error。
- [ ] 若動到內容：出處連結正確、可點。
- [ ] 若動到存檔：新舊 schema 相容或有遷移；reset 仍正常。
- [ ] 在本檔「變更紀錄」加一行：做了什麼、下一步建議。

---

## 工程 Harness（開發與驗證流程 — Phase 1–30 磨出來的做法）

> 這是本專案實際運轉 30 個 phase 的工作流程。任何 AI 代理（Claude Code / Codex / 其他）接手時照這套走；
> 同一份流程也放在repo 根目錄的 `AGENTS.md`。

### 每個 Phase 的節奏

1. **讀北極星**：先讀本檔（護欄＋變更紀錄最後幾條）與 `WORLD.md`（互動文法、擺放規則、效能與碰撞硬規則、29 項維護清單）。
2. **實作**：一個 phase 一個明確目標，端到端做完。動到玩家看得到的中文字串 → **一定要重跑 `npm run fonts`**（語料指紋測試不過就是在提醒你這件事）。
3. **驗證**（見下方測試策略）。
4. **收尾**：在本檔「變更紀錄」加一行（繁體中文：做了什麼＋下一步建議）→ commit ＋ push。
5. **絕不碰**：使用者的 dev server（port 5175）與 `vite.config.js`；自己開的測試伺服器用別的 port、結束時要清乾淨（含整個 process group，不留孤兒 Chrome）。

### 測試金字塔（由快到慢）

| 指令 | 內容 | 耗時 | 什麼時候跑 |
|---|---|---|---|
| `npm run test:rubric` | 1.6 萬+ 斷言：評分引擎、資料完整性、出處健檢、碰撞審計、中文掃描、字型語料指紋、存檔遷移 | ~15 秒 | **每次改動都跑**（最便宜的安全網） |
| `npm run test:playtest` | 每關「照提示一定過得了」的門檻：範例解 ≥A、快速填入必過、弱起手必不過、誤判迴歸案例 | ~10 秒 | 動到關卡資料 / 檢查器時 |
| `npm run build` | Vite 建置 | ~2 秒 | 每次改動都跑 |
| `npm run test:e2e` | 1600+ 項無頭瀏覽器實玩（走路、序章、刻碑、過關、分享…） | **15–20 分鐘**（此機為 SwiftShader 軟體渲染 ~200ms/幀） | 大改動、動到互動流程時 |

### 測試策略（重要 — 這是成本控制的核心）

- **先問再跑**：如果這次改動**項目不多**（純文案、單一元件樣式、資料微調…），先詢問使用者要「全跑 / 只跑快的（rubric＋build）/ 不跑」，不要預設把 15–20 分鐘的 e2e 跑下去。使用者明說「不用跑測試」就照辦（但 rubric＋build 這種十幾秒的快檢通常仍值得跑，除非連這個也被排除）。
- **不重複驗證**：實作交給 subagent 時，subagent 跑過全綠的 e2e，orchestrator **不要再跑一次完整 e2e**——只做快速驗證（rubric ＋ build ＋ curl dev server 確認 200）。
- **已知 flaky**：e2e 有少數「動畫時序類」斷言（拖曳、火盆亮度、風鈴擺動）在軟體渲染下偶發失敗——失敗清單**只有**這幾條時重跑一次即可，不要追；若要根治，把斷言改成輪詢式（poll until），不要用固定 sleep 對齊牆鐘時間。
- **測試要防「空泛通過」**：幾何/版面斷言要先確認元素真的可量測（曾發生在錯誤的幕次量到 0×0 而全部空過）；新增功能的斷言先讓它失敗一次再讓它通過。

### 慣例與地雷

- **Subagent 分工**：大型實作交給 subagent（一個 phase 一個 agent、附完整 brief：現狀、目標、驗證要求、禁區）；研究類（抓文件）可多個並行，但**寫程式的不要並行**（會互踩檔案）；並行 agent 一律指定「不要碰 CLAUDE.md」，changelog 由 orchestrator 統一補。
- **中文字串 → `npm run fonts`**：CJK 子集是掃描全部 `src/**` 與 `src/data/*.json` 的語料切出來的；漏跑會被指紋測試攔下。
- **內容正確性（護欄 2）是紅線**：`curriculum.json` 一個位元組都不能動；遊戲自撰的翻譯/教學一律放獨立的 `authored: "game"` 資料層，並附真實官方連結；官方文件過時要用「時代註記」層標注，不改原文。
- **音檔後製慣例**：BGM 統一 -20 LUFS、SFX 峰值 -6 dBFS、AAC(m4a) 進 `public/audio/`，授權逐檔登記 `public/LICENSE.md`；檔案缺席時合成音自動後備（離線護欄）。
- **e2e 的埠**：測試 harness 用自己的 port（5198/5199/9333…），跑完殺掉整個 process group；殘留的無頭 Chrome 會佔 CDP 埠讓下一輪測試接錯瀏覽器。

---

## 路線圖（Roadmap — 可調整）

- **M0 內容抽取**：把 prompting-crystal 的資料轉成 `curriculum.json`（技巧＋出處＋分群）。
- **M1 可玩骨架**：一個區域、可移動角色＋鏡頭、一個 challenge、Prompt 主控台、離線 rubric、過關回饋、localStorage 存檔＋reset。
- **M2 進程與搜集**：XP／等級、圖鑑、區域解鎖、廠家徽章、評價制。
- **M3 內容鋪滿**：5 大區域對應 15 主題、覆蓋 68 技巧，每區數個 challenge。
- **M4 美術與氛圍**：後製、燈光、資產、轉場——把「感知質感」拉起來。
- **M5 音樂與手感**：分區配樂＋淡入淡出、移動/鏡頭/回饋節奏打磨。
- **M6 打磨與選配**：成就、教學引導、（選配）真 LLM 評分模式、無障礙、行動裝置。

> 路線圖是建議不是枷鎖；哪個環節投報率最高就先做哪個。

---

## 非目標（Non-Goals）

- 不做多人連線、不做帳號系統、不做後端資料庫。
- 核心迴圈不強制接 LLM API。
- 不追求 3A 級寫實美術；追求的是**風格化 ＋ 高感知質感 ＋ 低成本**。
- 不為了畫面犧牲「學到東西」。

---

## SEO（給 README 與網站 head）

- `<title>`：`Promptasy — Learn Prompt Engineering by Playing`
- meta / repo 描述：`A browser game to learn prompt engineering by playing — explore a world and solve challenges by writing prompts. Techniques from OpenAI, Anthropic, Google & xAI, each cited.`
- GitHub topics：`prompt-engineering` `learn-prompting` `game` `gamification` `llm` `education` `threejs` `webgl`
- 鎖長尾詞：`prompt engineering game`、`learn prompt engineering by playing`、`interactive prompt engineering practice`。

---

## 指令（How to run）

```bash
npm install
npm run dev      # 本機開發
npm run build    # 靜態輸出到 dist/
npm run preview  # 預覽 build 結果
```

---

## 變更紀錄（Changelog — 每次迭代往下加一行）

- _(尚未開工。第一次迭代請從 M0 內容抽取或 M1 可玩骨架開始，並在此記錄。)_
- **2026-07-26 · Phase 1（M0 內容抽取 ＋ 專案骨架）**：用 `node:vm` 沙箱從 `prompting-crystal.html` 逐字抽出 `DATA / COMPARE / SOURCES / BUILDER / BEFOREAFTER / TOOLS / GROUPS`，產出 `src/data/curriculum.json`（5 區域 / 15 主題 / 68 技巧 / 105 條技巧出處 ＋ 24 條官方出處總表 ＋ 8 塊 builder ＋ 6 組 before-after ＋ 7 列對照矩陣），技巧文字與官方連結一字未改並通過驗證（每條技巧皆有 ≥1 出處連結）；5 大分群直接沿用原檔 `GROUPS` 常數（非自行推測）並對應為遊戲區域 id（foundations / reasoning / grounding / orchestration / config）。同時建好 Vite 骨架：`package.json`、`vite.config.js`（`base:'./'` 供純靜態部署）、含 SEO title/description 的 `index.html`、`src/main.js` 開機畫面（顯示已載入技巧數與五大區域）、`src/styles.css`、README、`.gitignore`、`public/` 授權說明，並依建議結構建立 `src/{engine,world,player,challenges,prompt,progression,audio,ui,save,data}` 與 `public/{audio,models,textures}`；已安裝 vite 6 / three 0.170 / howler 2.2。驗證：`npm run build` 通過、`npm run dev` 可開且頁面正確渲染、無 console error。**下一步建議**：M1 可玩骨架——先寫 `src/challenges/` 的離線 rubric 檢查器（`hasRole` / `hasFewShot` / `specifiesFormat` / `hasConstraint` / `positiveFraming` …）與第一個 challenge，再接 `src/save/` 的 localStorage 存檔與 reset，最後才上 three.js 世界與角色控制。
- **2026-07-26 · Phase 2（M1 可玩骨架 ＋ M2 進程與搜集）**：完成第一個端到端可玩的垂直切片。**離線 rubric 引擎**（`src/challenges/checks.js` ＋ `rubric.js`）實作 13 個可重用檢查器（`hasRole` / `assignsTask` / `hasFewShot` / `specifiesFormat` / `hasConstraint` / `positiveFraming` / `hasDelimiters` / `asksToVerify` / `groundsInContext` / `hasStepByStep` / `hasAudience` / `givesOutForUncertainty` / `explainsWhy`），中英雙語偵測、支援部分分數（1 / 0.5 / 0）與「證據式」教學回饋；反作弊採結構偵測而非關鍵字比對（`hasConstraint` 需數字＋單位、`hasFewShot` 需成對 input→output、prompt < 15 字一律 0 分、關鍵字堆砌測試驗證不會全過）。**關卡**：`src/data/challenges.json` 新增 foundations 區 6 關（清晰之門 / 郵箱精靈的分揀台 / 迷路的自動機 / 擬態之鏡 / 長卷檔案室 / 議會信使試煉），`teaches` 與每條 rubric 的 `techniqueId` 全部對應 curriculum 真實技巧 id、`source` 為該技巧本身的官方連結（測試強制驗證）。**世界與角色**：three.js 程序化低多邊形夜色高原（sin/cos 高度場、頂點著色地形、instanced 碎石與立石環、天空漸層 dome、霧、hemisphere＋月光＋暖色補光、bloom ＋ 自寫 vignette/色彩分級 shader、圓形光點粒子），第三人稱膠囊旅人（提燈點光源、走路起伏、平滑轉向）＋ 跟隨鏡頭（滑鼠拖曳 / Q・R 轉向、滾輪縮放）、地形貼地、世界邊界，6 個發光石座（走近顯示「按 E 互動」、通關後轉為暖金並標上評價）、4 個鎖住區域的屏障＋遠方島嶼剪影＋解鎖條件說明。**Prompt 主控台**：情境／可展開線索／rubric 預覽（每條可點官方出處）／已解鎖的 builder 技巧積木一鍵插入／textarea（Ctrl+Enter 送出、打字不會驅動角色）／結果面板（S-A-B-C 評價、得分條與門檻標記、每條檢查 過/部分/未過 ＋ 證據 ＋ 教學提示 ＋ 官方出處連結、XP、新收集技巧、新解鎖區域）。**進程與存檔**：XP／等級曲線、依評價給 XP 且重玩只補差額、`teaches` 自動收進圖鑑、廠家徽章由 collected 重算（冪等）、區域依等級 soft gating、`promptarcade.v1.save` 容錯讀寫＋欄位正規化＋版本遷移掛勾＋二次確認重置。**UI**：HUD（等級/XP 條、區域、最近目標、互動提示、浮動訊息）、圖鑑（5 區域 → 15 主題 → 68 技巧，未收集顯示 ???，已收集可展開看說明／範例／模型差異／全部官方出處，含廠家徽章與各區精通度）、設定（音量／靜音／畫質／進度統計／重置）、首次進入的操作教學（看過即存旗標）。**音訊**：Web Audio 即時合成的環境墊音＋隨機鐘聲＋過關提示音，無外部音檔。**驗證**：`npm run test:rubric`（新增 `scripts/test-rubric.mjs`）309 個斷言全過，涵蓋每個檢查器的 good/weak/bad fixture、反作弊、評價門檻、6 關示範解答皆可拿 A 以上、資料完整性（teaches/source 皆存在且為 https）、存檔毀損復原與正規化、進程 XP/升等/收集/徽章/重玩不刷分/重置；`npm run build` 通過；以 headless Chromium 實際載入 `npm run dev` 走完整流程（移動 → 走到石座 → E 開啟主控台 → 送爛 prompt 得到 4 條缺失提示＋5 個出處連結 → 送好 prompt 得 S ＋88 XP ＋3 條技巧入圖鑑 → localStorage 確實寫入 → Esc 關閉 → C 開圖鑑（3 已收集 / 65 未收集 / 4 徽章 / 出處連結可點）→ O 開設定 → 二次確認重置成功 → 重新整理回到起點），**零 console error**。**下一步建議**：M3 內容鋪滿——先做 `reasoning` 區（fewshot / cot / reasoning 三主題）的地形與 4–6 關，讓 `hasFewShot` / `hasStepByStep` / `asksToVerify` 這幾個已實作但目前只在測試中用到的檢查器真正上場；同時把區域切換（進出區域的鏡頭與音樂淡入淡出、跨區傳送點）補上，並考慮把 rubric 的 `pass` 門檻與 XP 曲線依實際遊玩節奏再校一次。
- **2026-07-26 · Phase 3（M3 內容鋪滿：四片新土地 ＋ 68 技巧全可收集）**：**關卡**：`challenges.json` 由 6 關擴到 **26 關**（每區 5 關）——示範與推理（示範迴廊 / 一致的燈列 / 靜默的推理者 / 思考室 / 火力熔爐）、脈絡與長文（引文閱覽台 / 不知之井 / 長卷之塔 / 查證之泉 / 檔案庫封印）、流程與代理（拆解工作台 / 草稿之輪 / 工具鍛造間 / 不可逆之門 / 回音工坊）、角色與參數（面具工坊 / 優先序階梯 / 刻度儀之室 / 四要素之鏡 / 抉擇之秤）；**68 條技巧全部至少被一關教到**（測試強制驗證涵蓋率 68/68），`source` 與每條 rubric 的 `techniqueId` 一律指向 curriculum 內真實存在的官方連結。**檢查器**：新增 9 個離線檢查（`asksToCiteSources`／`putsQuestionLast`（結構性偵測「資料在前、問題在後」）／`decomposesTask`／`asksToRefine`／`definesTools`／`setsPersistence`／`requiresConfirmation`／`mentionsParameters`／`keepsPromptLean`（推理模型：短、直接、無多餘鷹架）），全部中英雙語、支援部分分數並附 good/weak/bad fixture；同時讓 `imperativeVerb` 認得前置子句、`hasDelimiters` 認得多個 `#` 章節標題（GPT-4.1 骨架）、`hasStepByStep` 認得「think very hard」、`givesOutForUncertainty` 認得「資訊不足就反問使用者」。**世界**：地圖重寫成「中央高原 ＋ 四條橋 ＋ 四片各有調性的土地」（半徑 150、程序化高度場依區域混色：階梯迴廊、書架台地、齒輪工坊、面具劇場），地面依區域主色染色、每區一盞主色補光與專屬造景（懸浮思考環／書架與飄浮書頁／會轉的齒輪與桁架／觀眾席與面具柱），橋中央有實體閘門（鎖住時真的走不過去、解鎖時屏障淡出＋擴散光環），HUD 會跟著跨區更新區域名與「下一個目標」（本區全破就指向下一站）。**進程**：解鎖鏈改為「等級 ＋ 前一區通關數」雙條件（foundations → reasoning Lv.3 → grounding Lv.4 → orchestration Lv.5 → config Lv.7，各需前一區通關 4 關），閘門說明會即時算出還差什麼。**完成度回饋**：區域精通（圖鑑暖金封印 ＋ 世界補光轉金 ＋ toast）、68 全收集 ＋ 四廠徽章達標的隱藏成就慶祝畫面（附四廠官方文件入口，存檔加 `finaleSeen` 旗標、`save.js` 的 flags 改為通用布林欄位以相容新旗標）。**驗證**：`npm run test:rubric` 1521 個斷言全過（含 68 涵蓋率、26 關示範解答全部 ≥ A（25 關 S）、9 個新檢查器 fixture、解鎖鏈逐區推進、地形可走性與石座落點）；`npm run build` 通過；headless Chromium 實跑 56 項檢查全過、**零 console error**（四區石座齊全、跨區 HUD 更新、每區開一關送示範解答拿 S、閘門鎖住時走不過去／解鎖後可通行、圖鑑 68 條與精通封印、隱藏成就畫面、localStorage 存檔與重置）。**下一步建議**：M4/M5 打磨——分區背景音樂與進出區域的淡入淡出、跨區快速旅行（已解鎖區域的傳送點）、鏡頭在窄橋與階梯地形上的避障，以及依實際遊玩節奏再校一次各區 `pass` 門檻與 XP 曲線（目前以「A 評價順順走」為基準）。
- **2026-07-26 · Phase 4（M4 美術與氛圍 ＋ M5 音樂與手感 ＋ M6 小打磨）**：**美術**：整套色本重打光成「平靜的夜間探索」——地面基色壓暗（`0x3a4d5e→0x2a3947`）、hemisphere 1.45→0.52、月光 1.6→0.88、rim 0.62→0.42、曝光 1.25→1.02，畫面從「白天雪原」變回夜色；天空新增程序化**星空**（900 顆 Points ＋ shader 閃爍 ＋ 冷暖色分佈）、**月盤與月暈** sprite、兩層飄動的**極光帶**；後製改寫成 filmic 色彩分級（只抬暗部的 shadow lift、S 曲線、飽和度、跨區可漂移的色偏、柔和暗角、`uSwell` 光湧），bloom 調成 0.58/0.82/0.55；新增**貼地霧氣**（橋頭三段 ＋ 各區邊緣共 40 片會慢慢旋轉起伏的柔光平面）、**螢火依區域改密度與顏色**（`REGION_ATMOSPHERE.motes` 0.7–1.35，帶頂點色）；每座石座加上**遠距可見的光柱** ＋ **走近亮起的地面光環**，三種狀態（未解＝呼吸脈動／走近＝亮起加速／已解＝安靜暖金）一眼讀得出來，等於不用小地圖的導航；新增 `REGION_ATMOSPHERE` 五區氣氛表（霧色 / 色偏 / 環境光 / 霧遠近 / 曝光 / 螢火密度全部不同），**跨區時由引擎平滑漂移 ＋ 一記光湧**，過橋變成一個事件。**音訊**：`audio.js` 從單一環境墊音改寫成**分區生成式配樂**——五區各有自己的根音（110 / 130.81 / 98 / 116.54 / 146.83 Hz）、音階（自然音 / Lydian / Dorian / 四度堆疊 / Mixolydian）、pad 音色、低通截止、鐘聲密度與曲名，跨區用 `setTargetAtTime` 交叉淡入淡出；加上以噪音 impulse 生成的 convolver 空間感；音效表補齊 open / close / submit / pass（**依 S-A-B-C 加碼泛音**）/ fail（柔和不懲罰）/ unlock 四音符 / codex / 腳步（節流）/ toast，全部合成、零音檔；音量滑桿即時作用並寫進存檔。**手感**：移動改成加速／減速（damp 收斂，放開後有慣性滑行、撞牆該軸速度歸零）、鏡頭有**延遲與前瞻**（跑起來像被拉著走）、**raycast 避障**修掉 Phase 3 的鏡頭穿模（只對標記了 `blocksCamera` 的書架／齒輪／立石／閘門柱做碰撞，不含地形與粒子）、奔跑時 FOV 52→58.5、角色新增走路擺動／轉彎側傾／站立呼吸；HUD 加上 XP 條發亮、升等彈跳、**評價印章動畫**、S 評價與區域精通的螢幕級慶祝、toast 依長度決定停留時間並限制同時 4 則。**M6**：新增**開場標題卡**（品牌名 ＋ 定位句 ＋ 按任意鍵開始，同時提供 AudioContext 需要的使用者手勢並蓋住第一幀編譯）、面板全面補上**鍵盤無障礙**（Tab 焦點鎖、開啟自動 focus、關閉還原焦點、`aria-labelledby` / `aria-live`、可見的 `:focus-visible` ring、`prefers-reduced-motion`）、**畫質可即時切換**（後製 / 陰影 / pixelRatio 一起切，關掉後製時自動補曝光讓兩種畫質亮度一致，並持久化）、`prompting-crystal.html` 移到 `reference/`（內容 md5 未變）、README 全面翻新（特色 / 操作表 / 截圖區塊 / 專案結構 / 測試指令）。**驗證**：`npm run test:rubric` 由 1521 擴到 **1640 個斷言全過**（新增音訊模組 API smoke：五區配樂互異、無 AudioContext 時安靜降級不丟例外；氣氛表五區霧色／色偏互異且數值合理；設定持久化：音量／靜音／畫質寫入重讀、非法值正規化、改設定不動進度）；`npm run build` 通過；新增 `npm run test:e2e`（`scripts/headless-check.mjs`，只用 node 內建 fetch/WebSocket ＋ 系統 Chrome 走 CDP，不依賴 puppeteer/playwright），headless 實跑 **95 項檢查全過、零 console error**（標題卡→教學→加減速與慣性→奔跑 FOV→走近石座亮起→送爛 prompt 拿提示→送示範解答拿 S ＋印章動畫＋慶祝→焦點管理與 Tab 鎖→圖鑑 68 條→音量即時生效並持久化→畫質即時切換來回→五區配樂切換→閘門鎖住走不過去→種存檔重載後跨區（配樂與霧色都換了）→鏡頭避障→重置清空），收尾會殺掉整個 process group 不留孤兒 dev server。**下一步建議**：M6 續攤——已解鎖區域的傳送點／快速旅行、行動裝置（觸控搖桿與面板版面）、（選配）真 LLM 評分模式，以及依實際遊玩節奏再校一次各區 `pass` 門檻與 XP 曲線；美術上可再加「風吹過草地／布幔」這類單一 shader 的動態，成本低但畫面會再活一階。
- **2026-07-26 · Phase 5（主角改造 ＋ 場景敘事：世界開始會說話）**：**主角**：膠囊換成真正的人物 —— 新增 `src/player/character.js`，用巢狀 `Object3D` 搭出 `hips → torso →（neck → head / shoulder → elbow → hand）＋ hip → knee → ankle` 的關節階層，零件是 three.js 基本幾何體（兜帽 ＋ 外袍 ＋ 披肩 ＋ 暖色圍巾 ＋ 背包 ＋ 腰帶 ＋ 靴子 ＋ 手提燈 ＋ 兩顆會發微光的眼睛），**沒有骨架、沒有 GLTF、沒有動畫檔**；動作全部是相位的函式（rigless procedural animation）：左右腿差 π、手與對側腿同相、膝與肘用 `max(0, …)` 夾成單向鉸鏈（膝的最大屈曲落在擺盪中期）、軀幹以兩倍步頻起伏、骨盆與肩帶反向扭轉、頭部反向抵銷並用指數低通穩定視線、轉彎側傾、站立時 0.25 Hz 的不對稱呼吸（吸慢吐快）＋ 0.08 Hz 重心緩移，走 / 跑 / 站之間靠 `speedRatio`／`runRatio` 連續混合（沒有狀態機）；擺幅取自正常步態的關節活動度（髖屈 30°/伸 10°、膝 0–60°、踝 ±25°、骨盆 12° 對肩帶 7°、質心垂直位移約身高 2.6%）；過關時會舉手歡呼 1.25 秒後自己收回去。**場景敘事**：新增 `src/world/props.js`（38 種程序化道具 ＋ 5 座地標 ＋ 12 塊石碑 ＋ 路網），每區 3–4 組**故事小景**（共 16 組：刻到一半的碑林、沒說完的那場話＝兩張對坐石凳與沒收的茶具、初語之池、守望者的營地、沒刻完的思考者、走不到盡頭的階梯、示範的兩張牌、沒收的閱讀角、問題之井、抄書人的桌子、拆到一半的機器、交接到一半的工地、不可逆之門前、後台更衣角、照見自己的一排鏡、只演給自己看的小舞台），成組擺放而非均勻灑裝飾；**每區一個從中央高原就看得到的地標剪影**（斷環 / 無盡階梯塔 / 藏書之樹 / 巨臂吊車 / 面具拱門，高 21–27，周圍強制留白 14–16 公尺 —— 高的東西旁邊要矮才顯得高）；**走出來的路**用頂點色染在地形上（石座環路 ＋ 橋 ＋ 通往小景的岔路，**只改顏色不動高度場**，可行走性判定完全不受影響）；**12 塊世界觀石碑**（`E` 閱讀、程序化刻痕貼圖、走近會亮起，第一次讀給 8 XP，存檔新增 `loreRead` 欄位並由 `normalize` 對舊存檔補預設值）—— 碑文是**風味內容**，測試強制驗證它們不含連結、不含 `source`／`teaches` 欄位，真正的教學與官方出處仍然只在關卡與圖鑑（護欄 2）；每區再加 3 種剪影互異的植被 / 岩石原型（成叢分布而非均勻散佈）、小景旁的螢火群聚、反射水池與立鏡（夜間水面改用「深藍 ＋ 低粗糙高光 ＋ 天光自發光 ＋ 加色反光層」，避免無 environment map 時變成黑洞）。**留白與去雜訊**：石座 / 小景 / 地標 / 石碑組成統一的「淨空清單」（含各自半徑），立石環、碎石、各區既有造景一律避讓；orchestration 的浮空齒輪 22→12、桁架 16→8 並往外推，reasoning 的思考環 26→22 往外推，把中央讓給地標。**效能**：全部程序生成、幾何體與材質共用快取、重複元素（浮階、書葉、螺旋階梯、刻痕、吊車橫檔、卷軸、梯級）用 InstancedMesh；場景 **125k 三角形 / 41 個 InstancedMesh（1020 個實例）/ 47 盞燈 / 229 個 geometry**，低畫質開機時道具不帶實體光源（降到 34 盞）；headless（swiftshader 軟體渲染）A/B 量測：Phase 5 的新內容約佔 22% 幀時間。**驗證**：`npm run test:rubric` 由 1640 擴到 **2423 個斷言全過**（新增石碑資料合法性與「不得帶連結／出處」的護欄檢查、石碑與石座 / 石碑彼此的互動距離、小景與地標的落點與淨空、每區道具種類數、路網涵蓋石座與小景且虛空無路、`loreRead` 的新舊存檔相容與去重、讀碑 XP 只給一次且不進圖鑑 / 不算徽章 / 不足以解鎖新區、重置會清掉已讀）；`npm run build` 通過；`npm run test:e2e` 由 95 擴到 **151 項檢查全過、零 console error**（人形角色的 15 個關節都在、走路時髖與肩真的擺動且左右腿反相、膝蓋不會往前折、慶祝動作會自己收回去、石碑走近提示→`E` 開啟→焦點鎖→第一次給 XP→寫入 localStorage→重讀不再給分、五座地標與 17 組小景與五區植被都在場景圖上、地形帶頂點色、三角形與燈光預算上限）；手感量測改用引擎累加的模擬時間（軟體渲染掉幀時 `clock.elapsedTime` 會與模擬時間脫鉤），既有的移動 / 慣性 / FOV / 跨區 / 閘門 / 圖鑑 / 重置斷言一條未改。**下一步建議**：（a）石碑目前只是 flavor，可以做成「碑文圖鑑」分頁讓收集感延伸到世界觀；（b）小景可加入「解謎後改變」的動態環境敘事（例如通關後茶具被收走、機器被組好），把玩家的進度寫回世界；（c）效能上可考慮把遠處石座的點光源換成純發光體（26 盞是目前最大的單一開銷），或替場景加一張便宜的 PMREM 環境圖，水面與金屬就能真的反射；（d）行動裝置的觸控搖桿與面板版面仍未做。
- **2026-07-26 · Phase 6（UI／字體全面升級：介面開始有「編輯感」）**：**研究先行** —— 先查 Awwwards SOTY / SOTD 的可實作特徵（editorial 型級、oversized 襯線大標配 tracked-out 的全大寫 meta label、線遮罩式分行揭示、expo-out 招牌曲線、feTurbulence 顆粒、髮絲線取代陰影當層級、非對稱分欄）、CJK ＋ Latin 配對的排版準則（W3C clreq、Typotheque、jsclasses 的 1.08× 比例）與遊戲 HUD 可讀性（NN/g 的 100 / 200–300 / 400ms 門檻、狀態不可只靠顏色）。**字型（護欄 3 ＋ 6）**：新增 `scripts/subset-fonts.mjs`（`npm run fonts`），掃描 `index.html ＋ src/data/*.json ＋ src/**/*.{js,css}` 的**全部字元**（保守超集，寧可多切也不漏字），把六套 **SIL OFL 1.1** 字型切成專案專用子集並自架於 `public/fonts/`：**Fraunces**（Latin 大標／評價印章，104 字 70 KB）、**Newsreader**（Latin 長文襯線 132 KB）、**Inter**（介面 82 KB）、**JetBrains Mono**（等寬 28 KB）、**思源宋體 TC**（453 KB）、**思源黑體 TC**（345 KB），**合計 1.11 MB、零 CDN、完全離線**；CJK 檔用可變 wght 400–600 單檔（實測比 400／600 兩個靜態檔小 15%），並帶 `unicode-range` 讓純英文的標題卡不必下載那 800 KB。排版依研究落實三條硬規則：**Latin 字型一律排在 CJK 之前**（否則英數會被中文字型的英文接手）、Latin 面加 `size-adjust`（Fraunces 104% / Newsreader 109%，對齊思源宋體 0.729 em 的 cap-height）、**tracked-uppercase 的 0.22em 字距只給 Latin**（套在漢字上會變成明顯空隙，所以雙語小標拆成 `.zh` / `.en` 兩個 span 各自排版）；同時移除全域 `-webkit-font-smoothing: antialiased`（會削掉暗底上宋體的橫畫）、宋體最小尺寸抬到 15px、加上 `text-autospace`。**使用者自己打的字（textarea）走 `--font-input` 系統字型堆疊**，子集缺字也絕不破圖。授權原文 `OFL-*.txt` 一併散布並在 `public/LICENSE.md` 逐套登記。**設計語言「夜間檔案館」** —— `src/styles.css` 全面重寫成有文件的設計系統：明度階（`--ink-0…4` / `--paper…4`）取代玻璃感、髮絲線（`--rule` / `--rule-gold`）取代陰影當層級、冷星光為介面主色而暖金只留給成就熱點、8pt 節奏、fluid `clamp()` 型級、一條招牌曲線 `--e-out: cubic-bezier(.19,1,.22,1)`、行內 SVG `feTurbulence` 顆粒（零外部圖檔）。**逐面改造**：標題卡改成分字揭示（12 個 `<span>` 依序浮起、髮絲線展開、tagline 與開始鍵接續進場，`textContent` 仍是完整品牌名）；**練習室**重做成「委託書 ｜ 書寫檯」的非對稱分欄（editorial 標頭 ＋ `FOUNDATIONS — TRIAL 01 / 06` 的 meta label、情境用宋體長文排版並加首字放大、線索改成安靜的可展開、rubric 變成髮絲線目錄、textarea 包進會亮起的 `.desk` 並附即時字數／行數、結果依 `--i` 逐條錯開揭示 ＋ 圓形評價印章 ＋ XP 數字 quart-out 跳動計數）；圖鑑改成圖錄式的章節排版（未收集只留一行剪影，重複 65 次的說明收斂成頂端一句）；HUD 安靜化（等級方章 ＋ 髮絲 XP 條、區域名做成中英兩層的刻印並在跨區時播轉場）；設定／成就／石碑／toast／教學全部套同一語言。**修掉一個真 bug**：面板第二次之後開啟不會播進場動畫（`hidden=false` 後缺一次 reflow，補上 `void offsetWidth`）。**驗證**：`npm run test:rubric` 由 2423 擴到 **2509 個斷言全過**（新增字型檔存在／是 woff2／OFL 原文一併散布／manifest 記載完整、**語料指紋比對**（改了文案沒重跑 `npm run fonts` 會直接失敗，實測攔截成功兩次）、子集涵蓋語料每一個字、CJK 子集不含 Latin、直接解析原始 TTF 的 cmap 確認沒漏字、CSS 零 CDN ＋ `unicode-range` ＋ `font-display: swap` ＋ 字族堆疊順序 ＋ textarea 用系統字型）；`npm run build` 通過（CSS 38.7 KB / gzip 8.5 KB，字型 1.11 MB，dist 共 2.2 MB）；`npm run test:e2e` 由 151 擴到 **203 項檢查全過、零 console error**（新增 `document.fonts` 實載驗證 ＋ 零外部字型請求 ＋ 中文檔案大小證明是子集而非 16 MB 完整字型、標題分字揭示會跑完且 stagger 真的存在、面板進場動畫收斂無殘留位移、結果逐條揭示的 `animation-delay` 遞增且最終全部顯示、XP 計數停在正確數字、**1280×760 與 800×720 下三個面板都無水平溢位**），既有的移動／鏡頭／閘門／圖鑑／石碑／存檔斷言一條未改；另以 headless 截圖人工複審八個面板（含 `prefers-reduced-motion` 模式，確認動畫關閉後每個元素仍正確顯示）。**下一步建議**：（a）行動裝置仍未做——觸控搖桿與 720px 以下的面板版面；（b）練習室可再加「上一次的 prompt」與 diff，讓重寫拿 S 這件事有依據；（c）字型還能再省：CJK 子集目前是掃全檔的保守超集（含註解中文），若改成只掃字串字面值約可再省 10–15%，但會失去「絕不漏字」的保證，建議先做一個「執行期偵測缺字並回報」的開發輔助再談；（d）`index.js` 打包 958 KB（three.js 為主），可考慮 manualChunks 拆出 three。
- **2026-07-26 · Phase 7（序章「喚醒神諭」：先上引導課程，再放人自由探索）**：依產品指示「一定要先有引導課程，讓使用者練習原出處的核心概念」，先查了遊戲 onboarding 與學習科學的作法（Asher Vollmer 的 GDC「How to Make Great Game Tutorials」四要件：教會 / 安撫 / 引發興趣 / 尊重玩家；gamedeveloper.com 的 onboarding 準則：教學要長在關卡裡、前 15 分鐘決定去留、教學要可回看；一次只教一件事、立即回饋；學習科學的 worked example → faded scaffolding：先看做好的範例、再做部分完成的題目、最後獨立作答，鷹架逐題遞減），據此把首次進入的靜態教學卡換成一段 **9 拍的 diegetic 引導課程**。**流程**：標題卡 →「回聲」在起始祭壇喚醒旅人（新增 `src/data/prologue.json` 與 `src/ui/prologue.js`：字幕條一次只說一句、每拍附一個要真的做到的目標）→ 四道 **teach-by-doing 門檻**（走一步 / 轉鏡頭 / 按住 Shift 真的跑起來（走路不算）/ 走進祭壇光圈，全部由 `prologue.tick()` 量玩家的實際位移、鏡頭角速度累積與速度判定，不是按「我知道了」）→ **三堂核心概念實作課** → 畢業（把第一座石座「清晰之門」的光柱點亮指路、說明圖鑑與線索、交還操作權）。**三堂課的內容全部逐字取自 curriculum**（新增 `src/challenges/prologue.js` 負責解析，序章資料只記引用的 id，教學文字一律在執行期從 curriculum 取出 → 結構上不可能出現官方文件沒有的說法）：A「把話說清楚」（clarity-01 Anthropic 黃金法則 ＋ clarity-03 消除模糊語言，用 `beforeAfter`「控制長度」的弱→強對照）、B「正面表述」（positive-01，用「正面表述」對照）、C「指令與資料要分開」（clarity-04 ＋ structure-01，用 clarity-04 的官方範例本身），每堂輸入框**預填官方文件裡那句「弱」寫法讓玩家去修**（worked example），過關即收進圖鑑並顯示「出自 OpenAI／Anthropic 官方文件 ↗」。**練習台**（`src/prompt/practice.js`）**沒有第二套評分邏輯** —— 直接呼叫既有的離線 rubric 引擎，差別只在鷹架：**即時預檢**（打字 debounce 200ms 跑一次，達成的項目與證據會亮起來、右下角同步「x / 3 項達成」）、鷹架逐堂遞減（full：提示全開＋證據即時；partial：提示要失敗一次才出現；light：只剩亮燈），**「看看範例」一定要自己先試過一次才解鎖**。同一套預檢也回填到正式主控台，做成可開關並存進設定的「預檢」（預設開）。**世界**：出生點北邊新增**起始祭壇**（低矮供臺 ＋ 8 根立石 ＋ 會呼吸的暖金光圈與光柱，序章結束後沉回背景），並給石座加上 `setSpotlight()` 供畢業指路；祭壇納入留白清單，不影響既有地形、路網與任何既有測試。**存檔相容**：新增 `flags.prologueDone` 與 `prologueSteps`，`normalize()` 在**旗標不存在時**才由「有 XP／有收集／有通關／有讀碑／看過舊教學」推論老玩家為已完成（明寫的 false 會被尊重，所以設定頁的「重看引導課程」可以重播），序章只寫 `prologueSteps` 不寫 `bestGrades` —— 不佔「已通關 x / 26」、不算區域解鎖的通關數、重做不再給 XP。**隨時可退場**：字幕條的「跳過引導」有二次確認，跳過的人仍會拿到那張操作說明卡；設定頁新增「重看引導課程」。**驗證**：`npm run fonts` 重跑（語料 1302 漢字、指紋測試通過）；`npm run test:rubric` 由 2509 擴到 **2746 個斷言全過**（新增序章資料完整性：節拍與門檻類型合法、每句話 ≤ 90 字、四道門檻各只教一次、教學卡的 tip／example 與 curriculum 逐字相同、rubric 的檢查器與 techniqueId 真實存在、source 必屬於所教技巧且為 https、引用的弱→強對照逐字、**弱寫法一定不過關 ／ 官方寫法一定拿 S**、通過門檻夠嚴、序章只教基本功且之後仍有正式關卡再練、祭壇落點與石座距離；存檔：prologueDone 的五種推論情境 ＋ 明寫值優先 ＋ prologueSteps 去重 ＋ preflight 預設；進程：序章給 XP／圖鑑／徽章但不寫關卡評價、重做不刷分、重載仍記得、重置會清空）；`npm run build` 通過；`npm run test:e2e` 由 203 擴到 **315 項檢查全過、零 console error**（新玩家開機直接進序章、逐句推進、四道門檻都必須真的做到才過（走路不算跑）、第一堂課開啟時教學卡與出處齊全且強寫法藏著、**即時預檢隨打字從 1/3 → 2/3 → 3/3 亮起**、送出官方弱寫法會被擋下並給提示、試過才解鎖範例、看範例後送出拿 S 並收進圖鑑（出處連結逐一比對 curriculum 真實網址）、三堂課鷹架遞減、畢業時點亮第一座石座並交還操作權可自由行走、**老玩家的舊存檔（沒有 prologueDone）重整後不會被塞回教學且進度完整保留**、設定頁重看引導課程、跳過的二次確認含「繼續學」的反悔路徑、主控台預檢開關可即時生效並持久化），既有的移動／鏡頭／石座／石碑／圖鑑／閘門／字型／重置斷言一條未改（另修掉 e2e 自己的一個競態：`Page.reload` 後可能讀到舊頁面的把手，改成插旗等待換頁）。**下一步建議**：（a）序章目前是「一次性線性課程」，可考慮把三堂課做成可從圖鑑點進去的**隨時複習**入口（重看單堂而非整段）；（b）畢業後的第一關（清晰之門）與序章 C 課內容相近，可把它改成「把序章學到的三件事一次用上」的綜合題，讓交接更有落差感；（c）行動裝置仍未做（觸控搖桿、720px 以下的練習台版面）；（d）序章的「回聲」目前只有文字，若加一層很輕的合成語音提示音（每句一個音色相同的短音）會更有臨場感，成本很低。
- **2026-07-26 · Phase 8（關卡改造：每一關都變成一道真的題目）**：依實測回饋（「沒有設計題目讓使用者解謎」「看完 hint 還是不知道要輸入什麼」「像迷路的自動機根本不知道要輸入什麼才能過關」「積木都是英文範例」「給選項直接填進去降低遊玩成本」「展開關卡要回到頂端」「設定裡不用講網站技術細節」「石頭主角會穿過去」）把 26 關全部重新設計。**題目化**：每關新增 `mission`（一兩句話的明確指令，例如「把鐵板上這串『不要』全部改寫成『要做什麼』的正面指令，並告訴它為什麼」）與 `material`（NPC 真的遞給你的東西：守門人寫壞的告示、自動機胸口的禁止清單、黏成一團的信、兩卷收成帳、印刷工的要求…），主控台左欄改成「情境（氛圍）→ 你的任務（金線標注，視覺上最重）→ 素材卡片（引文式邊框）→ 線索 → rubric」，情境↔素材↔任務↔評分標準的因果一眼看得出來；rubric 檢查一條未動（68/68 涵蓋率、`source` 與 `techniqueId` 全部原封不動）。**降低起手成本**：每關新增**客製 placeholder**（填空骨架，半透明、打字即消、不會被送出）、**10 關預填 `starter` 弱寫法**讓玩家用序章那套「用修的」（測試強制驗證每個 starter 都不過關）、每關 2–4 片**快速填入**短句（游標處插入、用過會變樣式、組起來足以解題但不等於答案卷），以及**送出兩次沒過才解鎖的「看看範例」**（`sample` 從測試 fixture 搬進資料層，26 關全部改寫成**中文**示範解答且全部拿 S）。**技巧積木中文化**：新增 `src/data/builder-zh.json`（`authored: "game"`，檔頭明講是遊戲自撰的翻譯層、不是官方引文、出處一律回 curriculum），按下去插入的是中文片段，英文原句降級成標著「英文寫法」的次要參考；八片中文片段全部實測點得亮對應的檢查器。**捲軸**：`createOverlay` 新增 `resetScroll()`，展開新關卡／新練習一定回到頂端。**設定去技術味**：拿掉「後製 bloom / 色彩分級」「Web Audio 即時合成」「localStorage（promptarcade.v1.save）」等字眼，改成玩家看得懂的說法（高＝畫面效果全開、進度會自動存在這台裝置上）。**道具碰撞**：新增 `collectSolids()` —— 從場景圖掃出擺放時標記 `solidRadius` / `solid` 的道具，壓成 `{x, z, r}` 圓柱體登記表（286 個），**飄在半空的齒輪與思考環、膝蓋以下的矮件自動排除**；`clampPosition` 改用「地形 ＋ 閘門 ＋ 實體」三重判定並保留沿牆滑，另加 `escapeSolid()` 保險絲（站進道具裡時每幀往外推一小步，不瞬移也絕不會被關住）；**石座周圍 5 公尺、四條橋的主動線（±3.2 公尺）、出生點、起始祭壇一律強制淨空**（由資料層過濾，測試逐點掃描把關）。**驗證**：`npm run fonts` 重跑（語料 1371 漢字，指紋測試通過）；`npm run test:rubric` 由 2746 擴到 **6835 個斷言全過**（新增：26 關的 mission／material／placeholder／starter／quickFills／sample 完整性、starter 一定不過關、sample 一定 ≥ A（實際全 S）、示範解答與素材的二字詞重疊度（情境↔答案要有因果）、快速填入組起來解得開但不等於答案、積木中文片段的存在／非照抄／不附出處／真的點得亮檢查器、英文示範解答照樣過關（守住檢查器的雙語路徑）、**在 node 裡真的把整個 three.js 世界蓋起來**掃碰撞登記表並逐點驗證石座周圍四圈、四條橋整條主動線、出生點、祭壇、12 塊石碑周圍都沒有被擋，以及擋下／沿牆滑／脫困保險絲的行為）；`npm run build` 通過；`npm run test:e2e` 由 315 擴到 **362 項檢查全過、零 console error**（新增：重開關卡捲軸歸零、placeholder 顯示且打字後只留玩家打的字、快速填入插入中文且不給分、兩次沒過才解鎖範例→填入→送出過關、積木插入中文且英文只標為「英文寫法」不冒充官方、設定頁零技術字眼、**走進石頭會被擋下來且不會穿過去、被丟進石頭裡會被推出來、石座旁邊照樣互動得到**）。**下一步建議**：（a）26 關的 `material` 目前是靜態文字，可以做成「解完後世界會變」（例如清晰之門通關後守門人手上的告示真的換成你寫的版本），把答案寫回世界；（b）快速填入可以再進一步做成「拖曳排序」的組裝介面，讓 prompt 的**結構順序**也變成一種可練習的技巧；（c）行動裝置仍未做（觸控搖桿、720px 以下的主控台版面）；（d）碰撞目前是圓柱體近似，書架這類長條道具用一個圓會略胖，之後若要更貼可改成 OBB，但要先確認不會把走道縮窄。
- **2026-07-26 · Phase 9（對象轉向「中文圈一般人」：超容易學到東西、超容易過關）**：依實測回饋（「石碑叫我抬頭看天空但鏡頭抬不起來」「還有一堆英文」「規則型判定沒有講清楚方向，正面表述那關過不了」「希望有漂浮提示球 ＋ 黃色高亮教學框」「整體驗證放寬」「即時預檢要一直亮著」）把整個學習面重做。**① 抬頭看天空**：`player.js` 新增視線俯仰（滑鼠上下拖曳、或按住空白鍵），仰角夾在 −0.45…1.2 弧度，抬頭時鏡頭同步下沉（`PITCH_DROP`）讓角色退到畫面下緣、上半屏全是星空／極光／月亮；空白鍵的目標仰角刻意停在 0.62 弧度，**留一線地平線在畫面下緣**（整片黑天空反而看不出「這片夜色比你以為的還大」）；一開始移動就平滑收回跟隨鏡頭，序章的「轉鏡頭」門檻也改成抬頭同樣算過，HUD 與教學卡都補上「空白鍵抬頭看天空」。**② 全面中文化**：26 關的 hint／clue／starter／placeholder／sample／quickFills、序章三堂課、檢查器的 22 條預設提示與所有 evidence 回饋，全部改寫成白話中文（技術名詞如 JSON／temperature／`<thinking>` 保留）；面板標頭的英文 meta label（`Practice Chamber`／`Trial 01 / 06`／`Codex — The Collection`…）改成中文。**官方引文的處理原則**：`curriculum.json` 一字未改，但序章「弱→強對照」與教學卡改成**中文示範為主、官方英文降級成收起來的「原文 ↗」**（附出處連結與「這是遊戲自撰的示範，不是官方文字」的註明）—— 資料結構上把 `zh.*` 與官方欄位分開存放，**翻譯不可能被當成官方引文顯示**；輸入框預填的起手弱寫法也改成中文（玩家看得懂才修得動）。新增 `scripts/zh-scan.mjs`（連續 3 個以上英文單字＝一句英文句子，技術詞組另有白名單）當成可執行的規則，掃 challenges／prologue／coach 的所有玩家可見欄位。**③ 漂浮提示球 ＋ 黃色高亮教學框**：新增 `src/data/coach.json`（遊戲自撰，**22 條檢查全覆蓋**），每條寫「這是什麼（零背景知識的白話）／怎麼做／可以直接填的中文句子」；主控台右下角多一顆會呼吸的提示球，點開是黃色高亮的教學框，內容跟著即時預檢在**還沒做到的項目之間循環**，每片提示都有「幫我填」把中文句子插到游標處；**停手 20 秒或送出沒過一次**，球會溫和發光提醒——永遠不是 modal、不擋畫面、不搶焦點（提示框只蓋左欄，右欄的輸入框與送出鍵永遠看得到）。**④ 規則型判定講清楚方向**：22 條檢查的提示全部改寫成「照抄就能用」的句子（例如 positiveFraming：「訣竅：把『不要做 X』改寫成『請做 Y』。例如把『不要走左邊』寫成『請靠右走』」）；`lost-automaton-03` 端到端重驗，**只用畫面上的教練提示、1 次送出就過關**。**⑤ 整體放寬（新評價曲線）**：通過門檻全面由 66–75% 降到**總權重的 50%**（6→3、7→4、8→4、9→5，序章三堂課 4→2.5）；評價曲線 **S ≥ 0.95 / A ≥ 0.78 / B ≥ 0.60 / C = 達到門檻**（原本 1.0 / 0.85 / 0.70）；檢查器新增 **0.75 分這一階**（`MOST`）並大幅擴充中文口語偵測（角色「你是一位…」「假設你是」、格式「用 3 點條列／做成表格」、分隔符「【資料】…【/資料】／---／「」」、自我檢查「最後請檢查一次」、退路「沒寫就說資料中沒有提到」、拆解「拆成幾個步驟」、參數「思考深度設為高」…）；**反作弊未放寬**：空字串／亂打／關鍵字堆砌照樣 0 分，`一點`／`簡短`這類形容詞不再被誤判成量化限制（新增否定回顧斷言），問題擺在最前面仍然 0 分。26 關與序章三堂課全部實測「只靠教練提示、1–3 次填入就能過關」。**⑥ 即時預檢常駐亮燈**：預檢預設開（保留開關），新增**亮燈動畫 ＋ 每點亮一項一聲輕響**（`SFX.spark`）、**進度燈**「再完成 N 項就能過關（目前 x / 需要 y 分）」，達標時進度燈轉綠、送出鍵發光並改寫成「可以過關了！送出評分」。**驗證**：`npm run fonts` 重跑（語料 1392 漢字，指紋測試通過）；`npm run test:rubric` 由 6835 擴到 **8050 個斷言全過**（新增：coach.json 覆蓋每一條被用到的檢查且「照著填就真的點得亮」、coach 不自帶連結、新評價門檻逐格驗證、所有關卡門檻 ≤ 總權重 55% 且 ≥ 2、遊戲自撰欄位的英文句子掃描（challenges／prologue／coach／22 條檢查器提示／未過關時的 evidence）、序章的中文示範層與官方原文分開存放且官方逐字保留）；`npm run build` 通過；`npm run test:e2e` 由 362 擴到 **420 項檢查全過、零 console error**（新增：空白鍵與滑鼠拖曳都真的抬得起頭且視線朝上、抬頭時鏡頭下沉、走路會收回、上下限夾得住、天空 dome 在視距內且月亮／極光都在高空；提示球看得見、點開是黃色高亮框（實測 border-left 是暖金 3px）、「幫我填」插入中文並立刻點亮該項、提示會在未達成項目間循環、發呆／失敗會發光但不彈窗；預檢預設開、打字亮一盞燈有動畫、進度燈倒數、達標時送出鍵發光寫「可以過關了」；**只用教練提示 1 次送出就通過正面表述那一關**；序章的弱→強以中文為主、官方英文收在預設收起的「原文 ↗」且逐字未改）。**下一步建議**：（a）序章練習台還沒有漂浮提示球（目前靠上場提示 ＋ 一次失敗後解鎖範例），可以把同一顆球接上去，讓「第一次寫 prompt」也有同樣的扶手；（b）教練提示目前是每條檢查一份靜態文案，可依關卡情境替換例句（例如自動機那關直接用「靠右走」而不是通用例子），代入感會更強；（c）行動裝置仍未做（觸控搖桿、720px 以下的主控台與提示框版面）；（d）放寬之後 S 評價變得比較容易拿，之後若要維持長期挑戰感，可考慮加一層「不用任何快速填入／教練提示就拿到 S」的隱藏勳章。
- **2026-07-26 · Phase 10（把 26 關模擬試玩抓到的誤判、對不上的提示與行話一次修掉）**：承接「13 位模擬玩家只靠畫面資訊玩完 26 關」的試玩報告，修的是「過關太容易」以外的三件事——**假通過**、**提示與檢查器對不上**、**行話**。檢查器（`src/challenges/checks.js`）：`assignsTask` 大改——新增 `maskNonInstruction()`，評分前先把圍籬／三引號／成對標籤／【】／資料類章節／範例列與欄位列（輸入：、輸出：、工具名：、說明：、參數：…）／長引文遮掉，信件內文的「請大家先儲水」與範例列不再冒充任務動詞；新增中文複合詞守門（「輸出格式」「輸出上限」「完成前」「回覆裡」不再算動詞）；新增「請＋狀語＋動詞」句型（「請向新來的擺渡船員說明今晚的航次」不管在行首或句中都算），中文句號一律切句，「回答問題」這種空指令只給部分分數，證據改成乾淨的動詞片語（不再出現「寫一」或吞掉下一句的 token）。`hasConstraint` 改成候選清單＋排名：濾掉序數（第一次）、疊詞（一步一步／一件一件）、限定詞（每一個／哪一個）與形容詞後的「一點」，弱量詞（個／種／次／件／張…）要接規格名詞才算，證據挑最像規格的那一個（「3 組範例」而不是「一種」）。`specifiesFormat` 的「一行」要有格式脈絡（「埋在最後一行」不算）；`hasFewShot` 接受同一行寫完的「輸入：…輸出：…」，結尾的「輸出：」判定為接寫開頭、不再多算一組；`explainsWhy` 接受「問題是／原因在於／上一版…沒有人看到」；`hasStepByStep` 接受「仔細想過再回答」；`hasAudience` 接受「＿＿也看得懂／為＿＿而寫／給＿＿看得懂」；`decomposesTask` 對頓號列舉給部分分數；`putsQuestionLast` 與 `groundsInContext` 的回饋改白話（不再出現「第 91% 的位置」）。新增 rubric 的 `checkOptions`（`rubric.js` → `runCheck(id, text, options)`），讓關卡可以給貼近自己情境的示範句與同義詞——熔爐關的「高火／中火／低火」直接算 effort 等級，回音工坊與四要素之鏡的「因為＿＿」範例改成該關自己的理由。內容面：白話化 prompt chaining／小型 eval／code execution／## System／developer 訊息／貪婪解碼／overfit，reasoning_effort 與 verbosity 補上中文對照與可用等級；長卷檔案室統一成「資料在前、任務在後」；工具鍛造間與優先序階梯的兩處提示與骨架名稱對齊；查證之泉補上真實里程（3＋2＋4＝9 公里）讓「重算總和」有東西可算；一致的燈列把 lamp-4 改成「4 號燈」；刻度儀之室的示範改成 temperature 0（要每次一樣就設 0）；擬態之鏡與示範迴廊註明中文「輸入：／輸出：」一樣算；六關的快速填入補上真正的任務句，零思考路徑照樣走得通。新增 `scripts/playtest-verify.mjs`（`npm run test:playtest`，也併進 `npm run test:rubric`）：斷言 26 關的示範解答 ≥A、快速填入全按下去必過關、起手壞寫法必不過關，外加 36 條誤判回歸案例。驗證：`npm run fonts` 重跑、`npm run test:rubric` 8208 個斷言全過、`npm run test:e2e` 420 項零 console error、`npm run build` 通過。**下一步建議**：把 `assignsTask` 的遮蔽邏輯也套用到 `positiveFraming` 與 `keepsPromptLean`（目前貼進來的素材仍會被算成正面指令），並針對「數字只是複述素材內容」（例如抉擇之秤的『三條航線』）做更準的偵測。
- **2026-07-26 · Phase 11（石碑刻印：預設不再打字，改成一段一段「選」出 prompt）**：依產品指示「打字還是太難，把 prompt 變成選擇建構題」，26 關的預設互動全面換成**石碑刻印**。**資料**：新增 `src/data/flows.json`（`authored: "game"`，檔頭明講是遊戲自撰的示範層、不是官方引文，出處一律回 curriculum）—— 26 份流程、共 **111 段**、332 個選項；每一段是一句話的問題（「第一段：要先告訴守門人什麼？」）＋ 2–3 個選項、剛好一個正確；正確片段以既有的中文示範解答為底稿重新切段，**全部選對串起來丟進真的離線 rubric 引擎，26 關全部拿 S、而且每一條檢查都滿分**（測試強制驗證）。**221 個錯的選項全是一般人真的會犯的寫法**，每個都附一句白話的教學回饋，講出「為什麼這樣比較弱、那條檢查到底在要什麼」：含糊的「盡量簡短一點」、只說「不要走左邊」的禁止句、把資料直接貼上不加標籤、少了讀者的「寫給大家看」、只給一組範例、只有輸出沒有輸入的假範例、跟第一組一模一樣的第二組、問題擺在長資料前面、叫它「沒寫到就推測一下」、把旋鈕講成願望（「請每次都給我一樣的結果」而不是 temperature 設 0）、把 effort/verbosity 調反、對推理型模型硬塞角色與逐步鷹架…**互動**：新增 `src/prompt/stele.js` —— 右欄變成一塊立起來的石碑（CSS 漸層石面 ＋ 既有的 SVG 顆粒 ＋ 兩條金色髮絲礦脈 ＋ 上緣一道拱）。選對 → 石碑震一下（`is-stamp`）、噴 10 顆石屑、一記低頻悶響（新增合成音效 `stamp`），文字用 `clip-path` 由左往右「鑿」出來並留下 inset 陰影的刻痕；選錯 → 石碑抖 ＋ 裂光一閃（`is-reject`）＋ 悶響（`reject`），那個選項留在原地標成「石碑不收」並就地長出一條金線的教學回饋 —— **不扣分、不失敗、不跳失敗面板、不會前進，重選就好（你不可能卡住，只會學到東西）**；刻滿 → 石碑亮起（`is-full` ＋ `seal` 音效）並浮出一個會呼吸的**手掌印**（純 CSS 五指 ＋ 掌心），按住 0.6 秒（conic-gradient 蓄力環）→ 光爆 ＋ 走原本那支 `submit()` → 原本的結果面板、S 印章、XP、圖鑑、四廠徽章、3D 角色歡呼全部照舊。左欄的 rubric 清單變成刻碑的進度表（每刻一段就跑一次**同一支**預檢、亮一盞燈、進度燈改成「把手掌按上石碑就過關了」），石碑上方顯示「第 N / M 段」，鍵盤 <kbd>1</kbd><kbd>2</kbd><kbd>3</kbd> 可選、焦點自動跟著段落走、`prefers-reduced-motion` 下把抖動與石屑換成單純顯示。**模式**：新增存檔設定 `settings.promptMode`（`normalize()` 未知值與舊存檔一律回到 `guided`），主控台右上與設定頁各有一個平白說法的切換 —— 切到**自由書寫模式**就完整回到 Phase 8/9 的書寫檯（起手弱寫法、客製 placeholder、快速填入、技巧積木、漂浮提示球、即時預檢、看看範例一項都沒少），兩種模式送出的是**同一段文字、走同一支離線引擎**，所以評價與收集完全一致（護欄 3）；序章那三堂引導課不動（它本來就是自己的引導設計）。**修掉三個真的會影響玩家的問題**：（a）手掌印原本綁在 `requestAnimationFrame` 上判定按住多久 —— 軟體渲染／慢裝置一個影格可能要好幾百毫秒，會按到懷疑人生，改成計時器判定、影格只負責畫那圈環；（b）刻好下一段時的焦點移動也包在 rAF 裡，同樣會出現一段「鍵盤沒有焦點」的空窗，改成同步移動；（c）預檢亮燈時 rubric 清單缺第三欄，「幾分」與「出處」會擠到下一行並把標題推到右邊（Phase 9 就存在，但刻印模式把它推到了預設畫面上），一併修掉。**字型語料**：`scripts/subset-fonts.mjs` 的 `CORPUS_FILES` 從「只列 curriculum ＋ challenges」改成掃 `src/data/` 底下**全部** JSON（prologue / coach / builder-zh / flows 原本是靠與程式碼註解重疊才沒缺字，那是運氣不是保證），CJK 子集 1392 → 1457 字、總量 1215 KB（仍在 1.5 MB 預算內）。**驗證**：`npm run fonts` 重跑（語料 33 檔 / 1457 漢字，指紋測試通過）；`npm run test:rubric` 由 8208 擴到 **11666 個斷言全過**（新增：26 關流程完整性（3–6 段、每段 2–3 選項、剛好一個正確、錯的都有 ≥12 字的中文回饋）、**全部選對＝S 且每條檢查滿分**、錯的選項不是正確答案的複製、正確答案的位置有打散（不能一路按 1 過關）、選項與回饋的英文句子掃描（flows.json 併入 zh-scan）與「不自帶連結」、`promptMode` 的預設／未知值／明寫值／持久化）；`npm run test:playtest` 由 134 擴到 **212 個斷言**（新增：每關都有流程、全部選對每條檢查滿分、只刻第一段還不會滿分）；`npm run build` 通過；`npm run test:e2e` 由 420 擴到 **532 項檢查全過、零 console error**（新增 92 項：開關卡預設就是石碑刻印且輸入框與提示球都收起來、焦點落在第一個選項 → 選錯會抖、就地出現金線回饋、不扣分不失敗不前進、還能再選 → 一段一段刻上去刻痕數 1→4 跟著長、石屑真的噴出來、左邊的燈跟著亮 → 刻完的文字＝所有正確選項串起來、問句區收起、手掌印出現並拿到焦點 → 按一下就放不算（提示抖動）、按住 900ms 才發動 → 結果面板 S ＋ XP ＋ 石座轉已通關 ＋ 官方出處 → 800px 下石碑與長資料選項都無水平溢位 → 切到自由書寫後預檢／快速填入／送出全部照舊 → 設定頁的「答題方式」下拉寫進 localStorage 並跨重整生效），另以 headless 截圖人工複審六個狀態（問句 / 石碑不收 / 刻到一半 / 手掌印 / 結果 / 自由書寫）。**下一步建議**：（a）目前每段只有一個正確選項，可以做「兩個都對但評價不同」（例如格式選表格或條列都過關，但這一關的素材用表格更合適），把「取捨」也變成可練習的東西；（b）刻好的碑文可以收進圖鑑，讓玩家回頭看自己刻過的 prompt；（c）刻完之後多一顆「把這段搬到自由書寫模式繼續改」的橋，讓輔助輪真的變成畢業的路；（d）行動裝置仍未做（觸控搖桿、720px 以下的石碑與選項版面）。
- **2026-07-26 · Phase 12（四幕分鏡：把關卡面板變成導演式的引導視野）**：Playtest 回報「一打開關卡，題目、官方說法、答案區同時攤在眼前，不知道先讀哪一個」，所以把 `src/prompt/console.js` 重構成**四幕**（①委託 ②指引 ③刻印 ④手印），一次只有一幕擁有畫面：**第一幕**只有 NPC／情境／任務／素材 ＋ 一個出口「聆聽指引 →」（Enter 也可以），刻意**零官方連結**（e2e 有守）；**第二幕**把教學內容換皮成世界觀的「**神諭刻文**」（沿用序章既有的「喚醒神諭」設定），每一關的每一條 rubric 檢查各一段白話刻文（來自 coach.json），下面掛真正的出處並標成「**神諭原典：Anthropic · Prompting best practices ↗**」——換皮但**不說謊**：標籤後面一定接得出真實文件名與可點連結，並在幕首明講「刻文是抄寫人用白話寫下的，出處是各家官方文件」（護欄 2）；**第三幕**石碑（或自由書寫的書寫檯）成為舞台，「刻痕對照」即時亮燈就在**旁邊並排**（e2e 量了 `rail.left ≥ main.right`），第二幕的刻文縮成側頁籤「神諭刻文 · 翻回指引」（一行一條 ＋ 出處，不是一牆文字）；**第四幕**刻滿自動切鏡頭到手掌印儀式（`.console.is-palm`：石碑發光、對照退到 50% 不透明），全部改用世界觀說法「石碑已刻滿。把手掌按上去，讓神諭聽見。」。轉場走「鏡頭切換」：舊幕直接切掉、新幕從 `display:none` 回來讓 `act-cut`（招牌 `--e-out`）與幕內 `.reveal .d1/.d2/.d3`（沿用 llm-agent-playground 的 reveal 節拍）自己重播；幕指示器 ①②③④ 走過的幕隨時回得去、沒走過的按不下去，且**看過指引的關卡重玩時可以一鍵跳到刻印**（新存檔欄位 `guidanceSeen[]`，純加法、舊存檔自動補空陣列）。**系統術語清乾淨**：「送出評分」全數改為「**呈給神諭**」（含序章練習台、進度燈、coach.json），序章的「官方怎麼說」改成「神諭刻文」與第二幕同一套語言，e2e 會掃整份 DOM 文字＋title/placeholder/aria-label 確認「送出評分」再也找不到。**資料重整（只搬不改寫）**：26 關的 `mission` 原本把「委託」與「怎麼答」混在一句（例如「…重寫成一段 prompt——指令放最前面、用分隔符包起來、指定格式」），現在 `mission` 只留一句清楚的委託，怎麼答的工法全部搬到新欄位 `craft` 並在第二幕呈現；順手修掉 `.stage` 類名與 3D 畫布容器撞名的版面 bug（改名 `.carvestage`）。**驗證**：`npm run fonts` 重跑（語料 33 檔 / CJK 1468 字 / 1219.5 KB，指紋測試綠）；`npm run test:rubric` 11666 → **11796 個斷言全過**（新增 26 關 × 5 條 craft 門檻：存在、15–120 字、中文、與 mission 不同、無英文句子）；`npm run test:playtest` **212 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 532 → **610 項檢查全過、零 console error**（新增 78 項：第一幕只有題目且無出處連結、不能跳過未走過的幕、Enter 進第二幕、刻文段數＝rubric 條數且每段都有 https 官方連結與「神諭原典：<文件名>」標籤、指示器只有一幕 is-now、回顧委託後仍不能跳到手印、第三幕石碑＋對照並排＋側頁籤四條含出處、刻滿自動進第四幕並把光打在手印上、重玩可跳過第二幕／沒玩過的關卡不給跳、全介面找不到「送出評分」），另以 headless 截圖人工複審八個狀態（四幕 ＋ 結果 ＋ 側頁籤 ＋ 自由書寫 ＋ 820px 窄畫面，水平溢位皆為 0）。**下一步建議**：（a）幕與幕之間目前是「切」，可以再加一層極短的橫向推移（像鏡頭橫搖），並讓第二幕的刻文條目支援「這一條我懂了」的自我標記，讓重玩時只讀沒懂的那幾條；（b）第四幕拿到結果後可以多一顆「回到第三幕再刻一次」的橋，現在要靠指示器往回按；（c）行動裝置仍未做（觸控搖桿、720px 以下的四幕版面與手印儀式）；（d）第二幕的刻文與圖鑑內容有重疊，可考慮讓圖鑑直接引用同一份 `guidanceRows()`，避免兩處各寫一套。

- **Phase 13 · 序章重導與 ⓘ 資訊提示**：（1）把「神諭原典 —— 也就是各家官方文件」這種解釋世界觀的旁白從版面上收進一顆 ⓘ（`infoTip()` / `bindInfoTips()` in `src/ui/dom.js`，hover ＋ focus ＋ 點擊三種都開得起來、`aria-describedby` 指到氣泡、預設 `visibility:hidden` 但內容仍在 DOM），套用在主控台第二幕導言、序章刻文導言與圖鑑的「未收集」說明；**可點的官方出處連結一律不收進 ⓘ，永遠留在畫面上**（護欄 2）。（2）序章「喚醒神諭」整段用 Phase 12 的導演語言重做：一拍最多兩句、逐句 `.reveal .d1/.d2` 浮出、換拍重播鏡頭切換、門檻拍一進來就開始判定（移動／鏡頭／奔跑／走到祭壇仍然要真的做到）、練習拍拆成「回聲宣布 → 石碑 → 課後過場（bridge）」三段，畢業拍收斂成兩句台詞＋一條圖鑑小提示；回聲的台詞從 13 句／最長 51 字 收緊到 11 句／最長 30 字（連門檻回饋與過場共 19 句，每句都 ≤31 字）。（3）三堂課的互動從「打字修弱 prompt 的書寫檯」換成**與正式關卡同一套四幕分鏡＋同一支石碑**（`src/prompt/practice.js` 重寫：委託 → 神諭刻文（一課只講一個概念＋神諭原典連結）→ 刻印（3 段選擇建構、選錯不失敗、右側刻痕對照即時亮燈）→ 手印），流程資料寫進 `src/data/prologue.json` 的 `steps[].flow`，官方英文原文改收在「官方怎麼說 ↗」摺頁裡；`prologueSteps` / `prologueDone` 語意與存檔相容性未動，重看與跳過照舊。**驗證**：`npm run fonts` 重跑（語料 33 檔 / CJK 1472 字 / 1222.4 KB，指紋測試綠）；`npm run test:rubric` 11796 → **12115 個斷言全過**（新增：一拍 ≤2 句且每句 ≤40 字、practice 拍必須有 bridge、刻文一課一條且中文／≤30 字／掛得回真實原典、每課 flow 2–3 段且錯的選項都有 ≥12 字白話回饋、**全部選對用真的離線引擎跑一定拿 S**、委託那句弱寫法一定不過）；`npm run test:playtest` **212 個斷言全過**；`npm run build` 通過；`npm run test:e2e` 610 → **659 項檢查全過、零 console error**（新增：ⓘ 預設看不見／hover 與 focus 才出現／移開與失焦收回／`aria-expanded` 同步／導言不再內嵌解釋、序章一拍兩句且分批揭示、練習拍三段流程、四幕在序章也成立、選錯不被刻上去且就地給教學、刻一段亮一盞燈、全部選對拿 S ＋ XP ＋ 圖鑑、課後過場台詞、畢業拍兩句＋圖鑑提示），另以 headless 截圖人工複審 12 個狀態（醒來／門檻達成／宣布／四幕／ⓘ 展開／選錯／手印／結果／過場／820px 窄畫面，水平溢位 0）。**下一步建議**：（a）序章目前仍只在桌機驗過，行動裝置（觸控搖桿、720px 以下的四幕與手印）還沒做；（b）練習台與主控台現在有兩份幾乎一樣的「幕切換 ＋ 刻痕對照」程式碼，值得抽成共用模組再讓兩邊各自帶自己的結算；（c）ⓘ 的氣泡固定往上開，靠近面板頂端時會蓋住標題，可以加一個翻轉方向的判斷；（d）三堂課的 flow 目前每課固定 3 段，之後若要拉長序章可考慮第三課加到 4 段並引入「資料要原封不動貼上」的錯誤選項。
- **2026-07-27 · Phase 14（官方範例全面中文化 ＋ 說明文字放大 ＋ 指南針）**：依產品回饋修三件事。**① 官方範例全面中文化**：圖鑑等地方仍然把官方英文範例當主文（例如 clarity-01 的黃金法則整段是英文），對象是中文一般使用者根本讀不下去。新增 `src/data/curriculum-zh.json`（`authored: "game"`，檔頭明講是遊戲自撰的譯寫層、不是引文、出處一律回 curriculum），**`curriculum.json` 一位元組未改**；譯寫涵蓋 **32 條技巧**（27 條 `example` ＋ 4 條 `tip` ＋ 1 條 `note`），判準＝專案既有的 `scripts/zh-scan.mjs`「連續 3 個以上英文單字」規則（技術詞組白名單同一份），也就是「官方欄位裡只要有整句英文，就一定有中文譯寫」。`content.js` 新增 `displayTechnique()` 統一出「中文在前 ＋ 被取代掉的官方原文放進 `origin`」，圖鑑因此改成**中文主文 ＋ 收起來的『原文 ↗（官方英文）』摺頁（內含逐字原文、一句「這是遊戲自撰的譯寫」的註明與可點的官方出處）**，而**完整的官方出處清單永遠留在畫面上、不收進摺頁**（護欄 2）；技巧積木的英文寫法從按鈕上的一行降級成收起來的「英文寫法 ↗」摺頁；結果面板的出處從一長串網址改成官方文件名（新增 `content.sourceName()`）；第二幕神諭刻文的 `tech.tip` 退路與序章教學卡的 `example`／`tip` 也都接上譯寫層（序章原本就有自己的中文示範層，兩者互不覆蓋）。刻意保留的英文只剩「要玩家照抄打進去的字串」（例如「Think very hard」），一律包在「」裡並標明官方原句。**② 說明文字放大約 1.75 倍**：`:root` 的級數整組上調 —— `--t-lead` 1.13→**1.98rem**（情境文字 18.08px → **31.68px，×1.75**）、`--t-body` 0.9→1.58、`--t-small` 0.83→1.46、`--t-micro` 0.755→1.33、`--t-meta` 0.685→1.06（全大寫 ＋ 0.22em 字距的小標籤只放 ×1.55，2 倍會換行）、`--t-h3` 1.0→1.76、`--t-h2` 1.3→2.24、`--t-title` 1.95→2.62、`--t-display` 5.4→5.9rem（標題卡本來就滿版，只微調），另外把 21 處硬寫的 rem/px（線索本文、圖鑑說明、神諭刻文、素材、輸入框、碑文、評價印章、XP 數字、快捷鍵…）依同一比例調上去。**版面善後**：面板 880→**1060px**（wide 1200→1340px）、面板頭與內文的內距各收一階、`--lh-prose` 1.95→1.82（CJK 仍在 1.7 以上）、設定頁的欄位標籤改用 em 寬度且按鈕不換行、checkbox 16→22px、ⓘ 按鈕 16→22px、評價圓章 86→118px；**HUD／標題卡／toast 有自己的級數範圍，只放大約 1.4 倍**（同樣倍率會蓋掉半個世界）。**③ 指南針**：新增 `src/ui/compass.js` ＋ `world.objectiveTarget()` —— HUD 左下角一個純 DOM/CSS 的圓形羅盤，錶盤跟著 `cameraYaw` 轉（北 = −Z、東 = +X，四個方位字會自己轉回正的）、**金針指向「下一個目標」**（和 HUD 那一行同一套邏輯：本區未通關的第一關 → 下一個已解鎖區域 → 都破了就指還沒開的閘門）並在下面寫「目標名 ／ 約 N 步」、五座地標各一根該區顏色的淡針；每幀更新、面板打開時整個收起來、`prefers-reduced-motion` 下沒有任何抖動（角度直接套用，不做額外補間）。**驗證**：`npm run fonts` 重跑（語料 35 檔 / CJK 1505 字 / 1244.6 KB，指紋測試綠）；`npm run test:rubric` 12115 → **12825 個斷言全過**（新增：curriculum-zh 的 `authored`/註明/不自帶連結/只覆寫 example·tip·note/不照抄原文/真的是中文、**官方欄位有英文句子就一定有譯寫**、`displayTechnique()` 顯示中文且 `origin` 逐字保留官方原文、**68 條技巧預設顯示的三段文字都掃不出整句英文**、沒有譯寫層時安靜降級、26 關的出處都查得到文件名、序章顯示到的弱→強對照都有中文；級數：7 個 `--t-*` 的放大倍率、層級沒有被壓平、行距 ≥1.7、HUD 有自己的範圍）；`npm run test:playtest` **212 個斷言全過**；`npm run build` 通過；`npm run test:e2e` 659 → **702 項檢查全過、零 console error**（新增：圖鑑的範例是中文、「原文 ↗」預設收起且 `checkVisibility()` 為 false、展開後與 curriculum 逐字相同、出處連結永遠可見、**整本圖鑑 68 條預設看不到任何一句官方英文**、積木英文摺頁預設收起、情境文字 ≥ 舊值 1.7 倍、`--t-*` 放大且 CJK 行距 ≥1.7、指南針存在／四方位齊全／五根地標針／轉鏡頭時錶盤跟著轉／**金針角度＝用世界座標算出來的方位角（誤差 < 3°）**／走近目標步數變少／面板打開時收起來），另以 headless 截圖人工複審 14 個狀態（標題卡／世界＋指南針／四幕／圖鑑（1280 與 800）／設定／石碑／自由書寫／結果／成就／序章／練習台，1280×760 與 800×720 水平溢位皆為 0）。**下一步建議**：（a）`--t-display` 只放大 ×1.09、HUD 只放大 ×1.4，如果玩家仍覺得世界上的字太小，下一步該做的是「介面縮放」設定（一個 `--ui-scale` 乘在整組級數上）而不是再調死值；（b）指南針目前只指一個目標，之後可以讓已讀石碑／未讀石碑也各給一種淡針，變成真正的探索輔助；（c）`curriculum-zh.json` 只譯了「有整句英文」的欄位，`structure-02`／`reasoning-03` 那種純程式碼範例仍是原樣，若要做到「連 placeholder 都中文」需要再一輪；（d）行動裝置仍未做（觸控搖桿、720px 以下的四幕與指南針版面）。

- **2026-07-28 · Phase 15（刻印牌元件系統：每一個可以按的東西都變成同一塊被切過角的石牌）**：Playtest 回報「委託／指引那幾幕的按鈕與幕指示器都很醜、像網頁預設，我們是奇幻遊戲，按鈕回饋、邊角、每一個小元件的形狀都要有設計」。**研究先行**：先查 NN/g 的按鈕五態與時間預算（hover 150–200ms、focus / press 100–150ms、「focus 要用描邊而不是只換顏色」）、WCAG 2.2 的命中範圍（2.5.8 AA 24×24 是地板、2.5.5 AAA 44×44、斜切角會真的吃掉可點面積）、遊戲 UI 的原則（裝飾放在外框、狀態要靠形狀／圖樣改變而不是只靠顏色），以及三個關鍵的 CSS 事實：**clip-path 會把 border / box-shadow / outline / 子元素一起剪掉**（所以邊要用兩層假邊做）、**inset shadow 在剪過的形狀上失效**（立體感改用背景漸層假造，壓下去就把漸層上下顛倒）、**drop-shadow 會沿著剪出來的輪廓走而 box-shadow 不會**（所以符文光暈用 drop-shadow）；另外 45° 斜切往內縮 b 時切角只縮 b×(√2−1)≈0.414b，直接沿用外層 `--cut` 會讓斜邊的假邊粗 1.41 倍 —— 這一條寫進 `--cut-in` 了。**系統**：`styles.css` 開頭新增 `--cut / --seal-shape / --seal-shape-in / --seal-lit / --seal-shade / --seal-face-a·b / --inlay / --inlay-pad` 一組 token 與一節有文件的「刻印牌元件系統」，作法是 `::before`（inset 0，剪成八邊形，背景是上亮下暗的漸層 ＝ 那 1px 的邊）＋ `::after`（inset 1px，石面漸層 ＋ 上下兩條會注入的金鑲線），兩層 `z-index:-1`、本體 `isolation:isolate`，文字永遠在最上層。**三個階級**：hero（`.btn--primary` / `.title__start`：大斜切 ＋ 金石面 ＋ 常駐的鑲金溝槽 ＋ 沿著金邊跑的微光）、base（`.btn` / `.opt` / `.block`：暗石面，hover 才注金）、quiet（`.btn--ghost` / `.fill` / `.modeswitch` / `.panel__close` / `.acts__item`：小斜切、只剩髮絲邊）。**六種狀態全系統一致**：rest ／ hover（金線從溝槽兩端注入 165ms ＋ 邊變亮 ＋ 抬 1px，用 `translate` 而不是 `transform`，才不會被 `.opt` 的入場動畫蓋掉）／ active（石面受光方向顛倒 ＝ 真的被壓進去）／ disabled（灰階 ＋ 邊光降到六成，是「沒點燈的石頭」不是隱形）／ focus-visible（邊變金、加寬成 2.5px 的符文環 ＋ 沿輪廓的 drop-shadow 光暈，**刻意不補間**因為 focus 要立刻看得到，特異度也拉到贏過 hover）／ reduced-motion（保留受光方向與金線這些「在傳達資訊」的變化，只拿掉位移與掃光）。**逐元件**：**四幕指示器**重做成一件器物 —— 四塊封印石被三段軌道串起來，走過一幕它前面那段軌道就注滿金（純 CSS 相鄰選擇器 `.is-done + .acts__rule`，不動 JS），現在這一幕翻成星光白石面 ＋ 深色刻字 ＋ 浮起 ＋ 光暈，走過的蓋過章（受光顛倒 ＋ 金鑲線常駐），沒走到的是暗石，`disabled` 與 `aria-current="step"` 語意一條沒動；**選項卡**變成石籤（44px 以上、鍵盤數字是一顆小符文石），「石碑不收」時邊轉赤色、面上多兩道髮絲裂痕、金線收掉、不再被滑鼠抬起；**表單控制項**：音量是刻進石頭的凹槽 ＋ 一顆六角金滴滑鈕（踩到一個坑：軌道**不能**上 clip-path，滑鈕是畫在軌道裡的會被一起剪掉），勾選框是一塊八邊形石牌、蓋上封印才刻出勾痕，下拉是嵌在凹槽裡的石籤 ＋ 刻出來的金三角；**其他**：書寫檯 `.desk`、提示球 `.orb`、提示框的關閉鍵、ⓘ 符文石、`kbd` 鍵帽、廠家 `.chip`、區域精通封印、HUD 的互動提示卡、ⓘ 氣泡、`details` 的展開記號全部換成同一族的剪影。全部零圖檔（漸層 ＋ clip-path ＋ 既有的內嵌 SVG 顆粒），命中範圍 hero ≥46px / base ≥42px / quiet ≥40px / 勾選框 26px，Phase 14 放大的字級一個字沒縮 —— 是元件長大。**驗證**：`npm run fonts` 重跑（語料 35 檔 / CJK 1522 字 / 1255.1 KB，指紋測試綠）；`npm run test:rubric` **12825 個斷言全過**；`npm run test:playtest` **212 個斷言全過**；`npm run build` 通過；`npm run test:e2e` 702 → **771 項檢查全過、零 console error**（新增 69 項：三個階級都「沒有 CSS border ／ 兩層都被剪成多邊形 ／ 邊是漸層 ／ 零外部圖檔」、hero ≥44px 與面板內零過小命中範圍、四塊封印石 ＋ 三段軌道 ＋ 走一幕就往前注金 ＋ 只有一塊是亮的 ＋ 亮著那塊是淺面深字 ＋ disabled 語意還在、**真的用滑鼠 hover 到石牌上驗證金線注入**、**按 Tab 進鍵盤模式後 focus-visible 真的命中且邊變金、環寬 2.5px、光暈是 drop-shadow**、disabled 是灰階石頭不是透明、選項卡與符文石的尺寸、被拒絕石籤的裂痕與赤邊、滑鈕／勾選框／下拉的造型與尺寸、reduced-motion 下掃光與位移關掉但石面與邊還在、整份樣式表零外部資產），另外把一條**與本次無關但長期不穩**的斷言修掉：「過關慶祝時雙手真的舉起來」原本固定睡 550ms 再取樣，但 headless 用 swiftshader 只有 3 fps（實測把整個 `.ui` 移掉、完全沒有 CSS 參與也一樣），改成在慶祝期間持續取樣、記下舉得最高的那一刻，語意不變但不再綁在機器跑多快上。以 headless 截圖人工複審 20 個狀態（標題卡 ／ hover ／ focus ／ 序章 ／ HUD ／ 互動提示卡 ／ 四幕 ／ 選項 hover・focus・不收 ／ 手印 ／ 結果 ／ 自由書寫 ／ 提示框 ／ 圖鑑 ／ 技巧展開 ／ 設定 ／ 危險區 ／ 成就 ／ reduced-motion，1280×760 與 800×720 水平溢位皆為 0）。**下一步建議**：（a）行動裝置仍未做 —— 觸控搖桿、720px 以下的四幕與石碑版面，且觸控裝置沒有 hover，金線注入那一階回饋要改綁 `:active`；（b）石碑本體（`.stele`）與手掌印是 Phase 11 自己的一套造型，還沒併進刻印牌系統，可以讓它們共用同一組 token；（c）hero 的掃光目前是固定 5.4s 的無限動畫，之後若同畫面出現多顆主要按鈕會一起閃，可以改成用 `--i` 錯開；（d）`.opt` 的斜切在很寬的卡片上比例偏小，若要更有「石籤」的味道可以讓 `--cut` 隨卡片高度縮放。
- **2026-07-28 · Phase 16（操作分家：WASD 只管走路、方向鍵只管視角）**：依產品指示把鍵盤操作拆成兩件互不干擾的事 —— **W A S D 專責移動**（方向鍵不再驅動角色），**方向鍵專責視角**：← → 接手原本 Q / R 的鏡頭左右轉（同一組角速度 1.8 rad/s，手感一字未變），↑ ↓ 接手原本只有滑鼠上下拖曳與空白鍵才做得到的抬頭 / 低頭（新增 `PITCH_RATE` 1.2 rad/s，走的是同一個 `cameraPitch`、同一組上下限 −0.45…1.2、同一套 `PITCH_DROP` 鏡頭下沉，所以抬頭時角色照樣退到畫面下緣）；滑鼠拖曳、空白鍵直接抬到看得見星空的角度、Shift 奔跑、滾輪遠近全部不動。**Q / R 保留成不寫在任何說明裡的舊版別名**（老玩家的手指不會突然失靈），但 HUD、教學卡、序章、README 一律只講方向鍵這一套。順手修掉一個新組合會撞到的行為：原本「一開始移動就把仰角平滑收回」的規則只避開空白鍵，改成只要還按著 ↑ ↓ 或空白鍵就以玩家為準 —— **邊走邊抬頭看天空是合法的**。**文案**：HUD 操作列（改成「WASD 移動 · Shift 奔跑 · ← → 轉鏡頭 · ↑ ↓ 抬頭低頭（或拖曳滑鼠）· 空白鍵看天空 · E 互動」）、首次進入的操作說明卡（四列鍵位重排）、序章兩道門檻的行動目標（「按 W A S D 走動」「按 ← → 轉動鏡頭；按 ↑ 或空白鍵抬頭看星空」）、README 操作表（拆成轉鏡頭 / 抬頭兩列）。序章的鏡頭門檻本來就同時吃 yaw 與 pitch，所以方向鍵的兩種轉法都算過關。**驗證**：`npm run fonts` 重跑（語料 35 檔 / CJK 1522 字 / 1255.1 KB，指紋測試綠；← ↑ → ↓ 早就在 Latin baseline 裡，子集大小未變）；`npm run test:rubric` **12825 個斷言全過**、`npm run test:playtest` **212 個斷言全過**（兩者皆未動）；`npm run build` 通過；`npm run test:e2e` 771 → **791 項檢查全過、零 console error**（新增 20 項：序章移動目標只講 W A S D、鏡頭目標寫著方向鍵、按 ← 過關且 yaw 真的轉了而角色留在原地、← → 兩邊反向轉、↑ 抬頭 ／ ↓ 壓回去、方向鍵不會平移角色、一直按著也只停在上下限、按著 ↑ 邊走邊抬頭不會被收回、W A S D 四個方向照樣走得動、HUD 文案有 ← → 且不再出現 Q / R；原本用 KeyQ 驅動的兩處（序章鏡頭門檻、指南針錶盤跟著轉）改用 ArrowLeft；新的量測拆成數段短的 evaluate，避開 30 秒的 CDP 上限）。**下一步建議**：（a）行動裝置仍未做 —— 觸控搖桿、720px 以下的四幕與手印，且觸控沒有方向鍵，視角要改綁單指拖曳；（b）方向鍵目前是固定角速度，之後可以加一點加速 ／ 減速（和移動同一套 damp），長按轉大角度會更順；（c）Q / R 是靜默別名，若之後要徹底移除，記得同步改掉 player.js 的註解與這一行紀錄；（d）還缺一顆「重置視角」的回正鍵（setCameraPitch(0) 已經是公開 API，缺的只是綁定）。

- **2026-07-28 · Phase 17（介面不再提金鑰 ＋ 浮動效能監視器）**：**① 拿掉所有跟 API 金鑰有關的旁白** —— 這款遊戲從來沒有出過「自己填金鑰接真模型」那個選配模式，對一般玩家來說只是噪音。玩家看得到的三處全部改寫：標題卡底行「全部離線評分，不需要 API key」→「每一條都附得出官方出處」、操作說明卡「所有評分都在你的瀏覽器裡離線完成 —— 不需要任何 API key」→「你寫的每一段話都會當場評分，並告訴你缺了什麼、下一步怎麼改」、README 特色欄的「**不需要任何 API key 或後端。**」整句刪掉；`curriculum.json`（官方逐字引文，含「用 API 的 tools 欄位定義工具」「Gemini API」這類**課程內容**）與 `challenges.json` 教工具定義那一關的 clue 一個字都沒動（那是要教的東西，不是元宣傳），`rubric.js` 的開發者註解也依約保留。新增可執行的護欄：`test:rubric` 掃 `src/data/` 底下**除 curriculum.json 以外**每一份資料的每個字串、以及 `src/**/*.{js,css,html}`＋`index.html`＋`README.md`（先剝掉註解，只看真的會出現在畫面上的字），比對 7 條禁用樣式（`api key` / `apikey` / `API 金鑰` / `金鑰` / `bring your own key` / `自備…key` / `真 LLM 模式`），並反向確認官方技巧裡的 API 字樣仍在；`test:e2e` 再掃一次**實際渲染出來**的 DOM（標題卡 ＋ 操作說明 ＋ 設定 ＋ 圖鑑的全文與 title/placeholder/aria-label）。**② 浮動效能監視器**（`src/ui/perfmon.js` ＋ `.perfmon` 樣式）：右上角一塊切角石牌（`clip-path` 切角 ＋ 石面漸層 ＋ 既有的 SVG 顆粒 ＋ 金色 hairline，和刻印牌同一套語言，零外部函式庫、沒有 stats.js），顯示 **FPS（現值 ＋ 最近 60 幀的柱狀圖，60 FPS 有參考線、掉速時柱子由冷星光轉暖金再轉紅）、每幀毫秒、繪製次數、三角形數**，以及 **JS 記憶體（只有 Chrome 有 `performance.memory`，沒有就整列不顯示）**；數字每 0.25 秒更新一次、圖每幀畫一格。兩個量測上的講究：時間用自己的時戳算（engine 的 dt 為了物理穩定被夾在 0.1 秒，直接拿來當 FPS 永遠看不到低於 10 的數字），繪製統計則在開啟時把 `renderer.info.autoReset` 關掉、由監視器自己在每幀開頭讀完再 reset —— 否則後製開啟時一幀 render 好幾次，`info` 只會剩最後一個 pass 的 1 個 draw call；關掉時原樣還原。可收合成一塊「只有 FPS」的小牌子、抓上緣可以拖到別的角落；整塊 `pointer-events: none`，只有把手與收合鈕接得到滑鼠，**永遠不會擋住玩家對世界的操作**；`z-index: 60` 疊在所有面板之上（它是診斷工具）；`prefers-reduced-motion` 下不畫會動的圖、只留數字。**預設關閉**，設定頁多一列平白說法的「效能監視器」（附註 <kbd>F3</kbd> 也能開關，主 HUD 的操作提示不動），熱鍵 **F3** 在打字中與面板打開時照樣有效（診斷工具），切換會同步寫回設定並讓開著的設定頁重畫勾勾；存檔新增 `settings.perfMonitor`，`normalize()` 對舊存檔與非布林值一律補 false。**關掉時是真的停掉** —— 解除 engine 的 update 掛勾，不是只 `display:none`（e2e 實測關閉後幀數計數器 2 秒內完全不動）。**驗證**：`npm run fonts` 重跑（語料 36 檔 / CJK 1526 字 / 1258.1 KB，指紋測試綠）；`npm run test:rubric` 12825 → **12878 個斷言全過**；`npm run test:playtest` **212 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 791 → **837 項檢查全過、零 console error**（新增 46 項：預設不顯示且一幀都沒處理、設定頁那一列寫得出 F3、打開後面板出現並讀得到 FPS／每幀毫秒／繪製次數（>0）／三角形數（>1000）／記憶體、整塊 pointer-events 是 none 而把手是 auto、z-index ≥ 50、預設位置不蓋到指南針與 HUD 按鈕且在畫面內、收合只剩 FPS 且 `aria-expanded` 同步、F3 關得掉並凍住幀數計數器、F3 再開得回來、跨重整仍記得、以及介面全文找不到任何「API key」之類的旁白）。順手修掉 e2e 一條**本來就會偶發失敗**的斷言：標題髮絲線的展開判定用字串比對 `!/matrix\(0/`，`matrix(0.999992, …)` 這種正常收尾會被誤判成失敗，改成解析出 scaleX 再比數值（> 0.99）。**下一步建議**：（a）效能監視器目前只讀 `renderer.info`，可以再加「geometries / textures / programs」這幾個記憶體面的數字，以及一顆「把目前這台機器的數值複製起來」的按鈕，回報卡頓時直接貼給開發者；（b）拖曳後的位置沒有存進設定，重整會回到右上角，之後可考慮一併持久化；（c）行動裝置仍未做（觸控搖桿、720px 以下的四幕與手印，效能監視器在窄畫面下也只做了寬度縮減）；（d）`index.js` 打包已達 1.15 MB（three.js 為主），manualChunks 拆出 three 這件事從 Phase 6 拖到現在，值得排進下一次。
- **2026-07-28 · Phase 18（技巧積木回到書寫檯旁邊：卡片牆壓成一條工具列）**：依產品指示「自由書寫模式的技巧積木長在右下角、視覺動線不對，而且每一片都太大」，把 `src/prompt/console.js` 第三幕的積木從右欄（`.rail`，排在刻痕對照與側頁籤之後）搬進 `[data-free]`，**緊貼書寫檯下緣**（DOM 與視覺順序都是「書寫檯 → 技巧積木 → 快速填入 → 送出」，距離輸入框 12px），玩家看著輸入框就看得到可以按的建材。**元件降階**：一格積木原本是「名稱 ＋ 說明 ＋ 中文片段 ＋ 收起來的『英文寫法 ↗』摺頁」的雙欄卡片（單片約 90–120px 高、整牆約 400px），現在改成刻印牌 **quiet 階的單行小石籤**（`--cut: 6px`、`min-height: 40px`、`padding: 6px 14px`、和快速填入同一階，牌面只有一個金色 `＋` 與技巧短名），實測**每片 40px、整條列 181px（8 片全解鎖時 3 行）且比書寫檯矮**；牌面短名把括號裡的英文注解拿掉（「角色設定（Role / System）」→「角色設定」），完整名稱、說明、會插進去的中文片段與英文寫法全部收進 `title`（以及給讀螢幕的 `aria-label`），**畫面上預設仍然看不到整句英文、也沒有任何一處把自撰翻譯講成官方說法**（護欄 2 未動：積木本來就不帶出處，官方說法與連結仍只在圖鑑與每條 rubric 的「出處 ↗」）。**行為一字未改**：按下去照樣把中文片段插到游標處、只列出已解鎖的技巧、`Tab` 走得到、`:focus-visible` 的符文環照舊，另補上與快速填入一致的「用過」樣式（`.block.is-used`）。CSS 同步把 `.block` 從刻印牌的 base 階移到 quiet 階（文件註解一併更新）、刪掉 `.block-cell` / `.block__zh` / `.block__en` / `.origin--block` 與 720px 以下「積木撐滿整行」的舊規則。序章練習台（`src/prompt/practice.js`）**沒有書寫檯也沒有積木**（Phase 13 起就是純石碑刻印），所以不需要跟進，維持一致。**驗證**：`npm run fonts` 重跑（語料 36 檔 / CJK 1526 字 / 1258.1 KB，指紋測試綠）；`npm run test:rubric` **12878 個斷言全過**、`npm run test:playtest` **212 個斷言全過**（皆未動資料層）；`npm run build` 通過；`npm run test:e2e` 837 → **873 項檢查全過、零 console error**（改寫舊的積木斷言並新增：積木不在 `.rail`、和輸入框同一欄（左緣差 ≤ 8px）、DOM 順序書寫檯 → 積木 → 快速填入、貼著書寫檯下緣（12px）、離輸入框 < 200px、每片 ≤ 64px、牌面只有一行、整條列比書寫檯矮、按下去仍插入中文並標成用過、`title` 查得到說明／插入內容／英文寫法、`aria-label` 比牌面長、積木列上看不到整句英文，以及 **1280×760 與 800×720 兩個尺寸下重跑同一組幾何量測 ＋ 面板零水平溢位**；另修掉這組測試原本會**在第一幕（`display:none`）量到 0×0 而空過**的問題，改成先走到第三幕並加一條「量得到版面」的守門斷言）；另以 headless 截圖人工複審兩個寬度的自由書寫版面。**下一步建議**：（a）提示球是 `position: sticky` 貼在第三幕底部，捲動時會蓋到快速填入那一排的最左邊一片（Phase 9 就存在，這次版面變長後更容易遇到），可以在球的左側留一段安全距離或讓它自動避讓；（b）8 片全解鎖時積木列仍要 3 行，之後若再加積木可考慮「常用 4 片 ＋ 其餘收進『更多』」；（c）`title` 在觸控裝置上摸不到，行動版需要另一套（長按或共用一顆 ⓘ），這與仍未做的行動裝置版面（觸控搖桿、720px 以下的四幕與手印）可以一起做；（d）`index.js` 打包 1.15 MB 的 manualChunks 仍未做。
- **2026-07-28 · Phase 19（效能監視器補上顯示卡：型號 ＋ 每幀 GPU 耗時）**：產品實測「GPU 顯示呢? 沒看到」。**先講清楚事實**：瀏覽器**沒有**任何 API 拿得到「GPU 使用率 %」，所以不假裝有，改成給拿得到的兩件事實並標明它們是什麼。**① 顯示卡型號**：從引擎既有的 WebGL context 讀 `WEBGL_debug_renderer_info` 的 `UNMASKED_RENDERER_WEBGL`（沒有這個擴充就退回 `gl.RENDERER`），再用新的純函式 `shortenGpuName()` 把 ANGLE 那串雜訊（shader model `vs_5_0 ps_5_0`、裝置 id `(0x…)`、`driver-31.0.…`、`(R)/(TM)`）削成「顯示卡 · 後端」（`ANGLE (NVIDIA, NVIDIA GeForce RTX 4070 Direct3D11 vs_5_0 ps_5_0, D3D11)` → `NVIDIA GeForce RTX 4070 · D3D11`；`NVIDIA GeForce RTX 4070 (ANGLE D3D11)` 這種寫法也認得），超過 44 字截斷、**完整原字串留在 `title` 裡**（縮寫過仍查得到原文）；`isSoftwareRenderer()` 認得 SwiftShader / llvmpipe / softpipe / swrast / Microsoft Basic Render，命中就在型號下面亮一行暖金警示「軟體渲染（未用 GPU）」。**② GPU 每幀耗時**：新增 `createGpuTimer()`，用 `EXT_disjoint_timer_query_webgl2` 在 WebGL2 context 上做 TIME_ELAPSED 查詢 —— **不阻塞**（送出後隔幾幀再輪詢 `QUERY_RESULT_AVAILABLE`，永遠不等 GPU）、**查詢物件池化**（上限 4 個、回收再用）、**GPU_DISJOINT 那一筆整個丟掉**（不顯示假數字）、context 出事或驅動沒有這個擴充就**安靜降級**成「不支援」（不是錯誤、不吵人、不丟例外）。因為後製開著時一幀會 render 好幾個 pass，量測必須圍住**整幀**：`engine.js` 新增 `onBeforeRender()` / `onAfterRender()` 兩排掛勾，緊貼著那一行 `composer.render()` / `renderer.render()`，監視器把 begin/end 掛在那裡（不是掛在 update 上、更不是逐 pass 量）。**③ 版面**：石牌沿用刻印牌語言，只多兩段 —— 數值列改成「CPU 幀 / GPU 幀」兩列並排讀（原本只叫「每幀」），下面一段「顯示卡」＋ 型號 ＋（必要時）警示行；**收合成小牌子時仍然只有 FPS，一格沒變**；解釋收在一顆 ⓘ 後面：「瀏覽器拿不到 GPU 使用率；這裡顯示的是 GPU 型號與每幀渲染耗時」。修掉一個實際踩到的版面 bug：石牌本身有 `clip-path` 切角，子元素跑不出這塊石頭 —— ⓘ 的氣泡照預設往外開會**被裁掉半句話**，改成 `position: static` 讓氣泡掛在標題列上、貼著石牌內緣同寬展開（並收掉指不到人的小尖角），e2e 補一條「氣泡整塊都在石牌裡」的守門斷言。**④ 零開銷**：型號只讀一次；計時查詢**只在監視器打開時建立**，關掉時 `dispose()` 把查詢物件全部 `deleteQuery` 還給驅動、解掉兩排掛勾、`gpuMs` 歸 null（`state()` 的 `gpuTiming` 會變 false，e2e 有驗）。`state()` 新增 `gpuName` / `gpuRaw` / `gpuSoftware` / `gpuMs`（不支援時為 null）/ `gpuTiming`。**驗證**：`npm run fonts` 重跑（語料 36 檔 / CJK 1531 字 / 1260.4 KB，指紋測試綠）；`npm run test:rubric` 12878 → **12920 個斷言全過**（新增 42 條：`shortenGpuName` 對 NVIDIA / AMD / Intel / Apple / SwiftShader / llvmpipe / headless Chrome 那串 `SwiftShader driver` 的整理與截斷、`isSoftwareRenderer` 的正反例、`readGpuName` 的 UNMASKED → RENDERER → 例外三條路徑、以及用假的 WebGL2 context 跑完整個計時器循環：非阻塞輪詢、一幀一個查詢、奈秒換毫秒、查詢物件回收、GPU_DISJOINT 丟棄、dispose 真的刪查詢、context 中途出事會安靜停掉）；`npm run test:playtest` **212 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 873 → **900 項檢查全過、零 console error**（新增 27 項：CPU 幀 / GPU 幀 兩列都在、型號非空且＝`state().gpuName`、原字串留在 title、雜訊都清掉且 ≤44 字、**headless 的 SwiftShader 確實被認出是軟體渲染並亮出暖金警示**、GPU 每幀不是 ≥0 的數字就是安靜的「不支援」、ⓘ 預設收著／hover 才開／文字講明拿不到使用率／氣泡不出石牌／接得到滑鼠、收合時 GPU 段一起收起、關掉後計時查詢全部收掉且不留舊數字但型號仍記得、石牌沒有變寬也沒有跑出畫面），另以 headless 截圖人工複審兩個狀態（一般 / ⓘ 展開）。**下一步建議**：（a）GPU 每幀目前顯示最近一次查詢結果，可以像 FPS 那樣做一條 GPU 的柱狀圖，或至少加個幾幀平均，數字才不會跳；（b）拿到 CPU 與 GPU 兩個數字之後其實已經看得出「誰是瓶頸」，可以直接在牌子上寫一句白話結論（GPU 幀 ≫ CPU 幀 → 建議切低畫質），對一般玩家比兩個毫秒數有用得多；（c）`EXT_disjoint_timer_query_webgl2` 在不少驅動上會缺席（顯示「不支援」），必要時可退一步用 `renderer.info` ＋ CPU 幀時間估個大概，但要標明是估算；（d）行動裝置仍未做（觸控搖桿、720px 以下的四幕與手印、石牌在窄畫面的位置）。
- **2026-07-30 · Phase 20（下拉選單看得見字 ＋ 全場穿模稽核：不再有「看得到卻走得過去」的東西）**：兩個產品回報的 bug，都用「訂一條規則、寫成測試」的方式收掉。**① 下拉選單（設定頁的畫質與答題方式）打開後白底白字**：彈出清單是**瀏覽器 / 作業系統畫的**，CSS 只碰得到兩件半 —— `color-scheme: dark`（清單的底、外框、陰影、捲軸；Chrome / Firefox / Safari 都認，也是 macOS 原生選單唯一有效的手段）、`option` / `optgroup` 的 `background-color` 與 `color`（Chrome / Edge / Firefox 認，macOS 忽略），字型 / 圓角 / 列高一律碰不到。所以三層都補上：`:root` 與 `select` 都宣告 `color-scheme: dark`，`option`／`optgroup` 明講深底（`#0b1119`）＋亮字（`#eef4fa`），`option:checked` / `:hover` / `:focus` 加一層暖金底（鍵盤上下移動看得出走到哪）、`option:disabled` 轉灰；規則從 `.settings__row select` 提到**全域**，之後任何地方多一個下拉都直接是深色。收起來的石籤樣式一格未動（已用截圖複審）。**② 一座石座的石頭走得過去 → 全場稽核**：先寫 `scripts/collision-audit.mjs` 訂出「有份量」的可執行定義（世界座標外接盒：佔地半徑 ≥ 0.5 ／ 露出地面 ≥ 0.9 ／ 底緣離地 < 1.6 ／**最薄兩軸 ≥ 0.9**），把整棵場景圖 1703 個網格（含 instanced 的每個實例）攤開比對碰撞登記表，**抓出 174 個「有份量卻沒有碰撞體」的物件**：五區植被共 96 件（錐形矮樹 / 樹幹 / 巨石 / 方塊 / 圓環 —— Phase 5 加進來之後從來沒被標過）、中央高原 14 顆中型碎石（半徑落在舊的 0.62 下限之下）、reasoning 的 9 個思考環（高度沒把環自己的半徑算進去，最低的幾個插進地裡）、藏書之樹的 5 條板根與巨臂吊車那片 8.4 公尺高的立輪（地標只登記了塔身）、config 的 4 根面具柱與 foundations 的爐火碑（**被石座 / 祭壇的淨空濾網當成雜物掃成幽靈** —— 產品看到的那一顆就是這一類）、小舞台的布幔與示範牌等小景零件（只有根節點有一個圓）。**根因有三個**：（a）`collectSolids()` 用「局部外接盒 × 縮放」推高度與佔地，**沒有算旋轉** —— 躺下來的環、斜插的巨石、轉了 90° 的大齒輪一律量錯，於是被當成「貼地矮件」或「飄在半空」跳過；改成 `Box3.applyMatrix4()` 逐實例算世界座標外接盒。（b）淨空濾網 `CLUTTER_RADIUS` 是 1.2，把 0.7 的面具柱與 0.75 的石碑也掃掉了；收到 0.62（只掃真正的碎石），石碑改標 `keepSolid`，面具柱改成**擺放時就避開石座淨空圈與橋的主動線**（而不是擺了再把碰撞拿掉）。（c）一組小景只有根節點登記半徑；新增 `markSolidParts()`：凡是最薄兩軸 ≥ 0.9 公尺的網格自動標成實體（布幔、板根、輪盤、鏡面會擋；梯子的邊柱 0.1、招牌的桿子 0.05、細枝、電線不擋；半透明的光 / 水 / 霧一律不擋），套用在小景、地標與石碑上。另外把 `SOLID_MIN_RADIUS` 0.62 → 0.5、五區植被原型逐一標好擋不擋、植被的動線避讓從 2.6 放到 4.2、思考環抬到底緣離地 ≥ 3.2 公尺（真的飄起來）、`LANDMARK_SOLIDS` 補上藏書之樹的五條板根與吊車立輪的三個圓。**順手修掉一個手感問題**：沿牆滑原本只「鎖住被擋的那一軸」，正面直直撞上圓石時（鏡頭沒轉過，前進方向貼著座標軸）另一軸位移是 0 → 人整個黏在石頭上繞不過去；改成把位移拆成「指向圓心」與「切線」兩份、丟掉前者留下後者（軸向鎖定保留為退路，地形邊緣與閘門仍走那條）。碰撞體 475 → **828**（低畫質 597），**稽核剩 0 件**，石座 26 座 × 24 個方向 × 2/3/4/5 公尺全部走得到、四條橋主動線與出生點 / 祭壇全部淨空。**驗證**：`npm run fonts` 重跑（語料 36 檔 / CJK 1539 字 / 1268.7 KB，指紋測試綠）；`npm run test:rubric` 12920 → **12986 個斷言全過**（新增：高 / 低畫質兩份世界的**穿模稽核＝0**、例外清單短且每條都有白話理由、`markSolidParts` 的厚 / 細 / 半透明 / 已標過 / 縮小後不夠厚五種情況、12 塊石碑本體都擋得住人、板根與立輪各自擋得住而樹外面照樣走得過、面具柱不在任何石座的淨空圈裡、碰撞半徑分兩段把關（外接盒推的 ≤ 3.6、明講的地標臺座 ≤ 8）、碰撞體數量上限、正面直撞會被切線帶著滑開且落點不在任何道具裡）；`npm run test:playtest` **212 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 900 → **934 項檢查全過、零 console error**（新增 34 項：兩個下拉的 `color-scheme`、選項底色是實心深色（亮度 < 0.12）、選項文字對比 ≥ 4.5:1、選中那一列也 ≥ 4.5:1、收起來的石籤也 ≥ 4.5:1；走進矮樹被擋下來、擦邊走會沿著它滑過去並且真的繞到另一側、走進爐火碑被擋下來但 2.4 公尺外照樣讀得到、中型碎石也擋得住、石座四個方向都站得進去也按得到 E），另以 headless 截圖複審設定頁收起來的樣子。**下一步建議**：（a）彈出清單的**列高與字型**在任何瀏覽器都改不了，真要完全一致就得自己做一個 listbox（`role="listbox"` ＋ 鍵盤操作），但那要連無障礙一起做，值不值得看之後還會不會有更多下拉；（b）碰撞體現在是 828 個圓柱、`solidAt()` 是線性掃描，目前每幀幾十微秒沒問題，若之後道具再翻倍可以加一層 8×8 公尺的網格索引；（c）長條形的東西（書架、石凳）用「沿長軸排一串小圓」近似，轉角處會比看到的胖一點，要更貼得改 OBB；（d）行動裝置仍未做（觸控搖桿、720px 以下的四幕與手印）。

- **2026-07-30 · Phase 21（導航閃爍提示 ＋ 可分享的結果卡與稱號）**：**① 導航閃爍提示**（`src/ui/nudge.js`）—— 指南針只回答「哪個方向」，玩家在原地繞一分鐘時畫面上沒有人會提醒他。新增畫面**上方中央**一行會慢慢呼吸的刻文（不綁 3D 座標、`pointer-events:none`、壓在 HUD 頂列下方且與左下指南針／右上效能監視器互不重疊）。觸發條件三個同時成立：距離「下一個目標」（＝ HUD 與指南針同一個 `world.objectiveTarget()`）**50 秒沒有明顯靠近**（靠近判定＝把歷史最短距離再縮短 6 個世界單位）、這段期間沒開過任何面板也沒完成任何事（都會把計時歸零）、且不在 90 秒冷卻中；已經走到 14 單位內就不再催。收起條件：真的往那邊走了／打開面板／顯示滿 8 秒自己淡出。文案走回聲的口吻：`回聲 ／ 往<b>西北</b>，前往清晰之門 ／ 約 84 步 · 走近後按 E`，方位詞由世界座標算出（北 = −Z、東 = +X，八方位，與指南針同一套定義）。**新區域解鎖時立刻說一次**「○○ 已開啟，往前走吧 ／ 往西北 · 約 84 步」（不受冷卻限制，通關解鎖與讀碑解鎖兩條路都接上了）。`prefers-reduced-motion` 只留一次淡入、不閃爍。**② 可分享的結果卡**（`src/ui/sharecard.js` ＋ `src/data/ranks.json` ＋ `src/progression/ranks.js`）—— 里程碑按「分享」，就地用 **canvas 畫一張 1200×630（og-image 比例）的 PNG**，下載或（瀏覽器允許時）複製到剪貼簿；**完全離線**：沒有任何外部服務／SDK／網路請求，字型用已自架的子集（`document.fonts.load` 後才畫），石面斑駁、5200 顆顆粒、切角外框與金髮絲全部是程序繪製，零圖檔。卡面走夜間檔案館 ＋ 刻印牌：左欄稱號（大襯線）＋ 英文副名 ＋ 一句話 ＋（結果卡才有的）「本次刻印：關卡名」＋「刻進圖鑑的技法」三條（技巧中文名 ＋ 右對齊的真實技巧 id，過長自動補省略號），右欄等級方章 ＋（結果卡才有的）S/A/B/C 圓形印章 ＋「46 / 68」與金色進度線 ＋ 五片土地的封印（已精通亮暖金並標 MASTERED），頁腳是品牌名 ＋ SEO 標語 ＋「夜間檔案館 · THE NIGHT ARCHIVE」。**稱號 8 級**（`ranks.json`，`authored: "game"`，檔頭明講是遊戲自撰的世界觀稱謂、不是任何官方分級）：初醒的旅人（Lv.1 / 0 條 / 0 片）→ 刻印學徒（2 / 8 / 0）→ 抄寫人（3 / 14 / 0）→ 讀碑者（4 / 22 / 0）→ 守卷人（5 / 33 / 1）→ 釋義者（6 / 46 / 2）→ 神諭使者（7 / 57 / 3）→ 神諭共鳴者（8 / 68 / 5），規則是「等級 ＋ 已收集技巧數 ＋ 已精通區域數**三個同時滿足**取最後一個」，門檻全單調 → 進度變多不可能降級。**入口四處**：結果面板通過後的「分享這次的刻印」（卡片標亮這一關剛學到的技法 ＋ 帶評價）、圖鑑頂端新增的稱號列（顯示目前稱號／等級／下一個稱號還缺什麼）旁的「分享收集成果」、圖鑑裡每片**已精通**土地的「分享這片土地的封印」、隱藏成就畫面的「分享這趟旅程」；按鈕一律走刻印牌的 quiet 階，點下去開的是**疊在原面板上面**的預覽層（z-index 24 > 一般面板的 20），關掉後底下的結果／圖鑑原封不動，Escape 也一定先關它。**過程中修掉兩個自己做出來的坑**：（a）提示原本用 `hidden`（`display:none`）收起來，「從 none 變回 block 的同一拍又加上 `is-on`」會被瀏覽器合併成一次樣式計算、淡入整個被跳過（e2e 量到 opacity 卡在 0），改成永遠留在版面上、用 `opacity` ＋ `visibility`（收起時 visibility 延遲到淡出跑完）切換；（b）分享卡的圖原本是 `width:100%`，在 1280×713 的視窗下把「下載圖片」擠到摺線下面，改成把圖的寬度綁在面板可用高度上（`min(100%, max(240px, calc((94vh − 400px) × 1.905)))`，1.905 = 1200/630）並收窄標頭，e2e 直接斷言「下載圖片」不用捲動就看得到。**驗證**：`npm run fonts` 重跑（語料 40 檔 / CJK 1552 字 / 1277.7 KB）；`npm run test:rubric` 12986 → **13170 個斷言全過**（新增：ranks.json 的結構與「不得帶連結／不得有 source 欄位／不得出現英文句子」、8 個稱號的三個門檻全單調且每一級都真的變高、拿每個稱號自己的門檻查一定查回它自己、三個條件是 AND、**用真的進程系統跑一次「26 關全部只拿 C」的最壞路徑，八個稱號必須依序全部出現過且不倒退**、全 C 通關後 68/68 ＋ 五片精通 ＋ 等級足以拿到最高稱號、`rankStats` 的四個數字、方位詞八方向與世界座標一致、提示的四個時間常數在合理範圍且冷卻長於閒置門檻）；`npm run test:playtest` **212 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 934 → **1025 項檢查全過、零 console error**（新增 91 項：種一份 Lv.6 / 46 條 / 精通 2 片的存檔重載後稱號必須是「釋義者」且指得出下一個「神諭使者」、圖鑑頂端的稱號列與兩顆已精通土地的分享鈕、按下分享 → 卡片 1200×630、像素不是全黑且標頭那行真的有字、下載連結是 `data:image/png;base64,` 開頭且長度足夠、檔名帶等級與 `.png`、卡片資料的稱號／等級／收集數／精通數／技法三條全部正確、canvas 有 aria-label、焦點鎖在分享面板內、z-index 高於底下的面板且底下面板沒被關掉、面板內零外部資源節點、「下載圖片」不用捲動就看得到、820px 窄畫面下面板與整頁都無水平溢位、通關後的分享卡是 `result` 變體且標亮的技法就是這一關 `teaches` 的那幾條；提示：閒置 50 秒後出現且方位詞與世界方位一致、句子裡有目標名與金色方位詞、`pointer-events: none`、落在畫面上半部並水平置中、不蓋到指南針、往目標靠近立刻收起並進入冷卻、冷卻中閒置再久也不冒出來、冷卻結束後會再提醒一次、顯示滿 8 秒自己淡出、解鎖時的變體文案、打開面板立刻收起且閒置歸零），另以 headless 截圖人工複審導航提示的兩種變體（閒置 / 解鎖）、圖鑑稱號列、兩種分享卡（收集冊 / 通關結果）與預覽層。**下一步建議**：（a）分享卡目前是靜態一張，可以做「把 68 條的收集格陣列畫成一面牆」的第二種版型，收集感會更強；（b）區域精通的慶祝目前只有 toast ＋ 螢幕級橫幅（沒有可按的畫面），分享入口只好掛在圖鑑的該片土地上，之後若做成一張真正的「精通結算頁」就可以把分享鈕放在那裡；（c）導航提示只在桌機驗過，行動裝置（觸控搖桿、720px 以下的提示與分享面板）仍未做；（d）稱號目前只出現在圖鑑與分享卡，可以考慮讓 HUD 的等級方章旁也刻上稱號，讓「我現在是誰」隨時看得到。
- **2026-07-30 · Phase 22（世界觀聖經 ＋ 刻文小語 ＋ 會回應的東西與地圖彩蛋）**：**① `WORLD.md`（世界的單一真相來源）**：新增倉庫根目錄的世界觀聖經，八節——世界觀（神諭＝模型、回聲＝唯一會說話的存在且一句 ≤31 字、抄寫人＝所有中文譯寫層的設定來源、五片土地的性格與地標、「這個世界沒有會走動的 NPC」）、視覺與調性（一律夜景、有限色盤、暖金只給成就熱點、靠打光不靠資產量、高的東西旁邊要矮）、**互動文法**（`E` 是唯一互動鍵、四層互動與各自的半徑與搶鍵優先序、分幕揭示、**換皮但不說謊的對照表**、不會失敗、畫面禁字表）、場景敘事（地標／小景／走出來的路／會回應的東西／祕密的擺法）、命名規範（`類型:id` 的場景圖節點名、資料檔的 `authored` 標記、存檔欄位一律純加法）、效能與碰撞的硬規則（三角形／燈光／碰撞體預算、每幀迴圈的四條禁令、碰撞稽核的四條判定、五處絕不可被擋住的淨空）、參考來源、以及一份 **29 條的維護檢查表**（世界觀／互動文法／教學內容護欄／美術／效能／碰撞／存檔／測試八組，每次新增世界內容都要逐條回答）；CLAUDE.md 的「開發護欄」上方加一行指回它（本檔護欄仍優先）。**② 刻文小語（第三層互動）**：新增 `src/data/inscriptions.json` ＋ `src/world/inscriptions.js` ＋ `src/ui/inscription.js` —— **13 則**刻在角落的字（5 種載體：小刻石／木牌／刻柱／池邊石／工具吊牌，五區都有，成組落在小景旁 7–16 公尺），走近按 `E` 開一個**很小的對話窗**（沒有四幕、沒有石碑、沒有輸入框）：兩句世界的話分段浮出 → 「這一段刻的是」＋技巧名＋**既有的中文說法**＋一句可以照著做的白話 ＋ **`神諭原典：<文件名> ↗`** 的可點官方連結。護欄 2 的作法：資料層**只存 `techniqueId`**，教學句子一律在執行期由 `content.displayTechnique()` 取出、出處由 curriculum 產生（測試強制驗證資料層不自帶 `tip`／`source`／`teaches`），第一次讀 +5 XP 並把那條技巧寫進圖鑑，重讀不給分、不寫 `bestGrades`、不算區域解鎖的通關數；圖鑑底部新增安靜的「走出來的收集」（刻文小語 x / 13、藏起來的地方 x / 4）。**③ 會回應的東西（研究先行）**：先查了 Bruno Simon 的 portfolio case study（互動區域本身就是回饋、碰撞音靠同一組相似音隨機調整音量與速度）、Game Developer 的〈Creating an Interactive Audio Environment〉（稀疏的一次性觸發勝過更大聲的循環、保留「最近放過的 N 個音」拒絕重複）、web.dev 的〈Developing game audio with the Web Audio API〉（改播放速率模擬隨機性、最後一級放 `DynamicsCompressorNode`）與 Codrops 的 three.js instancing（per-instance 屬性 ＋ 單一 `uPlayerPos`），據此寫成 `src/world/reactive.js`：**6 種反應 × 22 處**（風鈴架晃起來響一聲金屬泛音／光菇圈一朵一朵依序亮起（純光無聲）／音石列每顆一個音、沿著走就走出一段五聲音階旋律／靜水盤盪開一圈水紋／守望的小獸遠遠看著你、太近就竄開約 9 秒後回來／螢火群往外散開再慢慢聚回），五區都有、彼此相距 >11 公尺（離太近會同時響、聲音會糊掉）。實作規則：**兩個半徑的 hysteresis**（離場 = 進場 × 1.3，站在邊界上不會連續觸發）、**平方距離**比較（不開根號、不 raycast、不算 Y）、**距離分級**（45 公尺外整組跳過、15 公尺外每 3 幀且用 index 錯開）、**零每幀配置**（觸發點壓成一條 `Float64Array`，tick 裡不 new／不 map/filter／不建閉包）、聲音三層節流（全域 90ms ＋ 每點 2.2 秒 ＋ 最近 4 個音的環狀緩衝，且緩衝的作法是**換一個音而不是不放**——沉默不是回應，音石列刻意排好的旋律則完全不受緩衝影響）、面板打開／序章進行中整組停手、`prefers-reduced-motion` 只關掉「動」不關掉光與聲音；audio.js 新增 8 支合成音（chimeSoft／songnote／ripple／scurry／flutter／bloom／secret／blessing）並在 master 之後掛上 `DynamicsCompressorNode`。**④ 4 個藏起來的地方**（`src/data/secrets.json`，純風味、與世界觀石碑同一層護欄：不教技巧、不放連結、無 `source`／`teaches`，測試強制驗證）：**第七十三條**（foundations 南緣一塊被推倒的玩笑碑）、**星圖林**（藏書之樹背面，六根細柱撐著一張抽象星圖——刻意不影射任何真實標誌）、**回聲的小祠**（巨臂吊車背面，獨一無二的六音祝福 ＋ 隱藏標記 `echoBlessing`，圖鑑低調標一句「回聲的祝福」）、**靜語窪地**（無盡階梯塔背面）；一律在地標背面或地圖角落、離主動線 >8 公尺，**走進去就算找到**（不用按 E ——好奇心不該還要學一個鍵），+12 XP ＋ 一句 toast ＋ 一記光湧，不彈任何面板。**⑤ 順手修掉一個一直在空轉的測試**：e2e 的「沿牆滑」擦邊路線用 `s.r + 0.5` 當側向偏移，但 `solidAt` 的判定半徑是 `s.r + 0.62` —— 這個前提在目標自己身上永遠是 false，於是**篩選結果從來都是空的**、每次都退回一棵兩側從未被驗證過的樹，量到的其實是「卡在兩顆石頭的夾角裡」（node 掃 24 個鏡頭角度：舊碼有 8 個角度會失敗，純看當下 yaw 落在哪一段）；改成 `s.r + 1.05` 之後 24 個角度全部通過，並補一條「找得到一個兩側都空的擦邊目標」的前提斷言。**效能**：三角形 121,366 → **133,394**（+12,028，+9.9%，預算 420k）、**燈光 44 → 44（一盞都沒加**，全部用自發光材質與加色光暈片）、低畫質 70,498 tris / 31 lights、碰撞體 815 → 809（新的留白清單掃掉 6 顆雜物碎石，倒下的第七十三條碑補上 1 個碰撞體）、幾何體 +28 / 材質 +35（全部走模組層快取）。**驗證**：`npm run fonts` 重跑（語料 45 檔 / CJK 1581 字 / 1293.5 KB）；`npm run test:rubric` 13170 → **14074 個斷言全過**（新增：13 則刻文的技巧與出處都真實存在且為 https、資料層不自帶教學句子與出處、世界的話與提示都是中文且無連結、落點在區域內／不搶石座與石碑的 E 鍵／彼此 >10 公尺／四周 16 個方向至少 14 個走得到；4 個祕密的純風味護欄與「不在必經路線上」；22 處反應的種類、五區覆蓋、彼此間距、不壓在刻文與石座上；**反應場的行為**：遠處不觸發、走進去響一次、站著不動不重觸發、在進場與離場半徑之間不抖動、冷卻內不再響、冷卻過了會再響、面板打開整組停手、走過一排音石每顆都響且音高互異、風鈴不會連續放同一個音、祕密只回報一次；兩個新存檔欄位的舊檔相容／去重／重載／重置；讀刻文會收技巧但不算通關、找祕密不進圖鑑不算徽章、兩者都不足以解鎖新區域）；`npm run test:playtest` **212 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 1025 → **1083 項檢查全過、零 console error**（新增 57 項：三層東西都在場景圖上且**反應層一盞燈都沒加**、走近風鈴管子真的在動、走近光菇圈亮度總和 >1.5 倍、刻文小語完整流程（提示→`E`→小視窗→分段揭示的動畫延遲遞增→技巧名與出處逐一比對 curriculum 真實值→+XP→寫進圖鑑與 localStorage→重讀不給分）、圖鑑的「刻文小語 x / 13」計數、祕密走進去就找到且不彈面板／只算一次／`echoBlessing` 出現在圖鑑、面板打開時走進反應範圍世界安靜、關掉面板同一個地方照樣有反應、13 則刻文與 22 處反應四周都走得到、重整後兩個新欄位與祝福都還在）。**下一步建議**：（a）反應層目前全是 CPU 距離判定，texture 級的連續反應（腳邊地面微亮、草往外分開）可以改用單一 `uPlayerPos` uniform ＋ per-instance 屬性做成 shader 版，成本幾乎為零而且「世界注意到你」的密度會再高一階；（b）刻文小語的 13 條技巧與 26 關的 `teaches` 有重疊，可以改成「優先掛還沒被任何關卡教到的技巧」讓兩層互補；（c）祕密目前只有 toast，可以做成一本很薄的「找到的地方」分頁（同石碑圖鑑的建議）；（d）行動裝置仍未做（觸控搖桿、720px 以下的小對話窗版面），而且反應層在觸控上沒有「走近」以外的表達方式，值得一併想。
- **2026-07-30 · Phase 23（純鍵盤走得完全程：不用滑鼠也玩得動）**：依產品指示「我想支援可以純鍵盤控制，使用者還要拉上去用滑鼠有點麻煩」，先把每一個畫面逐一走過、列出「只有滑鼠做得到」的動作，再一條一條補上鍵盤的路。**稽核出來的七個缺口與作法**：①**鏡頭拉遠拉近只有滾輪** → 新增 `-` / `=`（別名 `PageDown` / `PageUp`、數字鍵盤 `−` `＋`），按著會連續變化、與滾輪共用同一支夾範圍的 `setZoom()`，並開放 `player.cameraDistance` / `zoomRange` 給 HUD 與測試讀；②**輸入框把 `Esc` 與 `Tab` 一起吃掉**（`stopPropagation` 沒有例外）→ 鍵盤玩家會被關在書寫檯裡出不來，改成只擋會驅動角色的鍵；③**沒有任何地方講得出有哪些鍵** → 新增 `src/ui/keyhelp.js`「操作一覽」：`?`（Shift + /）隨時叫得出來、疊在最上面那一層（讀題讀到一半也行、不會把關卡收掉）、四組 21 條、`Esc` 收起，開場說明卡與設定頁各留一個入口，HUD 底下那行也補上；④**幕指示器 / 線索 / 神諭刻文側頁籤 / 提示球 / 換答題方式 / 分享只點得到** → 分別給 `Alt`+`1…4`、`L`、`H`、`M`、`S`（`1 2 3` 留給石碑，所以跳幕走 Alt），全部**在文字欄位有焦點時一律失效**（`isTypingIn()`），並把 `<kbd>` 鍵帽戴在對應的東西身上；⑤**快速填入石籤 / 技巧積木 / 石碑選項 / 圖鑑 68 條只能一個一個 Tab** → 新增共用的 `rovingList()`（方向鍵在同一組裡走、`Home` / `End` 跳頭尾、只移動焦點不改狀態），四處全部接上；⑥**每一層打開時焦點落在標頭那顆關閉鍵上**（等於一開就站在出口）→ `createOverlay` 改成優先落在**內容區**第一個可按的東西，內容區沒東西可按（石碑、刻文那種小窗）才退回關閉鍵；⑦**提示框開了焦點沒跟進去、關掉沒還回來** → 收斂成一支 `setCoachOpen()`。**過程中挖到的一個真 bug**：收起來的 `<details>` 裡面的東西在新版 Chrome 走的是 content-visibility，`offsetParent` 還在但根本沒被畫出來，`focus()` 對它是空包彈 —— 方向鍵會「移動了但焦點卡在原地」；`rovingList()` 改用 `getClientRects().length > 0` 判定，圖鑑的選擇器也收緊成 `.tech > details > summary`（不要把展開後才出現的「原文 ↗」摺頁算進去）。**維持不變**：滑鼠全部照舊（滾輪、拖曳、點擊一個都沒動）；效能監視器的**拖曳位置**仍是滑鼠專屬 —— 它是診斷工具、不影響遊玩，開關（`F3`）與摺疊（Tab 到摺疊鍵）都有鍵盤路徑，已寫進 WORLD.md 當成刻意保留的例外。**文件**：WORLD.md §3.1 重寫成「世界層 ／ 開著一層時」兩張表 ＋ 四條配套規則（單鍵快捷在打字時失效且 `Esc`/`Tab` 不准被攔、有快捷就戴鍵帽、開一層焦點落在內容區、焦點只准停在真的畫得出來的東西上）＋ 一條新鐵則「新增互動之前先問純鍵盤怎麼做到這件事」；README 的操作表拆成「世界 ／ 開著一關」兩張並加上「不碰滑鼠也走得完」的前言。**驗證**：`npm run fonts` 重跑（語料 46 檔 / Latin 242 · CJK 1583 · Display 104 / 1295.7 KB，指紋測試綠）；`npm run test:rubric` 14074 → **14262 個斷言全過**（新增：操作一覽的資料合法性（分組 id 唯一、每條中文 ≤ 40 字、無整句英文、不放連結、不宣稱技巧、不出現系統術語）、**一覽上寫的鍵程式裡真的接了**（拉遠拉近的按鍵表與每幀迴圈、`?` 的開關與 Esc 順序與 anyPanelOpen、`L`/`H`/`M`/`S`/`Alt`+數字 的 case、打字守衛、`Esc`/`Tab` 不被輸入框攔）、`rovingList` 四處都接上且只走畫得出來的項目、鍵帽戴在該戴的地方、三個入口都提到 `?`）；`npm run test:playtest` **212 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 1083 → **1177 項檢查全過、零 console error**（新增 94 項「純鍵盤走完一圈」：全程只發真的鍵盤事件 —— 按 W 走到石座（走近提示出現且標著 `E`）→ `E` 開啟 → `Enter` 推到指引（刻文段數與神諭原典連結都在）→ `L` 翻開線索再收起（焦點跟著走）→ `Enter` 進刻印（焦點自己落在第一個選項）→ 方向鍵移動焦點 → 數字鍵一段一段把石碑刻滿（刻痕對照跟著亮）→ 焦點自己落在手掌印 → 按住 `Enter` 觸發 → 結果 ＋ 評價 ＋ 記成通關 → `S` 開分享卡（焦點進去、底下那一關沒被收掉）→ `Esc` 收分享、再 `Esc` 收關卡 → `C` 圖鑑（焦點落在內容裡、方向鍵走條目、`Enter` 展開並看得到可點的官方出處）→ `O` 設定（Tab 走到畫質、方向鍵改值並即時生效再改回來、空白鍵切靜音再切回來）→ `Esc` → `-` / `=` 鏡頭真的拉遠拉近且夾得住上下限 → `?` 操作一覽（四組 21 條、鍵帽對得上、零連結、零水平溢位、打開時角色站著不動、`Esc` 收起後又走得動）→ 最後在書寫檯打進 `lehmsco1?` 九個字元，**一個不漏**且沒有翻開側頁籤、沒彈提示框、沒換模式、沒跳圖鑑 / 設定 / 分享 / 一覽、沒換幕，再按 `Esc` 直接走得出去）。**下一步建議**：（a）行動裝置是最後一個「輸入方式」缺口（觸控搖桿、720px 以下的四幕與手印儀式）；（b）可以做「按鍵重新配置」並存進設定 —— 左手 ／ 非 QWERTY 版面的人現在只能將就；（c）效能監視器的拖曳可以補上「焦點在把手時方向鍵移動」，那是唯一剩下的滑鼠專屬操作；（d）`KEY_GROUPS` 目前是畫面與測試的單一真相，但快捷鍵的**註冊**還散在各檔案裡，之後可以讓註冊也吃同一份資料，新增快捷就不可能忘記更新一覽。

- **2026-07-30 · Phase 24（結果卡分享出去：一條系統分享的主路 ＋ 各家老實的側路）**：Phase 21 的結果卡本來只能「下載 / 複製」，這次讓它真的走得出去，而且**零 SDK、零註冊、零外部請求、核心迴圈仍然不需要網路**（護欄 3）——全部是玩家按下去才發生的一次動作。**主路（唯一能把圖片本身交出去的路）**：`navigator.canShare({files:[png]})` 過關才露出 hero 的「分享…」，按下去開系統分享面板，玩家自己挑已登入的 Facebook / Instagram / Threads / Messenger / LINE…；`navigator.share()` **前面一行 await 都沒有**（手勢一斷就開不起來），所以 blob 與 `File` 在**開卡時就先備好**（1200×630 的 PNG 在軟體渲染機器上要編碼好幾秒），偵測則用一個只有 8 bytes 檔頭的假 PNG（`SHARE_PROBE`）在第一幀就問完 —— 入口不會晚一拍才冒出來把鍵盤焦點搶走；玩家自己取消（`AbortError`）不當成失敗、什麼都不說。**側路（桌機沒有檔案分享時）**：`分享到` 一排 quiet 階石籤 —— Facebook（`sharer.php?u=&quote=`）與 Threads（`/intent/post?text=`）是**真的 `<a target="_blank" rel="noopener noreferrer">`**（Enter 就開得起來、沒有彈窗攔阻問題），按下去同時把**圖 ＋ 那句話寫進同一個 `ClipboardItem`**（`image/png` ＋ `text/plain`，寫剪貼簿在手勢內同步開始）並吐一句「圖片已複製 —— 到剛打開的那一頁貼上就行」；Messenger 在手機走 `fb-messenger://share?link=`、**桌機沒有帶內容的入口就老實不做連結**，只複製並說「打開 Messenger 貼上就行」；**Instagram 沒有任何網頁投稿入口 → 那一片不點燈**（`disabled` ＋ 說明「只能用手機上的系統分享」），不做假按鈕、不假裝有路。畫面上直接寫明「Facebook 與 Threads 的網頁入口只帶得走文字和連結」——**換皮但不說謊**延伸到能力邊界。**那句話與網址**：`shareText()` 用世界的說法（「我在 PromptArcade 修行成了『釋義者』（Lv.6）—— 已把 46 / 68 條技法刻進圖鑑。」，四種卡各有一句），後面接品牌那一句與**唯一一個網址常數 `SHARE_URL`（目前是 repo 網址，帶著「TODO 部署後改成正式網址」，不發明網域）**；系統分享的 `title` 是「PromptArcade — <稱號>」，刻意**不帶 `url` 欄位**（有些系統會因此把檔案丟掉）。**鍵盤（Phase 23 文法）**：石籤走 `rovingList`（← → Home End，沒點燈的那片自動跳過）、`Enter` 就是按下去、鍵帽戴在畫面上；有系統分享時「下載圖片」自動退成 quiet 階，畫面永遠只有一個主角。**驗證**：`npm run fonts` 重跑（語料 46 檔 / CJK 1584 字 / 1295.8 KB，指紋測試綠）；`npm run test:rubric` 14262 → **14369 個斷言全過**（新增：網址常數與 TODO、沒有發明的網域、零 SDK 字眼、四種卡的那句話與禁字表、各家 intent 的編碼與「沒有路就回 null」、Instagram 不得有假網址、`systemShareSupported` 的七種偵測情境、手機判定要 UA ＋ 觸控兩者、**靜態掃描 `navigator.share` 之前不准有 `await` / `async`**、剪貼簿帶圖 ＋ 帶文字、假 PNG 探針、rovingList 與鍵帽、畫面說明的老實話與禁字）；`npm run test:playtest` **212 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 1177 → **1259 項檢查全過、零 console error**（新增 82 項：把 `navigator.share/canShare/clipboard/window.open` 換成假的來走三條路 —— 沒有檔案分享時入口收起來且畫不出來、四片石籤的 tag/href/target/rel/encode 逐一比對（含中文編碼與「沒有沒編碼的字元」）、桌機 Messenger 不亂開頁、Instagram 按不動也不偷偷做事、按 Facebook 只寫一次剪貼簿且同時含 `image/png` 與 `text/plain` 並開對的那一頁、方向鍵 / Home / End 在石籤間移動且跳過沒點燈的、`Enter` 等於按下去；有檔案分享時入口變 hero 且「下載圖片」退階、焦點落在它身上、真的按下去交出一個 **`instanceof File` 的 PNG（>20KB）** ＋ 標題 ＋ 那句話 ＋ 不帶 `url`，並用 `navigator.userActivation.isActive` **證明手勢沒斷**、`AbortError` 不吐任何提示；820px 下四片石籤都在面板內、無水平溢位、「下載圖片」仍不用捲動就看得到），另以 headless 截圖人工複審三個狀態（桌機 / 有系統分享 / 820px 窄畫面）。順手做的兩件事：`.sharecard__frame` 的高度預算 400 → 520px（多出來的那一排不會把「下載圖片」擠到摺線下面）、headless 的單次 CDP 上限改成 `PA_CDP_TIMEOUT`（預設 30s → 90s）—— 軟體渲染的機器一幀 200ms 時，「走過去撞它」那幾段本來就會被 30 秒誤判成失敗（這台機器上未改動的舊測試也會，屬既有環境問題）。**下一步建議**：（a）`SHARE_URL` 一旦部署就要換掉，並補一張 og-image ／ `og:*` meta，貼到 Facebook 時預覽才會長得對；（b）Instagram 與桌機 Messenger 現在只剩「複製 ＋ 自己貼」，若之後真要一鍵，唯一的路是行動裝置版（觸控搖桿與 720px 以下版面仍未做）；（c）可以記住「上次用哪一片石籤」把它排到最前面；（d）分享的那句話目前是固定句型，之後可依「這次最亮的那條技法」換一句，會更像戰績而不是模板。

- **2026-07-30 · Phase 25（世界的第五層互動：動得了的器物）**：依產品指示「環境再多設計一些小物件，讓主角可以跟它們互動，就像常見的 RPG 一樣」，先查了四份可實作的參考 —— Robert Yang 的《Open world level design: BotW》（三個明確分開的高度階、階與階之間的對比要守住）、Don Carson 的《Environmental Storytelling》（細節是有限預算，要留給你想把人吸過去的地方；塞滿細節反而更弱）、Roblox 的 Proximity Prompts 文件（提示要拆成「物件名 ＋ 動作動詞」兩格；**同時在範圍內的多個提示由鏡頭指向決定顯示哪一個，不是由距離決定**）、GDC 17 的《BotW: science and clever little lies》（整個世界只有兩類東西與三條規則，用「元素 × 材質」取代「所有東西 × 所有東西」）—— 據此加了第五層互動 `handle`（`src/world/handles.js` ＋ `src/data/handles.json` ＋ `src/ui/handle.js`）。**八種器物、22 件，五片土地都有**（撰寫基本功 6 ／ 其餘各 4，每區至少 3 種不同的）：**陶罐**（掀蓋 → 蓋子仰開、一縷光飄出 → 小窗看罐底的字，一次性）、**火盆**（點火 → 火起來而且**一直亮著**，存檔記得 —— 這是玩家在世界上留下的痕跡；之後可以撥火）、**響石**（敲 → 石盤震、光環擴散、2.6 秒尾巴的合成鑼音，**可以一直敲**）、**守望石**（觸碰 → 睜眼、把頭轉向**你還沒解開的那座石座** ＋ 地上一道很淡的指向 —— 指南針的實體版本）、**撈月池**（撈一把 → 一條光魚浮起、散成碎星）、**指路石**（閱讀 → 小窗列出四個方向各有什麼，跟指南針互補）、**絞盤**（**按三次 E**，每次咬一格、鼓真的轉 120°，第三次腳邊石蓋滑開、底下透出一道光；推到一半離開不算失敗，回到原地重來）、**長凳**（坐下 → 人真的移到凳面上、`character.js` 新增 rigless 坐姿（髖屈 85°／膝屈 89°／骨盆下沉 0.42，走一步權重自動歸零 → 不需要狀態機）、鏡頭拉到最遠、世界安靜一會兒；再按 E 或走一步就起身）。**互動文法**：`E` 仍是唯一的鍵（絞盤三下也是按三次 E，不發明「按住」），互動半徑 3.2 排在刻文小語（3.8）底下 → 搶 E 的順序變成「石座 > 石碑 > 刻文小語 > **器物** > 閘門」；八種動詞互不相同（掀開／點火／敲響／觸碰／撈一把／閱讀／推動／坐下）且動過之後換一句（再看一眼／撥一下火…）；**兩件同時在範圍內時改用「面向」排名**（`分數 = 距離 × (1 − 0.35 × 面向點積)`，取自 Roblox 的 camera-aim arbitration），沒有給面向時退回純距離。**只有陶罐與指路石開窗**，其餘六種只在畫面上說一句話 —— 走在路上的小互動不該把整個世界蓋掉。**護欄 2**：這一層跟世界觀石碑、藏起來的地方同一種東西 —— **純風味**，不教技巧、不掛 `techniqueId`、不放連結、資料層不准有 `source` / `teaches`（測試逐條驗證，連面板 DOM 裡都不准出現「神諭原典／官方／技巧」）。**存檔**新增 `handlesUsed`（純加法、`normalize()` 補預設、不寫 `bestGrades`、不算區域解鎖的通關數、重置清得乾淨），第一次動它給 4 XP、之後怎麼玩都不再給（可以一直敲鑼但不能刷分）；圖鑑的「走出來的收集」多一列「動過的器物 N / 22」。**音效**新增 11 支全合成（`lid` / `openSoft` / `ignite` / `ember` / `gong` / `touchstone` / `scoop` / `plank` / `ratchet` / `unseal` / `sit` / `stand`）。**效能**：三角形 133.4k → 142.7k（+9.3k，上限 420k）、**光源 +0**（44 盞，全部走自發光與加色光暈片）、網格 +214、幾何體與材質走模組快取（22 件各自獨立，遠低於 InstancedMesh 划算的 50 份門檻）、每幀迴圈零配置 ＋ 平方距離 ＋ 45／15 公尺分級。**擺法**：兩件器物之間硬性 14 公尺、離石座 7／刻文小語 8／反應物件 8／石碑 7／地標 14／橋的主動線 8 以上（用一支落點掃描器對每一區掃格點、依方位分扇形挑點，確保繞著區域一圈而不是擠在一邊），並納入 `keepClear` 讓碎石草叢不會把它們埋掉。**順手修掉兩個既有的真問題**：（a）齒輪工坊那 12 片躺平的大圓盤只用「圓心離地 5–11 公尺」定位，最大的幾片在斜坡上的**下緣**其實已經垂到地面 → 碰撞稽核判定為「有份量卻走得過去」（一個看得到的穿模點），改成用它自己的最低點算淨空（≥ 2.4 公尺）；（b）沿牆滑的測試挑的是 `solids[0]`，兩顆疊在一起的碎石會把人夾住 → 改成挑一顆**孤立**的石頭（要驗的是切線滑動，不是脫困）。**WORLD.md** 補上 §3.2 第五層與面向排名、§4.6 八種器物的完整規矩、**§4.7 三個高度階**（雜物 ≤ 2.6／中景 3–8／地標 21–27，中間要留得出空隙）、§5.1 `handle:<id>` 節點名、§5.3 `handles.json`、§5.4 `handlesUsed`、§6.1 更新後的預算與「飄空物件要用自己的最低點算淨空」、§7 四份新參考，以及 29 條維護檢查表的逐條過關紀錄。**驗證**：`npm run fonts` 重跑（語料 49 檔 / CJK 1611 字 / 1316 KB，指紋測試綠）；`npm run test:rubric` 14369 → **15415 個斷言全過**（新增：資料合法性與「零連結／零技巧宣稱」、八種動詞互不相同、每區 ≥ 3 種、落點與淨空逐點掃描、彼此 ≥ 14 公尺、**在 node 裡真的把器物場蓋起來**驗零光源／剪影 ≤ 2.6 公尺／面向排名／八種的 activate 行為（絞盤三下、響石敲不完、守望石真的轉到目標方向、長凳座位在凳面上、火盆點著就一直亮）、世界裡 22 件四周 20 個方向全部走得到、碰撞體預算、低畫質照樣蓋、存檔新欄位純加法、進程只給一次 XP 且不進圖鑑不算徽章不算通關不足以解鎖新區）；`npm run test:playtest` **212 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 1259 → **1364 項檢查全過、零 console error**（新增 105 項：五片土地都有且零光源、陶罐完整流程（遠處無提示 → 走近出現「名字 ＋ 狀態 ＋ E ＋ 動詞」→ E → 小窗兩行分段揭示 → 零連結零技巧宣稱 → XP ＋ 存檔 → 蓋子真的掀開 → 再掀不再給分且動詞換一句）、絞盤三下（前兩下不給 XP、提示直接寫出還要推幾下、全程不開窗、鼓真的轉一圈、石蓋滑開）、長凳（坐姿權重 ＋ 膝蓋真的彎、人在凳面上、鏡頭拉遠、提示換成「起身」、再按 E 起身、**走一步自己站起來**、鏡頭還原）、響石敲不完但只給一次 XP、守望石睜眼並轉到 `objectiveTarget` 的方向且畫面上說出看向哪裡、指路石四個方向且無橫向溢位、火盆點著後一直亮、**真的按下鍵盤的 E 也動得了器物**（純鍵盤鐵則）、圖鑑計數、22 件四周走得到、面板開著時按 E 不會去敲鑼、重整之後陶罐還開著／火盆還亮著／絞盤還是轉開的且不會還坐在凳子上）。**下一步建議**：（a）目前八種器物彼此獨立，可以做一條 BotW 式的統一規則（例如「提燈的光」當唯一的元素，讓每件器物宣告自己怎麼回應），之後新增種類就不用再寫一份互動邏輯；（b）火盆是唯一會「被玩家永久改變」的東西，這個手法可以推廣 —— 通關之後守門人手上的告示真的換成你刻的版本、拆到一半的機器被組好；（c）陶罐與撈月池目前只有文字與光，可以讓它們掉出一顆會滾的東西（世界目前沒有任何動態物理）；（d）行動裝置仍未做（觸控搖桿、720px 以下的版面），器物那一層的「走近按 E」在觸控上需要一顆實體的互動鍵。

- **2026-07-31 · Phase 26（課程總稽核：把已經被官方推翻的東西誠實標出來）**：依產品指示「comprehensive 的確認一次 prompt 的技巧，確保我們的關卡豐富度」，把 `docs/promptbooks/` 底下 9 份 2026-07-30 擷取的官方參考資料（Anthropic 102 條／OpenAI 139／Google 86／xAI 40，加上 Qwen／DeepSeek／Mistral／Meta-Llama／其他）逐條比對遊戲的 68 技巧、26 關、111 段刻印流程與 13 則刻文小語，產出 **`docs/promptbooks/gap-analysis.md`**（六節：過時／已反轉、未覆蓋的新技巧、關卡重複度診斷、路線圖、出處健檢、本階段做了什麼）。**稽核結果**：找到 **14 項過時或被縮限的內容**（A 級「照做會出錯」4 項：取樣參數三家各自否定且 `dial-room-43` 的滿分示範解答就是它、自我檢查在 Claude Opus 5 要刪掉而不是改寫、xAI 指南整頁 404、`role-04` 的官方立場已反轉；B 級「適用範圍縮小」10 項），**38 條值得新增的技巧候選**（5 分 15 條、4 分 15 條、3 分 8 條）與 **6 組現成的廠家反差素材**；同時逐一 curl 了 `curriculum.json` 引用的 **26 個網址**：21 個正常、**1 個硬 404**（`docs.x.ai/docs/guides/grok-code-prompt-engineering`，被 5 條技巧引用）、1 個官方自標即將移除（`ai.google.dev/gemini-api/docs/gemini-3`，被 4 條引用）、1 個舊路徑、1 個內容已搬走、1 個擋自動擷取。**兩個結構性發現**（比任何單條錯誤都嚴重）：（a）2026 年三家官方都轉向「最小必要結構」（GPT-5.6 精簡化讓分數 +10–15%／token −41–66%、Anthropic「最好的 prompt 不是最長的」、Gemini 3.x「冗長的 prompt 工程會讓它過度分析」），而遊戲的 S 評價正在獎勵相反行為；（b）遊戲主打中文圈，卻完全沒有 **Qwen** —— 唯一有中文母語 prompt 方法論的廠商（六要素框架、分隔符選字原則、「語言→語種」的歧義修正），而且 68 條裡**沒有任何一條教「怎麼讓模型用中文回答」**。**本階段的修法（最小外科手術，`curriculum.json` 逐字未改 —— 那些引文在它們的年代是正確的引用）**：新增第三層譯寫性質的資料 **`src/data/dated-notes.json`**（`authored: "game"`，檔頭明講不是官方文字、官方說法一律回 `curriculum.json`），內含 **4 條技巧時代註記**（`params-01` 取樣參數：Gemini 3.x 建議全部移除／Claude Sonnet 5 設了回 400／Mistral 說 0 也只是趨近確定，改用文字規則求一致；`params-02`：貪婪解碼不再通用，Qwen 思考模式明令禁止；`cot-04`：Claude Opus 5 本來就會自驗，官方建議刪掉驗證句；`decompose-02`：拆成多次呼叫已縮小到「要檢查中間結果或強制固定流程」）與 **2 條出處狀態註記**（xAI 404、Google deprecated，各附後繼參考），每一條都帶年月 ＋ 可點的 https 官方連結。**顯示層**：`content.js` 的 `createContent()` 收第 7 個參數、`displayTechnique()` 多回一個 `dated`、新增 `datedNote(id)` / `sourceNote(url)`（沒有這一層時安靜降級）；`dom.js` 新增共用的 `datedNoteHtml()` / `sourceNoteHtml()`；**圖鑑、主控台第二幕（神諭刻文）、刻文小語**三處都會在原文下面補一行小、冷色、帶 `※` 的註腳，官方出處清單旁邊則標出「此文件已下架（2026-07 查核）。後繼參考：… ↗」——**原網址永遠留在畫面上，只是旁邊多一句實話**（護欄 2）；`styles.css` 新增 `.datednote` / `.srcnote`。`challenges.json` 只動了兩處 `clue`（`dial-room-43` 補「取樣旋鈕要看模型、一次只轉一個」、`effort-forge-15` 補「自我驗算在 Opus 5 要刪掉」），**評分、示範解答、刻印流程、rubric 一條未改**。**驗證**：`npm run fonts` 重跑（語料 50 檔 / Latin 246 · CJK 1612 · Display 104 / 1317.2 KB，指紋測試綠）；`npm run test:rubric` 15415 → **15504 個斷言全過**（新增時代註記層：`authored` 與免責說明、查核年月格式、每條掛在真實技巧 id 上且不重複、文字是中文／看得到日期／無整句英文／連結不埋在文字裡、至少一個 https 官方出處、下架註記的網址真的被 `curriculum` 引用到且後繼參考不是原網址、**原網址仍留在 `curriculum.json` 沒被改掉**、`displayTechnique().dated` 與 `datedNote()` / `sourceNote()` 真的接得出來、活著的出處不會被誤標、沒有這一層時安靜降級）；`npm run test:playtest` **212 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` **1364 項檢查全過、零 console error**。**下一步建議**：（a）Phase 27 誠實度收尾 —— `role-04` 重寫、5 條 xAI 出處置換（`iterate-05` 可直接換成快取最佳實務頁、內容更強）、4 條技巧改指非 deprecated 的 Google 頁、Anthropic 那 14 條補 anchor（現在全指向同一個沒有錨點的合併頁）；（b）Phase 28 中文圈補課 —— 新增「說話的規矩」主題（回答語言跟隨提問語言、六要素、分隔符選字、語種歧義、沒有 system 欄位時怎麼辦），並把 `qwen` / `deepseek` 加進 vendors（徽章的隱藏成就條件要維持四廠，否則違反護欄 7）；（c）Phase 29 減法與工具 —— 處理「S 評價在獎勵把每一項都塞進去」的評分失衡，並把 `assignsTask`（26/26 全中）降權成地基、把 `specifiesFormat`（14/26）與 `hasDelimiters`（12/26）從 6 關與 4 關換成該關真正的主題（前三名合計占比 49% → 約 33%），工具與代理則需要新的題型（勾選該不該呼叫工具、把矛盾規則排成決策樹）；（d）Phase 30「分歧之廳」—— 六組廠家反差做成「先發模型卡、再出題」，同一個素材換一張卡正解就翻面。
- **2026-07-31 · Phase 27（除了 A B C 選擇題，再加兩種真的要動手的題型）**：依產品指示「也可以設計一些互動題，讓使用者去拖拉東西、互動操作，在過程中學會 prompt 技巧以及 LLM 使用的概念，像是 tool use、function calling」。**資料層**：`flows.json` 新增 `kind`（`choice` / `order` / `workshop`，**沒寫就是 `choice`** —— 其餘 24 關一個位元組都沒動、行為零變化），三種題型都留著原本的選擇題流程當後備資料；三者「做對」組出來的那段文字都**等於該關的 `sample`**、都走同一支離線 rubric 引擎、都以同一隻手掌印收尾（新增 `src/prompt/palm.js` 把按住 600ms 的儀式抽成共用零件，仍然用計時器判定、不看影格）。**① 排序刻印**（`src/prompt/order.js`）：石版已經刻好了，玩家要把它們**排順**——這是選擇題教不了的東西（次序本身就是官方文件講的技巧）。滑鼠拖得動（pointer 事件 ＋ FLIP 位移），純鍵盤也走得完（`Enter` 拿起 → `↑` `↓` 搬 → `Enter` 放下；沒拿東西時 `↑` `↓` 只移動焦點、`Home` / `End` 搬到頭尾、拿著時 `Esc` 是放回原地而不是關面板），每一次拿起 / 搬動 / 放下都用 `aria-live` 講出來；石版節點**只建一次、之後只搬不重建**（用 innerHTML 重建會讓拿在手上的那一片在拿起的瞬間掉焦點，後面的方向鍵就全部落空）。**排錯不會失敗**：排對的那一片立刻標上暖金的「位置對了」刻記，**排順了手掌印才浮出來** —— 「送出之後才發現排錯」這件事在設計上不存在。套用在兩關次序就是課程的關卡：**長卷之塔**（資料在前、問題在後）與**優先序階梯**（規則區放最上面）。**② 神諭工坊**（`src/prompt/workshop.js`）＋**新的第 27 關 `oracle-workshop-36`**（齒輪工坊，`npc` 管事·派工，教 `agentic-01` / `agentic-02` / `decompose-02` / `grounding-03`，出處 OpenAI · GPT-4.1 prompting guide）：旅人的委託是「明天湖邊會不會下雨？順便把答案寄給燈塔守」，玩家當派工人走四步 —— **挑工具牌**（三張裡兩張用得到，挑到「查帳」會就地長出一句解釋）→ **把值石拖進參數格**（六顆值石、四個格子；放錯只告訴你這一格要的是什麼，值石回托盤）→ **排出兩通呼叫的相依順序**（第三步直接沿用排序刻印）→ **立一條規矩**（「參數找不到就反問我，不要自己編一個」）。四步分別教「工具是**宣告**出來的：名字 ＋ 說明 ＋ 每個參數的型別與用途」「**參數不是用求的，是用填的**，而且只能來自委託裡真的講過的東西」「呼叫有先後，前一步的結果才是下一步的材料」「缺參數要反問，不要幻覺」。**世界**：新石座落在 `[-115, 66]`（離最近的石座 20.9 公尺，避開所有小景 / 地標 / 石碑 / 刻文小語 / 器物 / 祕密的淨空圈，24 個方向 × 2–6 公尺高低兩種畫質都走得到），石座 26 → 27、齒輪工坊 5 → 6 關，**沒有新增任何存檔欄位**（舊存檔完全不受影響）。**其他**：設定頁的答題方式說明改寫成三種題型都講得通的說法、操作一覽補上「拿起一片石版或值石」與「拿著東西時方向鍵是搬它」兩行、README 與 WORLD.md 同步（WORLD.md 新增 §3.3b「三種答題方式」的作答文法 ＋ 維護檢查表的 Phase 27 逐條結果）。**驗證**：`npm run fonts` 重跑（語料 53 檔 / CJK 1615 字 / 1319.8 KB，指紋測試綠）；`npm run test:rubric` 由 15504 擴到 **16098 個斷言全過**（新增 `flowKind()` 的預設與退化路徑、三種題型的關卡分佈、排序資料的 `pieces`／`order` 必須是同一組的排列、**初始排法不等於正解且沒有任何一片剛好站對**、排對＝S 且每條檢查滿分、排好的字＝`sample`；工坊的四步問句、工具規格三欄俱全、參數的型別與放錯教學、托盤一定有多餘的值石、規矩剛好一條對、錯的選項都有 ≥12 字白話中文回饋且不自帶連結、派工完成＝`sample` 且每條檢查滿分、只挑完工具還不滿分）；`npm run test:playtest` 由 212 擴到 **226 個斷言全過**；`npm run build` 通過；`npm run test:e2e` 由 1364 擴到 **1530 項檢查全過、零 console error**（新增 166 項：三種題型的分佈與 24 關的後備資料、排序刻印**純鍵盤走完一圈**（拿起 → 搬 → 放下 → `aria-live` → 對照亮燈 → 手掌印真的按住 900ms → S ＋ XP ＋ 出處）、**CDP 真滑鼠拖曳**把「角色與目標」搬到最上面並立刻標成位置對了、排錯時手掌印不出現／第四幕按不下去／不跳失敗面板／不扣分、神諭工坊四步純鍵盤走完（挑錯工具 / 放錯值石 / 立錯規矩全部只是就地教學）、派工單組出來的字＝`sample`、第一幕仍然零官方連結而第二幕四條刻文都掛「神諭原典：〈文件名〉↗」、新石座在場景圖上且四周走得到、切到自由書寫再切回來、24 關的石碑刻印一切照舊、820px 下兩種新題型都無水平溢位）；另外修掉 headless 驗證**中斷時會把已收集到的失敗訊息一起吃掉**的問題（現在中斷也會把前面的失敗印出來）。**下一步建議**：（a）工坊目前只有一關，`docs/promptbooks/gap-analysis.md` §2 的 N-16～N-20（工具說明該寫多長、結構化輸出、檢索預算、動作預算、授權邊界）可以直接複用同一個面板，換一份 `workshop` 資料就是一關；（b）排序刻印可以再做「兩種排法都過關但評價不同」的取捨題，把「權衡」也變成可練習的東西；（c）行動裝置仍未做 —— 拖曳走的是 pointer 事件所以觸控本來就動得了，但 720px 以下的工具牌 / 參數格 / 托盤版面還沒調；（d）Phase 10 留下的建議仍在：`assignsTask` 的遮蔽邏輯還沒套到 `positiveFraming` 與 `keepsPromptLean`。
- **2026-07-31 · Phase 28（分享修正：送出去的是「那張圖 ＋ 一段話」，不是一個連結）**：依產品回饋「分享的東西錯了，我想要的是分享使用者完成任務的那張圖，然後配上一段話」，把 Phase 24 的分享整個收斂回這兩樣東西。**拿掉的**：那排「分享到 Facebook / Threads / Messenger / Instagram」的網頁入口 —— 它們**只收得下文字和一個連結、收不下圖片**，收到的人看到的是一個程式碼倉庫的連結而不是玩家剛刻出來的那張卡，那正是這次要修的 bug（`platformIntent` / `SHARE_TARGETS` / `isMobileLike` / `shareTitle` / `shareBody` 五個匯出、`rovingList` 的方向鍵排、`.sharecard__chip*` / `__send*` / `__caption` / `__note` 的樣式全數移除，Messenger 與 Instagram 的特例跟著一起走）；預設那段話裡的 `SHARE_URL` 也拿掉（常數留著給之後部署改，但**不再進入玩家貼出去的字**）。**留下並強化的**：`shareCaption()` ＝ 世界的說法 ＋ 品牌落款（「我在 PromptArcade 通過了『清晰之門』，評價 S —— 現在的稱號是『釋義者』（Lv.6），已把 46 / 68 條技法刻進圖鑑。／ Learn Prompt Engineering by Playing」，四種卡各一句），開卡的第一幀就同步填進一個**真的能改的輸入框**（玩家自己打的字走 `--font-input` 系統字型；改過就不會被重畫蓋掉，換一張卡才回到預設）。**主路**「分享圖＋文」＝ `navigator.share({ files: [那張 PNG], text: 框裡當下的字 })` —— 按下去的那一刻**同步**讀框（前面仍然一行 `await` 都沒有，`userActivation.isActive` 實測手勢沒斷），**不帶 `url`、不帶 `title`**，圖與話一起交給系統分享面板上的 FB / IG / Threads / 訊息。**沒有系統分享時**主角換成「複製圖＋文」（同一份 `ClipboardItem` 裡有 `image/png` 與玩家改過的 `text/plain`）＋「下載圖片」保底，畫面只留一句白話：「開啟 Facebook / Instagram / Threads 直接貼上 —— 圖和文字會一起送出。」**版面**：分享卡改成「圖在左、那段話與按鈕在右」的兩欄（≥900px），預覽圖從 286px 寬長到 421px，四顆石頭與說明全部還在摺線上面；窄畫面仍是上下疊，820px 下無水平溢位。鍵盤回到最單純的 <kbd>Tab</kbd> / <kbd>Enter</kbd>（鍵帽跟著換），焦點落在當下的主角上。WORLD.md 新增 §3.5b 把規則寫死：**分享 ＝ 圖 ＋ 一段話、那段話裡不准有網址、不做任何帶不走圖的網頁入口**。**驗證**：`npm run fonts` 重跑（語料 53 檔 / CJK 1616 字 / 1319.6 KB，指紋測試綠）；`npm run test:rubric` 16098 → **16106 個斷言全過**（Phase 24 那一節整段改寫：四種卡的那段話**一律不得出現 http／github**、被拿掉的匯出與字串（`facebook.com/sharer`、`threads.net/intent`、`fb-messenger://`、`data-chip`、`rovingList`、`sharecard__chip`）逐一斷言不存在、`navigator.share` 的參數就是 `{ files: [lastFile], text }` 且不含 `url:` / `title:`、那段話從框裡同步讀、剪貼簿帶的是玩家改過的字、輸入框的 label / `aria-describedby` / 系統字型、說明不再提「連結」）；`npm run test:playtest` **226 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` **1521 項通過、零 console error**（分享那一節重寫成 74 項：石籤與「分享到」整排不存在、分享卡上沒有任何會開新頁的連結、整張卡的文字裡找不到網址或 github、那段話預設就寫好且不帶連結、用 `Input.insertText` 真的打字進去 → 複製出去與交給系統分享的都是**改過的版本**、`Object.keys(data)` 只有 `files,text`、換一張卡回到預設、沒有系統分享時「複製圖＋文」是主角且焦點落在它身上、820px 無水平溢位），另以 headless 截圖人工複審三個狀態（桌機兩欄 / 有系統分享 / 820px 窄畫面）。**已知（與本次無關）**：`排序刻印與神諭工坊（Phase 27）` 的兩項**滑鼠拖曳**斷言在這台機器上（軟體渲染、每幀約 200ms）失敗；把 Phase 28 這一整節從 e2e 拿掉再跑一次控制組，同樣兩項照樣失敗（1447 通過），而單獨重現那段拖曳三次全過 —— 屬既有環境問題，不是這次改動造成的。**下一步建議**：（a）`SHARE_URL` 部署後仍要換掉並補 `og:*` meta；（b）那段話可以依「這次最亮的那條技法」換句型，會更像戰績而不是模板；（c）行動裝置仍未做（觸控搖桿、720px 以下的分享卡版面）；（d）Phase 10 留下的 `assignsTask` 遮蔽邏輯還沒套到 `positiveFraming` / `keepsPromptLean`。

- **2026-07-31 · Phase 29（橋上的門會問你 ＋ 遊戲改名 Promptasy）**：兩件事。**① 詢問式閘門（可先行前往）**：四條橋中央的門本來是硬擋，已經懂這些東西的人只能被關在原地。現在門會**問一句** —— 走到門前（自動詢問半徑 7.5 公尺）或按 `E`，都會開一個「夜間檔案館」語言的小對話框：「這道門的考驗還沒完成（還差：Lv.3（目前 Lv.1）＋ 撰寫基本功 通關 4 關（目前 0））。想先過去看看嗎？前方的試煉不會因此變簡單。」兩個選擇：**直接前往**（走原本那支 `world.openGate(id, true)`，同樣的屏障淡出 ＋ 擴散光環，那一區立刻走得進去）／**先留下修行**（門留著；走遠離開互動半徑再回來才會重新問一次，站在門口不會被連問；`Esc`／點背景等同「先留下」，絕不偷偷開門）。新增 `src/ui/gate.js`（沿用 `createOverlay` 的焦點鎖 / `Esc` / `aria`，預設焦點在「先留下修行」—— 誤按 `Enter` 不會就這樣越過一整區；不放任何官方連結，護欄 2）。**記帳一律誠實**：存檔新增純加法欄位 `skippedGates[]`，`skipGate()` 只往 `unlockedRegions` 加一區並留下記號 ——不給 XP、不收技巧、不動徽章、不寫任何一關的 `bestGrades`，所以「已通關 x / 27」「已收集 x / 68」「四廠徽章」「等級」一個數字都不變；之後真的把條件補滿時 `refreshUnlocks()` 也不會再把它算成「新解鎖」（它已經在 `unlockedRegions` 裡），**不會慶祝兩次**；`gateStatus()` 新增 `skipped` / `needs[]`，門上的字改成「需要 … · 也可以先行前往」，開了之後改成「已開啟 · 你先行前往」，設定頁多一行「其中 N 道門是你先行前往的（門開了，關還在那裡等你）」。正常解鎖路徑一個字未改（達標仍是自動開門 ＋ 慶祝）。HUD 互動提示改成「`E` 問問這道門」、操作一覽的 `E` 補上「橋上的門」、README 按鍵表與 WORLD.md §1.4 同步；`player` 新增唯讀的 `inputEnabled`。**② 全面改名 PromptArcade → Promptasy（prompt ＋ fantasy）**：品牌字串一次掃完 —— 標題卡（分字揭示由 12 → **9** 個 `<span>`，`Prompt` 白 ＋ `asy` 主色，定位句 `Learn Prompt Engineering by Playing` 原樣保留）、`index.html` 的 `<title>` / `og:title`、`package.json` 的 `name`、分享結果卡的落款與四種說法與檔名與 `SHARE_URL`、圖鑑／設定／成就／稱號／各資料檔的「這是遊戲自撰」註記、全部檔頭註解、README / WORLD.md 標題、CLAUDE.md 的品牌行（**歷史變更紀錄一字未改** —— 它們在當年就叫 PromptArcade）。**存檔搬家（關鍵）**：key 命名空間 `promptarcade.v1.*` → `promptasy.v1.*`，`load()` 在「新 key 沒東西、舊 key 有」時把舊存檔搬過來並立刻寫進新 key，**舊 key 刻意留著**（幾 KB 的代價換一條退版的路）；兩個都在時**新的優先**；舊 key 壞掉也不會讓遊戲開不起來；`reset()` 兩個 key 一起清（不然重整又被搬回來）。`window.__promptarcade` 保留成 `window.__promptasy` 的別名（外面的書籤小工具與腳本不會壞），e2e 全面改用新名字。順手修掉標題卡上寫死的「26 個關卡」（Phase 27 之後其實是 27 關）。字型檔名 `arcade-*.woff2` 與字族名 `Arcade Serif TC` 等**刻意不改**（純內部別名，玩家看不到，改動風險大於收益）；`docs/promptbooks/*.md` 是當時擷取的研究稿，依指示不動。**驗證**：`npm run fonts` 重跑（語料 54 檔 / CJK 1616 字 / 1319.6 KB，指紋測試綠）；`npm run test:rubric` 16106 → **16212 個斷言全過**（新增：`skippedGates` 的新舊存檔相容與去重、`skipGate()` 的開門／重複按／亂傳參數／跨重整、**先行前往不動 XP／圖鑑／徽章／通關數／等級**、補滿條件時不重複慶祝、重置清空、對話框文案與「不放連結」、存檔搬家的四種情境（全新／只有舊 key／兩個都在以新為準／舊 key 壞掉）＋ 重置清兩個 key、品牌字串掃描與 9 字元品牌名）；`npm run test:playtest` **226 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 1521 → **1585 項通過、零 console error**（新增 64 項：走進閘門對話框自己出現（沿橋推進，非鏡頭方向）／講出還差什麼與「不會因此變簡單」／零官方連結／焦點預設在「先留下修行」→ 按下去門仍然關著且那一區仍走不進去、站在門口不被連問 → 按 `E` 也問得出來 → `Shift+Tab`（改用 `rawKeyDown` 的原生 Tab）走到「直接前往」→ `Enter` 開門 →那一區走得進去、`skippedGates` 寫進 localStorage、XP／通關數／圖鑑／徽章／等級一個都沒變、操控權還回來 → 門開了之後走過去與按 `E` 都不再問 → 重整後仍開著仍不問 → 設定頁誠實列出；`document.title` / `og:title` / 畫面上找不到舊品牌名 / `__promptasy` 與舊別名、種一份只有舊 key 的存檔重整後 XP／等級／評價／圖鑑／石碑／設定／答題方式／旗標全數還在且新 key 已寫入、舊 key 留著、重置兩個都清）。**已知（與本次無關）**：Phase 27 排序刻印的兩項**滑鼠拖曳**斷言在這台機器上（軟體渲染、每幀約 200 ms）仍然失敗，連跑兩次結果完全一致 —— 屬 Phase 28 已記錄的既有環境問題。**下一步建議**：（a）先行前往目前只留在設定頁一行字，可以在圖鑑或結果卡上做一個「這片土地你是走進來的，還是考進來的」的溫和標記，讓誠實記帳也變成收集的一部分；（b）先行前往的人會直接撞上難度斷層，可考慮在該區第一座石座旁放一句回聲的提醒（「這裡的題目預設你已經會前一片土地的東西」）；（c）改名後 `SHARE_URL` 指向 `romanticamaj/promptasy`，orchestrator 改完 repo 名要確認連結通；og-image 與 `og:*` 仍未補；（d）行動裝置仍未做（觸控搖桿、720px 以下的版面），Phase 10 留下的 `assignsTask` 遮蔽邏輯也還沒套到 `positiveFraming` / `keepsPromptLean`。
- **2026-07-31 · Phase 30（站長自己的音樂與音效上線：合成引擎退居備援）**：`public/audio/` 進了站長自己做的**五首分區配樂**（Suno 原創授權，各約 3 分鐘、響度對齊 −20 LUFS、AAC m4a）與 **10 支音效**（Splice 樣本授權至站長帳號，峰值 −6 dBFS），`src/audio/audio.js` 從「全合成」改寫成「**音檔優先、合成備援**」的雙軌架構 —— **Phase 4 的生成式引擎一行都沒拆**，它現在的角色是「檔案還在路上 / 抓不到 / 解不開 / 被關掉」時頂上來的那一層：把 `public/audio/` 清空，遊戲照樣有聲音、照樣能玩（護欄 3、7）。**配樂**：`fetch → decodeAudioData → AudioBufferSourceNode`，因為素材是「修過頭尾但沒做成完美 loop」的曲子，所以**不用 `loop = true`**（那會有接縫），改成**兩個 source 交替、等功率交叉淡入**：每一段在 `duration − 2.5s` 排下一段，前後各 2.5 秒 sin/cos 淡入淡出（等功率曲線用 24 段 linear ramp 疊出來，不用 `setValueCurveAtTime` —— 它禁止與其他自動化重疊，跨區時「淡出還沒跑完又要淡入」會直接丟例外）；跨區沿用既有的 `setRegion()` 呼叫點，改成約 3 秒的等功率交叉淡入。**載入**（護欄 5）：一律等到**標題卡按下去的那個使用者手勢**才開始抓（e2e 斷言手勢之前 `.m4a` 請求數 = 0），當區配樂與音效優先、鄰區配樂低優先排隊（最多兩條並行）；**解碼只在要播的時候做、同時最多留兩首**（3 分鐘立體聲解碼後約 69 MB，五首全解會吃掉 345 MB），其餘只留壓縮位元組，抓到一半玩家已經走掉就不解碼；檔案沒到的那段時間**當區的合成 pad 直接頂著**，解好再從合成漂到音檔上 —— 所以任何情況下都不會出現一段無聲。**節點圖**：`destination ← compressor ← master ← musicDuck ← [音檔 bus ＋ 合成 pad(dry+reverb)]`、`master ← sfxBus`，音量滑桿與靜音**同時管住兩條路**（共用 master），Phase 22 的 `DynamicsCompressorNode` 仍在最後一級；新增的 `musicDuck` 在過關的 9 秒頌缽響起時把配樂壓 3 dB。**音效對照**（沒有檔案的一律保留合成：spark / stamp 的悶響 / reject / toast / 腳步 / 全部世界反應音 —— 它們要隨機變音高，音檔做不到）：過關 → `sfx_pass`（依 S/A/B/C 微調力度 1 / .92 / .84 / .78 並 duck 配樂）、真的解鎖 → `sfx_unlock_shimmer` ＋ 慢 0.28 秒進來的 `sfx_unlock_door` 兩層、**先行前往（Phase 29 的跳門）→ 只有 `sfx_unlock_door`**（新 cue `gateOpen`：門開了，但沒有慶祝的微光）、刻印牌按下去 → `sfx_click`（節流 70ms，新 cue `click`，經 `stele.js` 新增的 `onTap` 送出，不是每個按鍵都響）、石碑收下一段 → `sfx_select`、呈給神諭 → `sfx_submit`、圖鑑與面板打開 → `sfx_page`、絞盤 → `sfx_gear`、**起始祭壇的門檻與刻文小語 → `sfx_shrine`**（刻文小語不再疊一聲通用翻頁）、68 全收集的隱藏成就 → `sfx_finale`。**授權**（護欄 6）：`public/LICENSE.md` 逐檔登記 —— BGM「站長以 Suno 創作之原創授權（本專案使用）」、SFX「Splice sample license（授權至站長帳號，本專案已釐清可用）」，並註明音檔合計約 15 MB、手勢後才下載、清空也不會壞。**新的除錯面**：`audio.debug()`（節點鏈 / 每區的 fetch 與解碼狀態 / 是音檔還是合成 / gain / 段數 / 已解碼首數 / 失敗清單）與 `audio.useFiles(false)`（＝把 `public/audio/` 當成不存在的手動退路，也是測試用的開關），測試一律驗節點圖與狀態、不依賴「聽得到」。**驗證**：`npm run fonts` 重跑（語料 54 檔 / CJK 1626 字 / 1326.6 KB，指紋測試綠）；`npm run test:rubric` 16212 → **16360 個斷言全過**（新增：15 個音檔真的在 `public/audio/`、命名與 manifest 一致、大小合理且總量在 20 MB 預算內、每一支有音檔的 cue 都還留著合成備援、解鎖兩層 / 先行前往單層 / 過關 duck / 按鍵節流、評價力度曲線、循環與跨區秒數、鄰區表都是真的區域、`files: false` 時每一支新音效照樣有聲音）；`npm run test:playtest` **226 個斷言全過**（未動）；`npm run build` 通過且 `dist/audio/` 15 個檔案都在（Vite 的 `public/` 原樣搬運）；`npm run test:e2e` 1585 → **1622 項通過、全程零 console error**（Phase 4 的「五區配樂互異」那一節改寫成雙軌驗證：手勢前零 `.m4a` 請求 → 手勢後才開始抓 → 節點鏈上 master / compressor / duck / bgmBus / sfxBus 都在 → 頁面自己解一次 `sfx_click` 判斷這台機器支不支援 AAC，**支援就強制要求 buffer 那條路真的接上**（`source = 'file'`、有 segment 在跑、`loopSeconds > 60`、合成 pad 退場、同時解碼 ≤ 2 首）並驗跨區交叉淡入（新區 gain 起來、舊區 gain 下去）；不支援就驗合成頂上 → 再用 `useFiles(false)` 走一次備援（配樂換回合成、沒有 buffer 還在播、九支 cue 一支都不啞）→ 切回音檔）。**已知（與本次無關）**：Phase 27 排序刻印的兩項**滑鼠拖曳**斷言在這台機器上（SwiftShader 軟體渲染、每幀約 205 ms）仍然失敗，與 Phase 28 / 29 記錄的完全同兩項。**下一步建議**：（a）設定頁可以把「正在播的曲名」顯示出來（`BGM_TRACKS[].title` 已經有了），順便給一個「只用合成配樂」的省流量開關（`useFiles()` 已經是現成的）；（b）foundations 是樞紐，預抓四個鄰區＝背景下載約 11.7 MB，之後可以改成看玩家走向哪座橋再抓；（c）齒輪工坊的浮空齒輪仍然沒有聲音（`sfx_gear` 目前只掛在絞盤上），可以讓走近的齒輪也低聲轉一下；（d）行動裝置仍未做（觸控搖桿、720px 以下的版面），Phase 10 留下的 `assignsTask` 遮蔽邏輯也還沒套到 `positiveFraming` / `keepsPromptLean`。

- **2026-07-31 · Phase 31（平台入口回來了，但這次圖一定跟著走）**：依產品指示「研究各站怎麼讓使用者一鍵開 FB / IG / Threads，把圖與文用複製貼上帶過去」，先把三家真正收得下什麼查清楚再動手。**研究結論**：**Threads** 有官方的網頁撰寫入口（`developers.facebook.com/docs/threads/threads-web-intents`，收 `text` / `url` / `tag` / `reply_control`，**沒有任何參數帶得動圖片**），實測 `threads.net/intent/post` 301 轉到 `threads.com/intent/post`（參數原封不動，未登入先到登入頁再自己回來），而 Threads 官方帳號 2023-11-02 的「Web Edition」明講撰寫框**收貼上與拖放的媒體** → 文字走網址、圖走剪貼簿；**Facebook** 沒有任何帶得動內容的撰寫入口（`sharer.php` 連文件都沒有、只吃 `u`，`quote` 自 2017 起失效，且 Meta 的 prefill 政策本來就禁止代填使用者貼文），圖片只能靠被分享網址的 `og:image` —— 走它等於把成果換成一個連結 → 只開首頁、圖走剪貼簿、文字另給一顆；**Instagram** 網頁版「建立」只選得了檔案或拖放、**沒有任何網頁分享入口**（官方文件只寫 Android intent / iOS universal link），`/create/select/` 這種深連結實測伺服器根本不認（落回一般首頁殼）→ 先下載再開首頁；**剪貼簿**方面 W3C 規格**沒有定義優先順序**（WebKit 與 Chrome 都明說由接收端自己挑，實務上撰寫框幾乎都是「有圖就貼圖」），所以「一次貼上就圖文都到」是猜的、不是真的。**做出來的東西**：`sharecard.js` 新增 `SHARE_TARGETS` ＋ `platformOpenUrl()`，那一排（安靜的刻印牌石籤）回到分享卡上、排在系統分享 / 複製圖＋文 / 下載圖片底下，但每一顆都是**「先把圖備好 → 同一個手勢裡開那一頁 → 提示明講接下來要按的那一下」**：Threads＝文字用 `threads.com/intent/post?text=` 直接帶進撰寫框、剪貼簿**只放圖**（那一下 Ctrl+V 不會變成貼出一段字）；Facebook＝只開首頁、圖走剪貼簿、文字靠旁邊那顆「複製文案」；Instagram＝**先把 PNG 下載下來**再開首頁，提示直說「按『建立』選剛剛那張圖」，**不假裝它貼得上**。全部走 `window.open(url, '_blank', 'noopener,noreferrer')`，開新頁之前一個 `await` 都沒有（手勢不斷、不會被當成彈出視窗擋掉）；補上 `ClipboardItem.supports('image/png')` 偵測與「複製不了圖就改走下載」的退路（一樣帶得走圖，不留死路）；方向鍵走得完那一排、`Enter` 按得下去、每顆都有給螢幕閱讀器的說明。**護欄未動**：那段話裡仍然沒有網址、卡片仍然是本機 canvas 畫的、零 SDK、零外部請求、零第三方網域。**WORLD.md §3.5b 改寫**：規則從「不做任何平台入口」演進成「**平台入口可以存在，前提是它一定帶得走那張圖**（剪貼簿或下載）；只送得出連結的入口永遠不准做」，並補上「一次貼上只帶得走一種東西」與三家實測對照表。**驗證**：`npm run fonts` 重跑（語料 54 檔 / Latin 247 · CJK 1627 · Display 104 / 1327.2 KB，指紋測試綠）；`npm run test:rubric` 16369 → **16429 個斷言全過**（新增：那一排的資料完整性（每顆都講得出圖怎麼跟過去、話怎麼跟過去）、**每一顆都一定帶得走圖**、貼上那條路剪貼簿只放圖、帶不進文字的一定指得出「複製文案」、三個網址逐一比對且都不夾帶我們自己的連結、開新頁之前沒有 `await`、「只複製圖」裡沒有偷塞文字、`noopener`）；`npm run test:playtest` **226 個斷言全過**（未動）；`npm run build` 通過；`npm run test:e2e` 1622 → **1697 項、1694 通過、零 console error**（新增 94 項：那一排四顆都在且是按鈕不是連結 → 按 Threads 真的開了官方撰寫入口且玩家改過的那段話被帶進網址、剪貼簿裡**只有** `image/png`、提示寫出 Ctrl+V、開新頁時 `userActivation.isActive` 仍是 true → 按 Facebook 開首頁、不走 `sharer`、提示指得出「複製文案」 → 按 Instagram **真的觸發下載**（檔名 `promptasy-*.png`、內容是那張 PNG）、不寫剪貼簿、提示講得出「建立」 → 「複製文案」只寫 `text/plain` 且是玩家改過的版本 → 再改一次那段話網址跟著改 → 方向鍵在四顆之間走、`Enter` 按得下焦點上那一顆 → 820px 下四顆都量得到且都在面板寬度內）。**順手修掉兩件不是這次造成的事**：標題卡開場曲那次提交只更新了 `test-rubric`，`headless-check` 還留著「手勢之前零音檔、零 AudioContext」「五區各有一首配樂」這五條已經過時的斷言（開場曲**刻意**在標題卡就響起、配樂表也多了一首），以及它必然會念的一句自動播放提示 —— 三處都依現在的設計改正。**已知**：連跑兩次完整 e2e，各有 3 項失敗但**兩次的清單完全不同**（第一次是 Phase 27 的滑鼠拖曳兩項，第二次是風鈴擺動 / 火盆亮度 / 鏡頭拉遠），全部屬 AGENTS.md 記錄的「軟體渲染下動畫時序類」偶發失敗；Phase 31 那 94 項兩次都全過。**下一步建議**：（a）那一排目前是「先備好圖再開頁」，玩家回來補文字要自己按第二顆 —— 可以在開頁之後於分享卡上把「複製文案」暫時升成主角（視覺上接上第二步），讓兩步變成一條看得見的動線；（b）`SHARE_URL` 部署後仍要換掉並補 `og:*` 與 og-image，補完之後 Facebook 那條路才有第二種選擇（分享網址讓它自己抓預覽圖）；（c）Threads 的 intent 還收 `url` 與 `tag`，之後若真的有官網，可以讓玩家選「要不要附上網址」；（d）行動裝置仍未做（觸控搖桿、720px 以下的分享卡與那一排版面）。

- **2026-07-31 · Phase 32（開源門面：真實截圖 ＋ 高星等格式的 README）**：為了 repo 轉公開，先用 `scripts/headless-check.mjs` 同一套 CDP 做法（自己的 port 5197 / 9337，全程沒碰使用者的 5175）在 1600×900 下實機取景，種一份「Lv.6 / 收集 26 條 / 八關有評價」的存檔後拍下六張真實遊玩畫面，經 ffmpeg 256 色調色盤壓到每張 162–292 KB 收進 `docs/media/`：`hero.png`（夜色高原上的提燈旅人、星空、兩座關卡光柱與遠方的斷環地標）、`stele.png`（第三幕石碑刻印，已刻三段並亮起即時預檢與出處）、`guidance.png`（第二幕神諭刻文與它掛回去的官方原典連結）、`codex.png`（圖鑑 26 / 68 ＋ 展開的技巧與 Anthropic 官方連結）、`workshop.png`（神諭工坊派工題）、`title.png`（標題卡）。**README 全面重寫成國際 GitHub 讀者看得懂的格式**：置中標題 ＋ 標語 ＋ 八枚**誠實的 shields.io 靜態徽章**（three.js r170 / Vite 6 / 100% 離線評分 / 無後端 / 68 條技巧皆附出處 / 遊戲語言繁中 / License TBD / PRs welcome —— **沒有偽造任何 CI 或覆蓋率徽章**，因為目前根本沒有 CI）＋ hero 圖，接著是英文為主的 What is this / 截圖藝廊 / 特色 / 快速開始 / 中英雙欄操作表 / 離線評分怎麼運作（附 `gate-of-clarity-01` 真實 rubric 片段）/ 專案結構 / 測試表（16,429 ＋ 226 ＋ 1,697 項，含 3 條已知 flaky 的誠實註記）/ 內容與出處（curriculum 逐字保留、`authored: "game"` 分層、時代註記與 `docs/promptbooks/`）/ 技術棧 / 路線圖（行動裝置與英文介面尚未做）/ 貢獻指南 / 致謝（配樂 Gary Hsieh 由 SUNO.ai 輔助、音效 Splice 授權、六套 OFL 字型逐一列名）/ 授權，最後接一段完整的**繁體中文**區塊。**授權誠實化**：repo 裡沒有 `LICENSE` 檔（`public/LICENSE.md` 是資產授權不是程式授權），所以 README 寫成「**Code license: TBD** —— `package.json` 目前宣告 MIT 但尚未提交 LICENSE 檔，在那之前請視為尚未授權」，並明講音樂／音效不可轉用。README 裡所有數字都先回到 repo 查證過（27 關 = 24 石碑刻印 ＋ 2 排序 ＋ 1 工坊、68 技巧 / 15 主題 / 105 條技巧出處 / 24 條官方文件、22 個檢查器、116 段刻印題 345 個選項、12 塊石碑 / 13 則刻文小語 / 22 件器物 / 4 個彩蛋 / 5 座地標、6 首配樂 ＋ 10 支音效、字型子集 1.1 MB）。**踩到一個既有紅線並修掉**：Phase 17 的「介面不提金鑰」規則連 `README.md` 一起掃，初稿寫的 no API key 直接讓 `test:rubric` 亮紅，改成「no model calls」後全綠。**驗證**：`npm run test:rubric` **16,429 個斷言全過**（字型語料指紋未動 —— README 不在語料掃描範圍，所以這次不需要重跑 `npm run fonts`）、`npm run build` 通過；本次只動文件與圖檔，未跑 e2e。**下一步建議**：（a）請 repo 擁有者決定程式授權（建議 MIT，與 `package.json` 一致，加一個 `LICENSE` 檔後把 README 的 TBD 換掉）；（b）GitHub 的 About 描述與 topics 照 CLAUDE.md SEO 段設定；（c）之後若做出線上 demo，README 頂端可以再加一顆「Play now」按鈕與 og-image；（d）截圖是靠種存檔取景的，之後畫面大改時要記得重拍（`docs/media/` 的六張圖沒有自動化驗證）。
- **2026-07-31 · Phase 33（入場門：把「必要的那一下手勢」變成推開一道門，標題卡才有真正的開場）**：Phase 32.5 的兩段式喚醒（第一下喚醒開場曲、第二下才進入）整段拿掉 —— 玩家不知道第一下在做什麼，通常直接連按穿過去，等於什麼都沒得到。改成把那一下手勢**獨立成一道入場門**（新增 `src/ui/entrygate.js` ＋ `.entrygate` 樣式）：一片**完全不透光**的近黑畫面（底色 `#010204` ＋ 一層加色微光，半透明版本會讓 HUD 的「下一個目標／圖鑑／設定」浮在門後面）、一枚會呼吸的金色印記 ✦、一句「推開夜色之門」、一條髮絲線、一行小小的 `enter`，底下一句「按任意鍵，或點一下」；門框是**一直都在**的 1px 髮絲（滑鼠與鍵盤看到同一個東西），受光時才亮起來。**開機的岔路**：`audio.titleIntro()` 先試一次 →（a）同步就 running（返客、或帶 `--autoplay-policy=no-user-gesture-required` 的測試環境）→ **完全跳過這道門**，直接開標題卡、一鍵進場，零摩擦；（b）被政策凍住 → 先出門，同時用新增的 `audio.whenRunning(220)` 再非同步探測一次（有些瀏覽器的 `resume()` 慢一拍才成功），探到其實放行就在玩家看見門的內容之前把它撤掉（門的內容延遲 0.3s 才浮出，撤掉時走同一段淡出並讓標題卡同時淡入，不會閃過底下的世界）。真的被擋住就等玩家推開 —— 那一下 **trusted gesture** 同步解鎖 AudioContext（`onUnlock` 刻意不包進 `setTimeout`／`rAF`，晚一拍瀏覽器就不認），門淡出 600ms，**標題卡這時候才 `open()`**：分字揭示與開場曲同時開始，那才是開場。為此把標題卡改成預設 `hidden`（CSS 揭示動畫要到 `open()` 才起跑），並在門與標題卡都加上 `e.repeat` 守衛（按著不放的自動重複不會穿透到下一層，和序章既有的 `keysArmed` 同一個道理）。**鍵盤優先**：任意鍵都推得開、焦點一開門就落在門上、`role="dialog"` ＋ `aria-label`、`Tab` 留給無障礙導覽、**`Esc` 在入口什麼都不做**（那裡沒有東西可以關）、`prefers-reduced-motion` 下印記與文字改成靜態顯示。護欄 2：入口不放任何連結、不做任何技巧宣稱 —— 它是世界的入口，不是課程。**驗證**：`npm run fonts` 重跑（語料 55 檔 / CJK 1632 字 / 1330.1 KB，指紋測試綠）；`npm run test:rubric` 16429 → **16469 個斷言全過**（新增「入場門」一節的原始碼看門狗：文案／✦／零連結、keydown ＋ pointerdown ＋ Tab ＋ Esc ＋ `e.repeat` 的處理、`onUnlock` 沒有被包進計時器、開機岔路的四條分支都在 `main.js` 裡、Phase 32.5 的 `awaken`／「夜色醒了」／`is-awake` 在 title.js／main.js／CSS 全數清乾淨、標題卡預設 hidden、門完全不透光、內容延遲 0.3s、reduce 下三個元素仍看得見；另外在音訊模組補上 `whenRunning` 在無 AudioContext 時回 false 且有逾時上限）；`npm run test:playtest` **226 個斷言全過**；`npm run build` 通過；`npm run test:e2e` 1697 → **1735 項檢查**（兩輪：1734 過／1733 過，失敗的都是已知的動畫時序 flaky，見下方（d））（e2e 改成可以開第二個瀏覽器：主測試那個仍帶 `--autoplay-policy=no-user-gesture-required`（＝政策放行的路徑，斷言**入場門連開都不開**、`dismissed` 也是 false，不是「開了又關」），結尾新增一段**刻意不帶那個旗標**的第二個 Chrome，用瀏覽器預設政策重現真實首次造訪：AudioContext 真的被凍住 → 門出現且量得到（354×222、零連結、零水平溢位、焦點在門上、`role=dialog`）→ **Esc 什麼都不做** → trusted Enter 推開 → 門收起、`dismissed=true`、AudioContext 變 running、標題卡這時候才登場（`.title` 由 hidden 轉可見）→ 輪詢確認開場曲真的在播且 gain 拉起來（實測 0.32）→ 再一下 Enter 進序章，第一拍仍是「醒來」（推門那一下沒有穿透進來）→ 該情境零 console error）。**下一步建議**：（a）入場門目前只有 `✦`，之後若要更有「門」的形象，可以把那個框做成上緣帶拱的門形（現在是矩形髮絲框）；（b）行動裝置仍未做 —— 觸控搖桿與 720px 以下的版面，入場門本身在窄畫面已驗過無溢位，但整體行動體驗還沒開工；（c）e2e 的第二個瀏覽器目前只跑入場門這一段，之後若還有「和瀏覽器政策有關」的行為（例如剪貼簿權限、通知）可以共用同一段基礎設施；（d）**已知 flaky 誠實記錄**：本次跑了兩輪完整 e2e —— 第一輪 1735 項中 1734 過、唯一失敗是「點起來之後火一直亮著」（火盆亮度 0.75，門檻 0.8）；第二輪 1733 過、失敗兩項：同一條火盆亮度（0.56）＋「玩家自己取消分享時什麼都不說」（toast 數在 340ms 窗內對不上）。兩條都在 AGENTS.md 已列的「動畫時序」家族，都是**固定 sleep ＋ 門檻**的寫法，在這台軟體渲染（SwiftShader，每幀 227ms）又剛好滿載的機器上會掉；和本次改動（標題卡／入場門／音訊探測／CSS）沒有交集，**入場門那 34 項與開機的跳過斷言兩輪都全過**。要根治就把這兩條改成輪詢式斷言（poll until），不要用固定 sleep。

- **2026-08-01 · Phase 34（開場重做：黑幕 ＋ 呼吸燈 ＋ 打字機）**：依站長回饋修三件事。**① 進場之前，3D 世界一眼都不准被看到**——新增黑幕 `#bootcover`，節點與樣式**寫在 `index.html` 行內**（`styles.css` 是 `main.js` import 進來的，那之前的每一幀都保護不到），第一幀就 `position:fixed; inset:0` 蓋滿、實色 `#010204`、`z-index:35`（世界／HUD ≤30 之上，標題卡 40 與入場門 45 之下），`pointer-events:none` 所以不吃滑鼠；引擎照樣在底下暖機（shader 編譯、地形生成都在這段時間做完），只是沒人看得到。**掀開只有一個時機**：標題卡按下開始 → 1.4 秒 expo-out 淡出（像劇場的燈慢慢亮起來）→ 1.8 秒後把節點移除；推開入場門**不**掀（門後面是標題卡，不是世界）。e2e 用 `elementsFromPoint` 證明黑幕真的疊在 `<canvas>` 上面（不是只有 opacity 好看）。**② 入場門極簡化**：原本的「印記＋外框＋中文一行＋英文 enter 一行」整組換成**一盞呼吸燈**——13px 的暖金光點 ＋ 兩層外散光暈，5.6 秒一次的慢呼吸（比心跳慢一半，要像睡著的東西不是待機指示燈）；三樣東西**一件一件**出現：光（0.35s）→「推開夜色之門」（1.5s，純濃度變化不位移）→「點擊進入⋯」（2.9s），全部延遲 ≥0.3s，所以「探到自動播放其實放行、220ms 內撤掉這道門」那條路上不會有字閃過去；鍵盤的人靠 `sr-only` 的「或按任意鍵」＋焦點落在門上，`aria`／`role=dialog`／reduce-motion 靜態顯示全部保留。**推開那一下現在有聲音**：`audio.cue('gateOpen')` 掛在 `onUnlock` 的同一拍（有音檔是 `sfx_unlock_door.m4a` 的石門滑開，沒有就退回 87 Hz 的低頻合成版）——這是玩家在這個世界裡聽到的第一個聲音，開場曲第一個音進來時它剛好散掉；`audio.debug()` 新增 `cues`／`lastCue` 環狀紀錄（12 筆）讓測試看得到「到底有沒有響」。**③ 標題卡重做**：分字揭示（9 個 span 各自彈一下）**整組移除**，改成 `Promptasy` 從 `blur(11px)` 慢慢對焦（1.7s）；定位句與中文句改成**打字機**——JS 驅動、用展開運算子切字元所以中文一個字算一個，中英各有自己的速度（Latin 34ms／CJK 72ms），帶一根 1px 星光色的閃爍游標，分鏡是「名字對焦（0.15s）→ 髮絲線（1.0s）→ 定位句打字（1.35s）→ 中文句 → 開始鍵浮出（`.is-ready`）」約 4.9 秒；**底部那行「68 條技巧 · 27 個關卡 …」連同樣式整條刪掉**。打字機**永遠不擋輸入**：任何時候按下去都直接進場，兩句話在同一拍被 `finishTyping()` 補完（不會有半句話淡出去）；`prefers-reduced-motion` 下不打字直接顯示、游標不畫；完整句子另外放一份在 `.sr-only`（螢幕閱讀器不會念到打一半的字，`textContent` 也永遠是完整句）。另補 `title.hide()`（收起來但不觸發 `onStart`）給重播動畫的測試用。**驗證**：`npm run fonts` 重跑（語料 55 檔 / CJK 1634 字 / 1331 KB，指紋測試綠）；`npm run test:rubric` 16469 → **16502 個斷言全過**（新增黑幕的行內樣式／層級／淡出秒數／只在標題卡掀開、呼吸燈的延遲與循環、三樣東西的先後、`gateOpen` 掛在手勢同一拍、分字揭示與底部統計已移除、中文打得比英文慢、`finishTyping` 真的接在 `start()` 上）；`npm run test:playtest` 226 全過；`npm run build` 通過；`npm run test:e2e` 1735 → **1786 項**（新增 51 項：開機第一幀就有黑幕且疊在畫布上／入場門與標題卡兩段都還蓋著／按下開始才掀且淡完自己移除、標題卡沒有分字 span 與底部統計、兩句話的 sr-only 完整、打到一半按下去會被補完、重播整段開場量到「開頭模糊 → 結尾對焦」與「一句一句打」、入場門的呼吸燈／文案／三拍先後／`gateOpen` 真的響了）；動畫時序類斷言一律改成**輪詢**（AGENTS.md 的建議），不用固定 sleep。順手用 headless 重拍 `docs/media/title.png`。**已知 flaky 誠實記錄**：跑了兩輪完整 e2e —— 第一輪 1782 過 3 失敗（含我自己新寫的「點擊進入⋯到齊」用了固定 sleep，已改成輪詢）；第二輪 **1784 過、2 失敗**，兩條都是同一組滑鼠拖曳（「把角色與目標搬到最上面」「搬對的立刻標成位置對了」），屬 AGENTS.md 已列的「拖曳」家族，寫法是固定 sleep ＋ 逐格 `mouseMoved`，在這台軟體渲染（SwiftShader，每幀 195.7ms）的機器上兩輪都掉；和本次改動（開場／黑幕／音效紀錄）沒有交集。**下一步建議**：（a）把那組拖曳斷言也改成輪詢式（poll until 排序真的變了），把 e2e 最後兩條不穩定的斷言收乾淨；（b）黑幕目前是純黑，之後可以在淡出時多一層「由中心往外亮起來」的遮罩動畫，讓世界第一次出現更像鏡頭推進；（c）行動裝置仍未做（觸控搖桿、720px 以下的開場版面）；（d）入場門的呼吸燈可以和開場曲的第一個長音同步（現在燈的節奏與音樂各走各的）。
- **2026-08-01 · v2 Phase 0（基線與契約鎖定，dev 分支）**：依 `task_plan.md` 完成課程 v2 前置：機器產生 27 關遷移 manifest（`docs/design/curriculum-v2-migration.json`，逐關 primaryTechniqueId／主檢查／地基檢查／pass −0.5 前後值／teaches legacy 處置，附 8 條設計與資料的衝突裁決）；D1–D3 契約裁決入檔（保留 5／改造 20／應用 2、`primaryTechniqueId` ＋ legacy 收集層、literal −0.5 ＋ playtest 安全門）；修復 12 條過期的開場斷言（改斷言現行「呼吸燈入場門＋逐行淡入標題」契約，先紅後綠驗證）；新增 `curriculum.json` sha256 不可變測試與 manifest 驗證測試。基線實跑：rubric **16,490→17,479 全綠**、playtest 226、build ✓；e2e 依成本策略 Phase 0 跳過（純測試＋文件變更），首跑排在 Phase B。實測世界效能 142,664 tris／45 lights（文件舊數字已記勘誤）。**下一步**：Phase A 重複度手術（依 manifest 執行，hasDelimiters 只降 2 關——逐關表優先於摘要）。
- **2026-08-01 · v2 Phase A（重複度手術，dev 分支）**：27 關依 Phase 0 manifest 逐關執行、零偏差——`assignsTask` 全數降 0.5 標地基、`hasDelimiters` 依裁決只降 2 關（3 關 hold）、1 移除 5 替換（權重中性）、pass 全面 literal −0.5（首見小數門檻，新增 `formatScore()` 統一顯示）、25 關掛 `primaryTechniqueId`（2 應用關維持 null 多教學）。第二幕改為**只呈現主技巧**的刻文與神諭原典（其餘檢查收斂成「順手會用到」一行、無教學塊無出處）、第三幕對照表分 primary/foundation/minor 三層；收集仍走 legacy `teaches`（D2，68/68 覆蓋不回退）。flows 同步收斂 6 段（silent-thinker 以 reasoning-02 成功標準段替換、mask-workshop 任務句改寫供新 `hasAudience` 列計分——改內容不弱化引擎）。rubric 17,479→**17,705**、playtest 226→**263**、fonts 重切、build ✓;**e2e 1,811 項零 console error 零 flaky**——順手修復 Phase 34.5 以來套件不可跑的三處既有壞損（打字機舊斷言／reload 競態／entrygate hint 殘留），並根治拖曳 flaky（真因：殘留 overlay 吃掉 pointer events，以 CDP 獨立重現證明後修復）。**R1 檢查點達成**。**下一步**：Phase B——v2 catalog bridge（skill-codex-v2／regions-v2／loader）＋ `fix`/`spot` 新題型＋ foundations 14 座。
- **2026-08-01 · v2 Phase B（catalog bridge ＋ fix/spot ＋ foundations 14，dev 分支）**：**B1**：新增 authored 資料層 `skill-codex-v2.json`（130 技能，來源由程式從大清單逐條解析出 445 列官方出處、235 個相異 URL、零杜撰）與 `regions-v2.json`（12 區，既有 5 區 id 不變），單一 loader `catalog.js` 開機驗證即擲錯（130/12/先修無環/來源形狀/區域加總），runtime 與測試全面去硬編碼（歷史快照斷言改 invariant ＋ `expected-counts.json` 契約檔）。**B2**：新題型 `fix` 改碑（弱稿逐句替換、Esc 三段還原）與 `spot` 點碑（點出有問題的句子），同一 rubric／手掌印／不失敗文法，缺 kind 預設 choice 契約不變；foundations 補 10 座新神廟達 **14 座教學神廟**（每座 1 主檢查 w3 ＋ 1 地基 w0.5、來源自 catalog 真實官方連結、位置經地形/淨空/碰撞驗證）；新增 5 個結構性檢查器（noUndefinedReference/statesScope/avoidsPressureLanguage/disambiguatesTerms/namesComponents）；存檔新增 additive `skillsV2[]`（D2 收集不回退）。**石座光源改 8 燈池**（59→26 盞,燈數與關卡數解耦——後續階段的規模前提）。驗證：rubric 21,393→**25,877**、playtest **396**、fonts 重切（CJK 1,696）、build ✓、**e2e 1,920 項零 console error**（新題型鍵盤全流程先紅後綠;誠實記錄:5 座神廟的 tradeoff/constraint/reverse/disclose 題型待該 kind 上線後於後續階段換裝,文字/rubric/來源不動）。**下一步**：Phase C——`induct`/`tradeoff` 題型 ＋ reasoning 15 座。
- **2026-08-01 · v2 Phase C（induct/tradeoff ＋ reasoning 15，dev 分支）**：新題型 **`induct` 推規**（範例牆一組一組揭示、猜錯就地教學不前進、資料層強制「第四例真的在驗證規則」）與 **`tradeoff` 雙面碑**（兩面都能前進、各得誠實評語、輸的一面不得被寫成『錯』、兩面必須各贏至少一張卡——不把取捨教成假通則），皆為 choice 變體共用新抽出的 `slots.js` 刻寫台與同一 palm 結尾，資料不合形狀自動退回 stele。reasoning 區達 **15 座教學神廟**（改造 5 ＋ 新增 10），foundations 兩座佔位關換裝真 tradeoff;新增 4 檢查器（justifiesExampleCount/labelsNegativeExample/asksForRationaleNotTranscript/asksMultipleSamples），`keepsPromptLean` 補「愈詳細愈好」鷹架偵測。驗證：rubric 25,877→**29,846**、playtest **554**、fonts CJK 1,721、build ✓、**e2e 2,010 項零 console error**;第一輪紅燈抓到真 bug（CDP 原生鍵不觸發按鈕預設 click,induct/tradeoff 補 Enter 處理）;**拖曳測試根因確定**——order.js 的 FLIP 動畫讓 rect 量到移動前版面,軟渲染 168ms/幀下形成確定性失敗,測試側改逐輪重量測修正(非 flaky)。誠實記錄:三座神廟題型待 G/H 期換裝（well-pause→multi、effort-forge→sim）、three-wells 依內容本質改用 choice 非 workshop。**下一步**：Phase D——`constraint` 題型＋grounding/config 補齊＋行動版還債點。
